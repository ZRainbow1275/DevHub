# 04 — 进程模块需求表

> **填写时长**: 约 10–15 分钟
> **重要程度**: 直接回应"卡片状态权限不足 + 卡片列表显示不一致"两大痛点
> **依赖**: 完成元层 / 全局体验 / 主题
> **当前状态参考**: `99-research-snapshot.md` 第 3 项 — Card 用 ProcessRelationship API，List 用 ProcessDeepDetail API，**两套 API 字段不一致**

---

## A. 视图模式与统一性

### [Q-4.A.1] 进程视图模式  锚定 [Must]

**背景**: 当前已有"卡片"和"列表"两种模式，但底层 API 不一致。R8 需明确：保留几种模式？是否合并实现？

**选项**:
- A. **仅卡片**（统一为单视图，简化）
- B. **仅列表**（密度更高）
- C. **卡片 + 列表**（保持双视图，但统一底层 API）— **推荐**
- D. **卡片 + 列表 + 树形（按父子进程）**
- E. **卡片 + 列表 + 树形 + Treemap（按 RSS 占比可视化）**

**推荐默认**: E（视图越多越能给用户选择，但要求底层 API 完全统一）

**影响范围**: 选 C/D/E 必须先完成"detail API 合并"，对应 R7 RCA 中的 ProcessDetailPanel vs ProcessDetailDrawer 双线问题。

**用户回答**: ____________E____________

---

### [Q-4.A.2] Card / List 详情面板的统一策略  锚定 [Must]

**背景**: 当前问题是 Card 走 `ProcessRelationship`（轻量）+ List 走 `ProcessDeepDetail`（重型）。

**选项**:
- A. **统一为 Deep**（永远调 deep，两个视图渲染同样字段）
- B. **统一为 Relationship**（永远调轻量，按需展开 deep）
- C. **分层 ViewModel**（一份 viewmodel 同时含轻 / 重 字段，按视图取用）
- D. **首次显示轻 + 用户点"深度查询"才取 deep**

**推荐默认**: C + D 混合（默认轻，进阶按钮取深，但 viewmodel 已分层）

**用户回答**: _________C + D 混合（默认轻，进阶按钮取深，但 viewmodel 已分层）_______________

---

### [Q-4.A.3] 详情字段清单（统一后用哪些字段）  协商 [Should]

**期望字段**（可勾选）：

#### 基础（必有）
- [x] PID / PPID
- [x] 进程名 / 可执行路径
- [x] 命令行
- [x] 工作目录
- [x] 启动时间 / 运行时长
- [x] 用户 / SID
- [x] CPU% / RSS / WS / 句柄数 / 线程数

#### 进阶
- [x] 加载的模块（DLL）列表
- [x] 打开的文件 / 句柄
- [x] 网络连接（端口）
- [x] 注册表键打开数
- [x] 环境变量
- [x] 进程树（父 / 子 / 兄弟）
- [x] 数字签名 / Authenticode 验证
- [x] 服务关联（Windows Service）
- [x] AppContainer / UAC Level
- [x] WMI 详细字段（CommandLine / CreationClassName / Caption）
- [x] GPU 占用（NVAPI / DXGI）

#### 安全/权限
- [x] Integrity Level（Low / Medium / High / System）
- [x] Token 信息（用户/组）
- [x] 是否提权运行
- [x] DEP / ASLR 状态

**推荐默认**: 基础全选 + 进阶勾选（模块、文件、网络、进程树、数字签名、服务、WMI）+ 安全（Integrity、提权、DEP/ASLR）

**用户回答**: ____________全部全选，必须全部做到，这才是能让产品产生差异的东西____________

---

## B. 权限提升（UAC）流程

### [Q-4.B.1] 权限不足时的提示策略  锚定 [Must]

**背景**: 用户报告"卡片状态资源详情显示权限不足"。R8 必须明确提权交互。

**选项**:
- A. **静默降级**（隐藏需要权限的字段，显示"需要权限"小图标）
- B. **横幅提示**（详情顶部 "提升权限以查看完整信息" + 按钮）
- C. **弹窗强制选择**（每次访问都弹）
- D. **首次提示**（首次提权后记忆 24 小时）
- E. **永久记忆**（用户授权后永久以管理员模式运行）

**推荐默认**: B + D（顶部横幅 + 24 小时记忆）

**影响范围**: 选 E 安全风险大；选 D 平衡。

**用户回答**: _________ B + D（顶部横幅 + 24 小时记忆）_______________

---

### [Q-4.B.2] 提权方式  协商 [Should]

**选项**:
- A. **重启 DevHub 为管理员**（粗暴但简单）
- B. **单次 spawn 提权子进程**（仅查询单进程时提权，主进程保持普通用户）
- C. **辅助 Service（NT AUTHORITY\SYSTEM）**（DevHub 启动时安装 Windows Service，主进程通过命名管道通信）

**推荐默认**: B（最佳安全平衡）；C 作为未来探索

**影响范围**: B 需做 IPC 跨进程权限模型，工作量中等；C 工作量高但是企业级。

**用户回答**: __________B______________

---

### [Q-4.B.3] 哪些字段必须提权才能取  委托 [Could]

**期望勾选**（这些字段在普通用户下不可读，需要 Administrator）：
- [x] 系统进程的命令行
- [x] 其他用户进程的内存详情
- [x] 服务进程的句柄列表
- [x] System Integrity 进程的所有信息
- [x] WMI 的部分字段（如 ParentProcessId 在某些场景需要）

**推荐默认**: 全部勾选

**用户回答**: ____________全部勾选____________

---

## C. 操作清单（Operations Catalog）

### [Q-4.C.1] 进程操作清单  锚定 [Must]

**当前**: 仅"释放/结束"等少量操作。R8 扩展。

**选项**（勾选要支持的操作）：

#### 基础
- [ ] **结束进程（Kill）** — 确认 + 强杀
- [ ] **优雅终止（Graceful Stop）** — 发送 CTRL+C / SIGTERM 等
- [ ] **挂起（Suspend）** / 恢复（Resume）
- [ ] **重启** — 终止 + 按原 cmdline 重启

#### 调度
- [ ] **设置 CPU 亲和性**（Affinity Mask）
- [ ] **设置优先级**（Idle / BelowNormal / Normal / AboveNormal / High / RealTime）

#### 调试
- [ ] **创建内存转储**（procdump 集成）
- [ ] **附加调试器**（VS / WinDbg）
- [ ] **打开模块路径**（资源管理器定位）
- [ ] **复制命令行 / PID / 路径**

#### 关联跳转
- [ ] **打开关系视图**（直接跳转到拓扑）
- [ ] **查看占用的端口**
- [ ] **查看关联窗口**
- [ ] **查看父进程 / 子进程**
- [ ] **添加到"我的进程"标签**

#### 高级
- [ ] **进程注入（Inject DLL）** — 谨慎
- [ ] **修改环境变量** — 仅子进程派生时
- [ ] **写入文件 handle 列表到剪贴板**

**推荐默认**: 基础全选 + 调度全选 + 调试前 3 项 + 关联跳转全选 + 高级首项不选（注入风险大）

**用户回答**: _______________基础全选 + 调度全选 + 调试前 3 项 + 关联跳转全选 + 高级首项不选_______

---

### [Q-4.C.2] 操作触发方式  协商 [Should]

**选项**（可多选）：
- A. 卡片右上角下拉菜单
- B. 列表行右键菜单
- C. 详情面板顶部工具栏
- D. 命令面板（Cmd+K → "kill PID xxxx"）
- E. 拖拽（拖到"垃圾桶图标"=结束）
- F. 键盘快捷键（选中 + Delete = 结束）

**推荐默认**: A + B + C + D + F（不选 E，拖拽风险大）

**用户回答**: _________A + B + C + D + F_______________

---

### [Q-4.C.3] 危险操作的二次确认  锚定 [Must]

**选项**:
- A. **结束系统进程必须确认**（PID < 1000 / 关键服务）
- B. **结束所有进程都需确认**
- C. **配置白名单**（用户可关闭某些类型的确认）

**推荐默认**: A + C

**用户回答**: _____________A + C___________

---

## D. 过滤 / 分组 / 排序

### [Q-4.D.1] 过滤维度  协商 [Should]

**选项**（可多选）：
- A. 进程名 / 路径关键字
- B. PID 范围
- C. 用户（System / Administrator / 当前用户 / 其他）
- D. CPU% / RSS 阈值
- E. 是否含 AI 工具特征（codex / claude / gemini / cursor / copilot）
- F. 父进程链（按 PPID 树过滤）
- G. 是否监听端口
- H. 是否有窗口
- I. 启动时间窗
- J. 数字签名验证状态
- K. Integrity Level

**推荐默认**: A + B + C + D + E + G + H

**用户回答**: ____________ A + B + C + D + E + G + H____________

---

### [Q-4.D.2] 过滤组合的保存  委托 [Could]

**选项**:
- A. 不保存
- B. 保存为命名预设（"我的开发进程" / "AI 工具"）
- C. 保存为预设 + 一键切换

**推荐默认**: C

**用户回答**: __________C______________

---

### [Q-4.D.3] 分组维度  委托 [Could]

**选项**（可多选）：
- A. 不分组（扁平列表）
- B. 按用户
- C. 按父进程（树形）
- D. 按 EXE 路径（同一可执行文件归一）
- E. 按 项目（按 cwd 推断）
- F. 按 AI 工具类型
- G. 按 Integrity Level

**推荐默认**: A 默认 + C/D/F 用户可切换

**用户回答**: _________A 默认 + C/D/F 用户可切换 + 可自定义自分组_____________

---

### [Q-4.D.4] 排序维度  委托 [Could]

**选项**（可多选）：
- A. CPU% 降序（默认）
- B. RSS 降序
- C. PID 升序
- D. 启动时间降序
- E. 进程名字典序

**推荐默认**: A 默认 + B/C/D/E 用户可切换

**用户回答**: ____________A 默认 + B/C/D/E 用户可切换 + 用户可自定义自切换____________

---

## E. 用户标签（User Tags）

### [Q-4.E.1] 是否支持用户为进程打标签  协商 [Should]

**背景**: 用户可手动给某进程加标签（"我的 dev server" / "项目 X" / "重要不要 kill"）。

**选项**:
- A. 不支持
- B. 仅按 EXE 路径标签（同名进程下次启动自动继承）
- C. 按 EXE 路径 + cwd 双键
- D. 按 cmdline hash

**推荐默认**: C（最稳）

**用户回答**: ____________C____________

---

### [Q-4.E.2] 标签的显示位置  委托 [Could]

**选项**: A. 卡片角落 Badge / B. 列表行尾 / C. 详情顶部 / D. 全选

**推荐默认**: D

**用户回答**: _________D_______________

---

## F. 历史与时间线（Process History）

### [Q-4.F.1] 进程历史追踪  委托 [Could]

**背景**: 类 Process Hacker 的"History" 选项卡。

**选项**:
- A. 不追踪
- B. 仅当前会话出现/消失时间戳
- C. 24 小时滑窗内所有事件
- D. C + 资源占用 Sparkline

**推荐默认**: D

**用户回答**: ___________D_____________

---

### [Q-4.F.2] 历史的可视化形式  委托 [Could]

**选项**:
- A. 文本时间线
- B. 横条时间线（Gantt 风）
- C. Sparkline 矩阵
- D. 全选

**推荐默认**: D

**用户回答**: _________D_______________

---

## G. AI 工具特征识别

### [Q-4.G.1] 哪些进程被视为"AI 工具进程"  锚定 [Must]

**背景**: AI 任务模块依赖此识别。

**选项**（识别规则，勾选要启用的）：
- [ ] 进程名包含 `codex` / `claude` / `gemini`
- [ ] 命令行含 AI 工具特征词
- [ ] 父进程是终端（Windows Terminal / cmd / pwsh / bash）
- [ ] 子进程派生模式（短时间多个 git/npm/python 子进程）
- [ ] 注入式识别（用户手动标记）
- [ ] 通过窗口标题识别（"Claude Code - xxx"）

**推荐默认**: 全部勾选 + 多信号融合

**用户回答**: _________全部勾选 + 多信号融合_______________

---

### [Q-4.G.2] AI 进程的特殊处理  协商 [Should]

**选项**（可多选）：
- A. 不允许误 kill（额外确认）
- B. 自动加入"AI 任务监控"列表
- C. 卡片高亮特殊 Badge
- D. 自动关联到正在运行的 CSV 任务批次
- E. 状态栏单独计数

**推荐默认**: 全选

**用户回答**: ____________全选____________

---

## H. 关系视图（Topology）入口

### [Q-4.H.1] 关系视图入口位置  锚定 [Must]

**背景**: 用户认为"原本的设计消失了"。R7 已实现，但入口不显眼。

**选项**:
- A. **当前**：详情面板内部"关系视图"子 Tab（不变）
- B. **加引导**：保持子 Tab，但首次访问时显示气泡 Tooltip "点这里看关系图"
- C. **首屏即展示**：详情面板默认展开关系视图（其他子 Tab 折叠）
- D. **顶部 Tab 按钮**：详情面板顶部独立大按钮"关系视图" + 弹出全屏图
- E. **卡片角标**：进程卡片右上角小图标 →点击直接进关系视图全屏

**推荐默认**: B + E（保持现状但加发现性）

**用户回答**: _________B + D + E_______________

---

### [Q-4.H.2] 关系视图默认深度  协商 [Should]

**选项**: A. depth=1 / B. depth=2 / C. depth=3 / D. 用户上次选择记忆

**推荐默认**: B + D（默认 2，记忆用户偏好）

**用户回答**: _________全选，默认2_______________

---

### [Q-4.H.3] 关系视图显示哪些边类型  协商 [Should]

**选项**（可多选）：
- A. **owns**（进程拥有窗口/端口/句柄）
- B. **parents**（进程父子关系）
- C. **listens**（进程监听端口）
- D. **connects**（进程作为客户端连出）
- E. **binds**（进程绑定 IP/端口）

**推荐默认**: 全选

**用户回答**: _________全选_______________

---

## I. 进程模块小结

**Q-4.I.1**: 上述 H 节中，进程关系视图您最希望"第一眼看到"什么？（自由填写）：____________父子关系____________

**Q-4.I.2**: 您觉得"卡片"模式相比"列表"应该独有的功能是？（自由填写）：__________过滤和更加详尽的内容______________

**Q-4.I.3**: 上述以外，进程模块还有什么必须明确的？自由填写：________________________
