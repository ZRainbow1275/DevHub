# 05 — 端口模块需求表

> **填写时长**: 约 10–15 分钟
> **重要程度**: 直接回应"端口卡片太小，要做悬浮卡片"主诉
> **依赖**: 完成元层 / 全局体验 / 主题 / 进程
> **当前状态参考**: `99-research-snapshot.md` 第 4 项 — 当前 420px 固定面板，无 popout

---

## A. 视图模式

### [Q-5.A.1] 端口视图模式  锚定 [Must]

**当前**: 端口列表 + 右侧 PortFocusPanel（420px 固定）。用户感知"全屏下显示紧张"。

**选项**:
- A. **保持现状**（左列表 + 右固定面板）— 不解决问题
- B. **左列表 + 右悬浮卡片**（用户可关闭右栏）
- C. **网格卡片**（替代列表，每端口一卡）
- D. **A + 悬浮卡片混合**：默认 A，用户可"摘出"成 popout
- E. **端口树形**（按"项目→端口"分层）

**推荐默认**: D（保持习惯 + 提供 popout）

**影响范围**: D 需引入悬浮卡片框架（见 [Q-2.B.3]）

**用户回答**: ____________D____________

---

### [Q-5.A.2] 默认视图  协商 [Should]

**选项**: A. 列表 / B. 卡片 / C. 用户上次选择

**推荐默认**: C

**用户回答**: _______________C_________

---

## B. 悬浮卡片（Pop-out）系统

### [Q-5.B.1] 悬浮卡片的触发方式  锚定 [Must]

**选项**（可多选）：
- A. **Hover 预览**（悬停 1s 显示浮卡 / 离开消失）
- B. **Click 锁定**（点击端口卡片 → 浮卡固定 / 再点关闭）
- C. **拖拽启用**（拖出列表项 → 自动浮卡）
- D. **右键菜单 → "悬浮显示"**
- E. **快捷键**（选中端口 + Cmd+P → 浮卡）

**推荐默认**: A + B + D（不要 C，避免误操作）

**用户回答**: ____________A + B + C + D____________

---

### [Q-5.B.2] 悬浮卡片的可操作性  锚定 [Must]

**选项**:
- A. **只读浮卡**（仅显示信息，无操作）
- B. **可释放端口**（含释放按钮，但仅端口操作）
- C. **完整操作**（释放 / 跳转进程 / 查看关系 / 复制 / 加白名单等）

**推荐默认**: C

**用户回答**: ______C__________________

---

### [Q-5.B.3] 悬浮卡片之"摘出"  锚定 [Must]

**背景**: 用户原话"摘出来的悬浮卡片"。

**选项**:
- A. **仅渲染层 Floating（z-index）**（DevHub 内部，不出主窗口）
- B. **真实 Electron BrowserWindow 独立窗口**（可移到第二屏）
- C. **A 默认 + B 升级**（默认 A，"在新窗口打开"按钮升级到 B）

**推荐默认**: C

**影响范围**: B 需要主进程支持多窗口 + IPC 桥接，工作量较大

**用户回答**: ____________C____________

---

### [Q-5.B.4] 悬浮卡片的初始位置  委托 [Could]

**选项**:
- A. 鼠标光标位置
- B. 屏幕中心
- C. 屏幕右下角（离主区不挡）
- D. 用户拖拽过的位置记忆（按端口号 hash）

**推荐默认**: A 触发位置 + D 后续记忆

**用户回答**: __________A 触发位置 + D 后续记忆______________

---

### [Q-5.B.5] 悬浮卡片的最大数量  委托 [Could]

**选项**: A. 1 / B. 3 / C. 5 / D. 不限

**推荐默认**: C（避免屏幕被刷屏）

**用户回答**: _________C_______________

---

## C. 端口字段清单

### [Q-5.C.1] 端口卡片显示字段  协商 [Should]

**字段池**（勾选默认显示的）：

#### 必有
- [x] 端口号
- [x] 协议（TCP / UDP）
- [x] 状态（LISTENING / ESTABLISHED / TIME_WAIT 等）
- [x] 占用 PID
- [x] 占用进程名
- [x] 端口标签（COMMON_DEV_PORTS：3000=React Dev / 5173=Vite 等）

#### 可选
- [x] 远端地址（ESTABLISHED 时）
- [x] 远端端口
- [x] 本地接口（127.0.0.1 / 0.0.0.0 / ::1）
- [x] 监听用户
- [x] 启动时间（端口绑定时刻）
- [x] 累计连接数
- [x] 当前连接数
- [x] 数据流量（rx / tx 字节）— 需深度集成
- [x] 安全等级（本地 / LAN / WAN / 公网可达）

**推荐默认**: 必有 + 远端地址 + 本地接口 + 当前连接数 + 安全等级

**用户回答**: _________全选_______________

---

### [Q-5.C.2] 详情卡片（Pop-out 内）的扩展字段  委托 [Could]

**额外字段**:
- [ ] 历史连接列表（最近 100 条）
- [ ] HTTP 包嗅探（仅当端口为 HTTP/HTTPS）— 通过 mitmproxy MCP
- [ ] mDNS / Bonjour 服务名（如果广播了）
- [ ] WebSocket 帧统计

**推荐默认**: 历史连接 + 可选 HTTP 嗅探（用户授权）

**用户回答**: ___________全选，危险内容走用户授权_____________

---

## D. 安全分级

### [Q-5.D.1] 端口安全分级方案  锚定 [Must]

**等级定义**（默认）：

| 等级 | 含义 | 视觉徽章 |
|------|------|---------|
|  Local | 仅 127.0.0.1 / ::1 | 绿色徽章 "仅本地" |
|  LAN | 192.168.x.x / 10.x.x.x / 172.16-31.x.x | 黄色徽章 "局域网可访问" |
|  WAN-Capable | 0.0.0.0 / :: 监听全部接口 | 橙色徽章 "外部可访问" |
|  Suspicious | 已知挖矿/木马端口 / 异常远端连接 | 红色徽章 "可疑" |

> 注：上面的色块 emoji 仅作 markdown 标识，**不进入产品 UI**，UI 走 lucide-react ShieldIcon + 颜色 token。

**用户回答**:
- A. 接受默认四档
- B. 我有调整：__________A______________

---

### [Q-5.D.2] 端口黑名单  委托 [Could]

**背景**: 列出常见可疑端口（4444 / 6666 / 6667 等）。

**选项**:
- A. 不维护黑名单
- B. 内置黑名单（约 30 个常见可疑端口）
- C. 内置 + 用户可补充

**推荐默认**: C

**用户回答**: _________C_______________

---

### [Q-5.D.3] 安全提示位置  委托 [Could]

**选项**:
- A. 卡片角标
- B. 详情卡片顶部 banner
- C. 详情卡片专属"安全"分区
- D. 状态栏聚合（"3 个公网端口"）
- E. 全选

**推荐默认**: E

**用户回答**: _________E_______________

---

## E. 端口冲突可视化

### [Q-5.E.1] 冲突检测策略  协商 [Should]

**当前**: `getConflictingPorts` 检测同端口多 PID LISTENING（PortView.tsx:30）。

**扩展选项**:
- A. **保持现状**（仅同端口多 PID）
- B. **A + 端口被一个进程占用，但另一进程命令行也含相同端口号**（潜在意图冲突）
- C. **B + 跨项目冲突**（两个项目 cwd 不同但都试图绑相同端口）

**推荐默认**: B

**用户回答**: _________B_______________

---

### [Q-5.E.2] 冲突的解决建议  委托 [Could]

**选项**:
- A. 仅显示冲突 Badge
- B. 提供"释放冲突方"按钮
- C. 提供"查看关系图"按钮（拓扑高亮）
- D. 全选

**推荐默认**: D

**用户回答**: ____________D____________

---

## F. 端口与项目自动关联

### [Q-5.F.1] 关联策略  协商 [Should]

**背景**: 当某进程的 cwd 匹配某项目时，自动把该端口归属该项目。

**选项**:
- A. **不自动关联**（用户手动）
- B. **按 cwd 匹配**（精确）
- C. **按 cwd 匹配 + 父子进程链推断**（如 npm 子进程派生 node）
- D. **C + 用户手动覆盖**

**推荐默认**: D

**用户回答**: ____________D____________

---

### [Q-5.F.2] 关联后的展示  委托 [Could]

**选项**:
- A. 端口卡片显示项目 Badge
- B. 项目卡片显示"占用端口"列表
- C. A + B
- D. 不显示

**推荐默认**: C

**用户回答**: _________C_______________

---

## G. 端口操作清单

### [Q-5.G.1] 端口操作  锚定 [Must]

**选项**（勾选）：
- [ ] **释放（kill 占用进程）**
- [ ] **跳转占用进程详情**
- [ ] **查看关系视图**
- [ ] **复制端口号 / 完整地址 / curl 模板**
- [ ] **打开浏览器**（http://localhost:port）
- [ ] **添加到收藏**（顶部固定栏）
- [ ] **导出为 .http 文件**（可在 IDE REST Client 用）
- [ ] **嗅探流量**（启动 mitmproxy 拦截）— 需用户授权
- [ ] **切到端口的关系图全屏**

**推荐默认**: 全选（嗅探需用户授权）

**用户回答**: ____________全选____________

---

### [Q-5.G.2] 危险操作确认  委托 [Could]

**选项**: A. 释放系统端口必须确认 / B. 释放任何端口必须确认 / C. 用户白名单

**推荐默认**: A + C

**用户回答**: _________A + C_______________

---

## H. 端口查询超时与降级

### [Q-5.H.1] 查询超时策略  协商 [Should]

**背景**: R6-P3.1 报告"查询超时显示缓存数据"。R7 已规划但 UX 文案需明确。

**选项**:
- A. **静默降级**（仅显示缓存，无提示）
- B. **黄色 banner 提示**（"查询超时 - 显示 N 秒前的缓存数据"）— 当前
- C. **B + 可手动重试按钮**
- D. **C + 自动 30s 后再次刷新**

**推荐默认**: D

**用户回答**: _________D_______________

---

### [Q-5.H.2] 缓存最大年龄  委托 [Could]

**选项**: A. 30s / B. 1min / C. 5min / D. 不限（直到下次成功）

**推荐默认**: B

**用户回答**: ____________B____________

---

## I. 关系视图入口

### [Q-5.I.1] 关系视图入口位置  锚定 [Must]

**选项**:
- A. **保持当前**（PortFocusPanel 内部"关系视图"折叠段）
- B. **A + 卡片角标**（端口卡片右上角小图标 → 直接进图全屏）
- C. **悬浮卡片内置子 Tab**（pop-out 中含 "信息 / 关系" 两 Tab）
- D. **B + C 全部**

**推荐默认**: D

**用户回答**: ______D__________________

---

## J. 端口模块小结

**Q-5.J.1**: 您日常最关心的端口操作是？（自由填写）：______释放和是否冲突__________________

**Q-5.J.2**: 您是否希望 DevHub 在某端口被绑定/释放时弹通知？（选项：A. 全部弹 / B. 仅安全等级=橙/红时弹 / C. 仅黑名单端口弹 / D. 不弹）：____________A____________

**Q-5.J.3**: 上述以外，端口模块还有什么必须明确的？自由填写：________________________
