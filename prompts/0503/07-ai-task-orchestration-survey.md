# 07 — AI 任务编排需求表（R8 最大模块）

> **填写时长**: 约 30–45 分钟（最重要 + 最长）
> **重要程度**: ⭐⭐⭐⭐⭐ 用户在 R8 的核心扩展诉求
> **依赖**: 完成元层 / 全局 / 进程 / 端口 / 窗口
> **当前状态参考**: `99-research-snapshot.md` 第 5、6 项 — 6 信号融合已实现但需调优；进度纯启发式无 CLI 集成
>
> **本文档涵盖 8 大主题**：感测引擎调优 / 进度可信度 / 监控窗口 / SKILL 库 / CSV 任务驱动 / Watchdog / 自动注入 / 任务编排 DAG

---

## A. 感测引擎调优（用户主诉"误报/瞎报/错报/迟报/漏报"）

### [Q-7.A.1] 当前 6 信号融合保留 / 增强 / 替换  锚定 [Must]

**当前实现**: AITaskTracker 使用 6 信号 — CPU variance、I/O write rate、子进程派生、终端 title 模式、输出速率、时间阈值。

**选项**:
- A. **保留 6 信号**，仅调权重
- B. **保留 6 + 增加 4 信号**（屏幕 OCR、stdout 解析、文件系统监听、网络流量）
- C. **完全重做**（替换为 stdout-driven，直接读 CLI 输出）
- D. **A + 信号源插件化**（核心 6 + 用户可装插件信号）

**推荐默认**: B + D（保留现有 + 新增 + 插件接口）

**影响范围**: B 工作量较大但显著提升精度；D 长期最优。

**用户回答**: __________B + D（保留现有 + 新增 + 插件接口）______________

---

### [Q-7.A.2] 新增信号源详细  锚定 [Must]（仅当 [Q-7.A.1] 选 B/D 时填写）

**新增信号选项**（勾选要启用的）：

#### 屏幕 OCR
- [ ] **截图 + Tesseract OCR**（兜底方案，CPU 较高）
- [ ] **截图 + 云 OCR**（如 Azure Computer Vision，需联网）
- [ ] **截图差异 + OCR**（仅当截图有显著变化时跑 OCR，省 CPU）

#### stdout / stderr 解析
- [ ] **node-pty 接管子进程 stdio**（直接监听）
- [ ] **替换 codex/claude 启动方式**（DevHub 启动它们，捕获流）
- [ ] **写包装 SHIM 脚本**（codex.exe → DevHub-codex-shim.exe → 真 codex.exe，中转 stdio）
- [ ] **CLI 自带 JSON 输出**（codex --output-format=json，结构化解析）

#### 文件系统监听
- [ ] **chokidar 监听 cwd 改动**（推断"正在写代码"）
- [ ] **监听 git status 变化**
- [ ] **监听特定 marker 文件**（codex 写 `.codex/state.json` 等）

#### 网络流量
- [ ] **netstat 端口活动**（与 AI 工具相关的 API 端口）
- [ ] **mitmproxy 接管 AI API 流量**（最强但最重）

#### Windows ETW 事件
- [ ] **ETW Process / Image Load 事件**（精确捕获 spawn）

**推荐默认**:
- 屏幕 OCR: 第 3 项（差异+OCR）
- stdout: 第 3 项（SHIM 脚本，最佳兼容性）+ 第 4 项（结构化）
- 文件系统: 第 1 项 + 第 2 项
- 网络: 第 1 项（netstat 已有）
- ETW: 启用（spawn 高精度）

**用户回答**: ___________沿用默认_________________

- 屏幕 OCR: 第 3 项（差异+OCR）
- stdout: 第 3 项（SHIM 脚本，最佳兼容性）+ 第 4 项（结构化）
- 文件系统: 第 1 项 + 第 2 项
- 网络: 第 1 项（netstat 已有）
- ETW: 启用（spawn 高精度）

---

### [Q-7.A.3] 信号融合算法  协商 [Should]

**当前**: 加权求和（completionScore = Σ weight × signal_value）。

**选项**:
- A. **保持加权求和**（简单可解释）
- B. **贝叶斯置信度**（每信号给后验概率）
- C. **逻辑回归 / 小型 ML 模型**（学习用户标注的 false positive 模式）
- D. **决策树**（透明化）
- E. **A + 用户反馈循环**（用户反复标"这是误报" → 自动调权重）

**推荐默认**: A + E（不引入 ML 复杂度，用反馈循环）

**用户回答**: _________A + E（不引入 ML 复杂度，用反馈循环）_______________

---

### [Q-7.A.4] 状态机（idle / thinking / running / completed / error）  锚定 [Must]

**当前**: monitorState (idle/thinking/running/completing) + status.state (idle/active/completed)。两套状态有时不一致 → R6 报"显示空闲 + 56%"。

**选项**:
- A. **合并为单一状态机**（彻底重构）
- B. **保持双状态但严格同步**（每次更新强制一致性约束）
- C. **三层状态**（系统层=idle/active；任务层=phase；UI 层=展示态）
- D. **Hierarchical State Machine（HSM）**（XState 风格）

**推荐默认**: C（清晰分层，避免冲突）

**影响范围**: C 需引入小型 state machine，约 200 LoC，但消除歧义。

**用户回答**: __________C______________

---

### [Q-7.A.5] 误报反馈循环  锚定 [Must]

**背景**: 用户标"这是误报" → 系统学习。

**选项**:
- A. **不实现**
- B. **每个通知附"是误报"按钮**（点击 → 调阈值 + 标本进库）
- C. **B + 周期性回顾**（每周展示"过去 N 次误报，已自动调整"）
- D. **C + 信号贡献透明度**（显示每条通知是哪几个信号触发的，用户可指认"这个信号没用"）

**推荐默认**: D

**用户回答**: ______________D__________

---

### [Q-7.A.6] 误报阈值上限  委托 [Could]

**背景**: 当前每 3 次误报阈值 +0.05。R8 是否调整？

**选项**: A. 保持 / B. 每次误报 +0.02（更敏感）/ C. 每次误报 +0.10（更宽容）/ D. 自动校准（基于用户标注样本）

**推荐默认**: D + A 兜底

**用户回答**: _____________D + A 兜底___________

---

## B. 进度可信度（Progress Confidence）

### [Q-7.B.1] 进度计算方式  锚定 [Must]

**当前**: 100% 启发式（phase weight + 历史时长融合）。

**选项**:
- A. **保持启发式**（不接 CLI）
- B. **接入 CLI 真实输出**（Codex/Claude 的 stdout / event-stream）
- C. **A + B 混合，置信度区间**（同时跑两套，显示"进度 X% [置信度 Y%]"）
- D. **多策略并行竞争**（取最自信的策略输出）

**推荐默认**: C（用户最希望的）

**影响范围**: C 需要新增 CLIOutputParser 模块，约 600 LoC + 每个 CLI 的特定解析。

**用户回答**: __________C______________

---

### [Q-7.B.2] CLI 输出解析支持的工具  锚定 [Must]

**选项**（每个工具的支持级别）：

#### Codex CLI
- A. 不支持
- B. 解析 stdout（已知模式：`Step N/M`, `Generating...`, `Completed`）
- C. 解析 JSON 输出（如有 `--output-format=json`）
- D. 替换为 SHIM（DevHub 控制 codex 进程的 stdio）

**推荐默认**: D

#### Claude Code (claude CLI)
- A. 不支持
- B. 解析 stdout
- C. JSON 流（`--output-format=stream-json`）
- D. SHIM

**推荐默认**: C + D（已有 JSON 流支持，最佳）

#### Gemini CLI
- A. 不支持
- B. stdout
- C. JSON
- D. SHIM

**推荐默认**: B + D

#### Cursor / GitHub Copilot
- A. 不支持
- B. 通过窗口标题感测
- C. 通过文件系统变化感测

**推荐默认**: B + C（无 CLI，仅信号感测）

**用户回答**: _________参照默认_______________

---

### [Q-7.B.3] 进度的 UI 呈现  协商 [Should]

**选项**（可多选）：
- A. **数字百分比**（"56%"）
- B. **进度条**（线性）
- C. **环形进度**（圆形）
- D. **置信度区间**（"56% [置信度 75%]"）
- E. **阶段指示器**（"Phase 3/5: Coding"）
- F. **预计完成时间**（"剩余约 2 分钟"）
- G. **历史折线**（最近 N 分钟进度变化）

**推荐默认**: B + D + E + F（信息密度最高）

**用户回答**: _________B + D + E + F_______________

---

### [Q-7.B.4] 进度回退处理  委托 [Could]

**背景**: 任务可能失败重试，进度从 80% 退到 30%。

**选项**:
- A. **直接退回**（数字直接变化）
- B. **保留高水位**（显示 80% 但状态切到"重试中"）
- C. **A + 历史折线**（折线显示曾到 80%）
- D. **B + 历史折线**

**推荐默认**: D

**用户回答**: _____________D___________

---

## C. 监控窗口（Monitoring Window）

### [Q-7.C.1] 监控窗口的形态  锚定 [Must]

**背景**: 用户原话"监控窗口：按照根据任务保证长时间超长任务进行"。

**选项**:
- A. **顶级 Tab 内的子面板**（在 ai-task Tab 中开"监控"子面板）
- B. **独立的 Electron 子窗口**（单开一窗，可拖到第二屏）
- C. **状态栏聚合**（仅状态栏显示，无独立面板）
- D. **A + B 混合**（默认 A，可"摘出"为 B）

**推荐默认**: D

**用户回答**: ________D________________

---

### [Q-7.C.2] 监控窗口的功能  锚定 [Must]

**选项**（勾选要支持的）：

#### 实时态
- [x] 当前所有 AI 任务列表（含状态/进度/置信度）
- [x] CSV 任务批次队列（已完成/进行中/排队/失败）
- [x] 整体吞吐率（任务/小时）
- [x] 每个 AI 工具的实例数

#### 历史
- [x] 今日完成任务列表
- [x] 失败重试记录
- [x] Watchdog 介入次数
- [x] 自动注入操作记录

#### 操作
- [x] 启动新 CSV 批次
- [x] 暂停当前批次
- [x] 跳过失败任务
- [x] 强制重启某个 AI 实例
- [x] 给某个 AI 实例发送追加提示
- [x] 查看任务录像

#### 可观测性
- [x] 每个信号源实时图表
- [x] 每个 IPC 通道的 RPM
- [x] 主进程 / 渲染进程的资源占用

**推荐默认**: 全选

**用户回答**: _____________全选___________

---

### [Q-7.C.3] 监控窗口的刷新频率  委托 [Could]

**选项**: A. 1s / B. 2s / C. 5s / D. 用户可调（500ms–10s）

**推荐默认**: D（默认 2s）

**用户回答**: _________D_______________

---

## D. SKILL 库（Skill Library）

### [Q-7.D.1] SKILL 是什么  锚定 [Must]

**背景**: 用户原话"通过 SKILLS + 提示词优化"。需明确 SKILL 的定义。

**选项**（您理解的 SKILL 是）：
- A. **可复用提示词模板 + 变量插槽**（"代码评审 SKILL" 含模板 + {filepath} + {standard}）
- B. **A + 钩子（pre/post 处理）**（执行前 lint，执行后跑测试）
- C. **A + B + 输出 schema 验证**（确保 AI 输出结构化）
- D. **A + B + C + 链式编排**（多 SKILL 串成 pipeline）
- E. **类 Anthropic 官方"Agent Skills"格式**（YAML frontmatter + tools/scripts）

**推荐默认**: D + E（兼容官方格式，最强能力）

**影响范围**: E 需引入 YAML 解析 + Skill SDK，约 1000 LoC。

**用户回答**: __________D + E（兼容官方格式，最强能力）______________

---

### [Q-7.D.2] SKILL 存储位置  协商 [Should]

**选项**:
- A. `%APPDATA%/devhub/skills/`（用户全局）
- B. 项目级 `.devhub/skills/`（每个项目自带）
- C. A + B（全局 + 项目级合并）
- D. C + 内置 SKILL（DevHub 自带几个常用）

**推荐默认**: D

**用户回答**: _________D_______________

---

### [Q-7.D.3] SKILL 的内置库  协商 [Should]

**期望内置 SKILL 清单**（勾选）：
- [ ] **代码评审**（输入文件 → 输出评审意见 + 改进建议）
- [ ] **写测试**（输入函数 → 输出 unit test）
- [ ] **重构提取**（输入选区 → 输出重构方案）
- [ ] **文档生成**（输入文件 → 输出 JSDoc / docstring）
- [ ] **解 bug**（输入错误 + 上下文 → 输出修复 patch）
- [ ] **类型补全**（输入 JS → 输出 TS 类型定义）
- [ ] **commit message 生成**（输入 diff → 输出 conventional commit）
- [ ] **PR 描述生成**
- [ ] **CSV 转任务批次**（输入 CSV → 拆解为 N 个 codex 任务）
- [ ] **任务总结**（汇总当日 AI 任务结果，生成报告）

**推荐默认**: 全选（10 个内置 SKILL，覆盖主流程）

**用户回答**: ______全选__________________

---

### [Q-7.D.4] SKILL 调用方式  协商 [Should]

**选项**（可多选）：
- A. 命令面板（Cmd+K → "运行 SKILL X"）
- B. AI 任务窗口的 SKILL 抽屉
- C. 右键菜单（在某文件/进程上 → "运行 SKILL"）
- D. CSV 任务批次声明（CSV 中的 skill 列）
- E. Watchdog 自动触发（任务停滞 → 自动调用 "解 bug" SKILL）

**推荐默认**: 全选

**用户回答**: __________全选______________

---

### [Q-7.D.5] SKILL 编辑器  委托 [Could]

**选项**:
- A. 仅外部编辑（通过文件管理器编辑 .md/.yaml）
- B. 应用内简单编辑器（Monaco / CodeMirror）
- C. B + 实时预览
- D. C + 变量提示与校验

**推荐默认**: D

**用户回答**: _________D_______________

---

## E. CSV 任务驱动器（CSV Task Driver）

### [Q-7.E.1] CSV 的 schema  锚定 [Must]

**背景**: 用户原话"编写 python 脚本根据 CSV 逐条拉起或并行拉起 codex 持续工作验收的模式"。

**期望 CSV 列**（勾选必有列）：
- [x] **id**（任务唯一 ID）
- [x] **tool**（codex / claude / gemini / cursor）
- [x] **prompt**（提示词内容 / 引用 SKILL）
- [x] **cwd**（工作目录）
- [ ] **timeout**（最大运行时间，秒）
- [ ] **retry**（最大重试次数）
- [ ] **on_fail**（next / abort / human / fallback-tool）
- [ ] **dependency**（依赖任务 id，DAG 编排）
- [ ] **parallel_group**（并行组 ID，同组任务可并行）
- [ ] **success_criteria**（成功判定，如"测试通过"/"git diff 包含 X"）
- [ ] **post_action**（成功后动作：commit / push / next）
- [ ] **env**（环境变量 JSON）
- [ ] **input_files**（前置文件路径，task 启动时注入到工作目录）
- [ ] **output_files**（任务产出文件，自动收集）
- [ ] **alias**（任务起别名）
- [ ] **priority**（high / normal / low）
- [ ] **tags**（自由标签）

**推荐默认**: 全部勾选（必有 4 + 14 个可选 = 18 列；提供模板）

**用户回答**: ____________全部勾选____________

---

### [Q-7.E.2] CSV 文件的存储位置  协商 [Should]

**选项**:
- A. 用户拖入应用
- B. 项目目录 `.devhub/tasks/*.csv`
- C. 用户全局 `%APPDATA%/devhub/tasks/`
- D. B + C（合并）

**推荐默认**: D

**用户回答**: _______________D_________

---

### [Q-7.E.3] CSV 启动方式  锚定 [Must]

**选项**:
- A. **DevHub 内置批次启动器**（用户在 UI 上点"运行 CSV"）
- B. **Python 脚本桥接**（DevHub 提供 `devhub-batch.py`，用户外部运行）
- C. **CLI 子命令**（`devhub run-csv tasks.csv`）
- D. **A + B + C 全部支持**（全方位接入）

**推荐默认**: D（用户最有自由度）

**影响范围**: D 需要 DevHub 暴露 CLI + 内嵌 Python 运行时（或调用系统 Python）

**用户回答**: ______D________________

---

### [Q-7.E.4] CSV 拉起 codex 的执行方式  锚定 [Must]

**选项**:
- A. **每条任务派生新 CLI 进程**（独立 spawn，简单但慢）
- B. **复用同一个 CLI 实例**（适合 codex 支持的 REPL 模式）
- C. **拉起 N 个 CLI 实例池**（并发 = parallel_group 数）
- D. **C + 智能调度**（按依赖图 + 优先级编排）

**推荐默认**: D

**用户回答**: ____________D____________

---

### [Q-7.E.5] 并发上限  锚定 [Must]

**背景**: 与 [Q-1.B.3] 关联。

**选项**: A. 1 / B. 3 / C. 6 / D. 12 / E. CSV 中指定 / F. 用户运行时调整

**推荐默认**: F（默认 3，用户运行时滑块调）

**用户回答**: ____________F____________

---

### [Q-7.E.6] 失败处理策略  锚定 [Must]

**选项**（按 on_fail 列的可选值）：
- [ ] **next**: 跳过失败任务，继续下一个
- [ ] **abort**: 整个批次中止
- [ ] **retry**: 按 retry 次数重试，超过转 next
- [ ] **fallback-tool**: 切到另一个 AI 工具（codex 失败 → claude）
- [ ] **escalate-model**: 升级模型（claude haiku → opus）
- [ ] **human**: 暂停，等待人工介入
- [ ] **execute-skill**: 调用某个修复 SKILL

**推荐默认**: 全部支持

**用户回答**: _________全部支持_______________

---

### [Q-7.E.7] CSV 任务的成功判定  锚定 [Must]

**选项**（可多选 success_criteria 类型）：
- [ ] **CLI 退出码 0**
- [ ] **stdout 含特定字符串**
- [ ] **stdout 不含错误关键字**（"error" / "failed" 不出现）
- [ ] **git diff 满足条件**（行数 / 文件 / 模式）
- [ ] **测试命令通过**（npm test / pytest）
- [ ] **lint 通过**
- [ ] **文件存在 / 内容匹配**
- [ ] **AI 工具自报告完成信号**
- [ ] **CSV 中 success_criteria 自定义脚本**

**推荐默认**: 全部支持

**用户回答**: ______________全部支持__________

---

### [Q-7.E.8] CSV 任务的可视化  协商 [Should]

**选项**（可多选）：
- A. **任务列表**（表格，含状态列）
- B. **甘特图**（横向时间线）
- C. **DAG 图**（依赖关系可视化）
- D. **进度看板**（todo/doing/done 三列）
- E. **全选**

**推荐默认**: E

**用户回答**: _________E_______________

---

## F. Watchdog 引擎

### [Q-7.F.1] Watchdog 的核心功能  锚定 [Must]

**背景**: 用户原话"写 watchdog"。Watchdog 是确保超长任务可靠完成的看门狗。

**选项**（勾选要支持的）：
- [ ] **心跳检测**（任务每 N 秒应发出心跳信号，超时即认为卡死）
- [ ] **CPU/RSS 异常检测**（CPU 长时间 0% 或 RSS 异常飙升）
- [ ] **stdout 静默检测**（输出长时间无变化）
- [ ] **窗口无响应检测**（IsHungAppWindow Win32 API）
- [ ] **文件系统活动检测**（任务应改动文件，长时间无改动）
- [ ] **自动重启**（卡死时 kill + restart）
- [ ] **自动重新注入上下文**（重启后将原 prompt 重新发给新实例）
- [ ] **多次失败转策略**（连续 3 次卡死 → 切工具 / 升级模型 / 通知人工）
- [ ] **Watchdog 自身的健康检查**（避免 Watchdog 进程自己挂掉）

**推荐默认**: 全选

**用户回答**: ____________全选____________

---

### [Q-7.F.2] 心跳定义  协商 [Should]

**选项**（哪些算心跳）：
- A. AI 工具自暴露心跳（写 marker 文件 / 网络 ping）
- B. stdout 任意输出
- C. 进程 CPU > 5% 持续 1s
- D. 窗口标题变化
- E. A + B + C + D（任一即心跳）

**推荐默认**: E

**用户回答**: ______E__________________

---

### [Q-7.F.3] 心跳超时阈值  协商 [Should]

**选项**: A. 30s / B. 1min / C. 5min / D. 用户可配置（默认 2min）

**推荐默认**: D

**用户回答**: __________D______________

---

### [Q-7.F.4] Watchdog 的运行位置  协商 [Should]

**选项**:
- A. **DevHub 主进程**（与监控同进程）
- B. **独立 Watchdog 子进程**（与 DevHub 分离，DevHub 崩溃也能继续）
- C. **Windows Service**（开机自启，不依赖 DevHub）

**推荐默认**: B

**影响范围**: B 增加进程管理复杂度但更可靠；C 需要安装 Service 且涉及 SYSTEM 权限。

**用户回答**: _______________B_________

---

### [Q-7.F.5] 自动注入上下文的方式  锚定 [Must]

**选项**（重启 AI 工具后如何重发 prompt）：
- A. **保存原 prompt，重启时模拟键盘输入**（[Q-6.D.2] 中的 inject text 路径）
- B. **保存对话历史，重启时通过 CLI 参数传入**（如 `claude --resume <session-id>`）
- C. **写入文件 + AI 工具读文件**（约定 `.devhub/inject.md`）
- D. **A + B 混合**（CLI 支持就用 CLI，否则模拟键盘）

**推荐默认**: D

**用户回答**: _____D___________________

---

### [Q-7.F.6] Watchdog 的告警通道  委托 [Could]

**选项**（可多选）：
- A. 应用内 Toast
- B. 系统通知
- C. 状态栏徽章
- D. 邮件
- E. Webhook（POST 到用户配置的 URL）
- F. 桌面响铃 / 弹窗强制确认
- G. 暂停所有任务等待人工

**推荐默认**: A + B + C + E + F（按严重度递进）

**用户回答**: __________A + B + C + E + F______________

---

## G. 自动注入（Auto-Inject）

### [Q-7.G.1] 自动注入的范畴  锚定 [Must]

**背景**: 用户原话"自动注入"。这与 Watchdog 的注入有重叠，需明确区分。

**注入 = 向 AI 工具的输入框写入文本，含哪些场景**：
- [ ] **CSV 任务驱动**（每条任务自动注入到对应 AI 实例）
- [ ] **Watchdog 重启后注入恢复 prompt**
- [ ] **任务完成后自动注入下条任务**（链式）
- [ ] **错误时注入修复 prompt**
- [ ] **用户预定义 schedule**（每天 9 点自动给 AI 注入"开始日志整理"）
- [ ] **手动 + 模板**（用户在 DevHub 选模板，一键注入到目标窗口）

**推荐默认**: 全选

**用户回答**: ____________全选____________

---

### [Q-7.G.2] 注入的目标选择  协商 [Should]

**选项**:
- A. **当前焦点窗口**（最危险）
- B. **指定 hwnd**（用户在监控里选）
- C. **CSV 中指定 alias**（最稳）
- D. **AI 工具自报告的 ready 实例池**

**推荐默认**: C + D（不要 A）

**用户回答**: ______________C + D__________

---

### [Q-7.G.3] 注入的可中断性  协商 [Should]

**选项**:
- A. **不可中断**（提交后 DevHub 不可干预）
- B. **3 秒倒计时**（用户可点"取消"）
- C. **明确二次确认**（每次都弹确认）
- D. **B 默认 + C 用户可启用严格模式**

**推荐默认**: D

**用户回答**: _________D_______________

---

### [Q-7.G.4] 注入的审计日志  锚定 [Must]

**选项**:
- A. **不记录**
- B. **本地 append-only 日志**（含时间 / 目标 / 内容前 200 字符）
- C. **B + 完整内容（隐私警告）**

**推荐默认**: B

**用户回答**: ____________B____________

---

## H. 任务编排 DAG（可选高级功能）

### [Q-7.H.1] 是否需要任务编排  协商 [Should]

**背景**: 类轻量 prefect/airflow，多 AI 工具串成 pipeline。

**选项**:
- A. **不做**（CSV 顺序执行足够）
- B. **简单依赖**（CSV 中 dependency 列，DAG 自动构建）
- C. **可视化编辑器**（拖拽节点）
- D. **C + 复杂分支**（条件分支 / 循环 / 并发）

**推荐默认**: B（轻量起步）

**用户回答**: __________B______________

---

### [Q-7.H.2] DAG 引擎实现  委托 [Could]

**选项**: A. 自研（轻量）/ B. 基于 [bullmq] 队列 / C. 基于 [graphlib] 拓扑排序 / D. 嵌入 [prefect](Python)

**推荐默认**: C（轻量、纯 JS、可扩展）

**用户回答**: _________C_______________

---

## I. 任务录像与回放

### [Q-7.I.1] 录像内容  协商 [Should]

**与 [Q-1.E.4] 关联**。

**选项**（勾选要录的内容）：
- [ ] stdout / stderr 全文
- [ ] stdin 用户输入
- [ ] 屏幕截图（每 N 秒一帧或事件触发）
- [ ] 文件系统改动列表
- [ ] git diff 时间线
- [ ] 信号融合得分变化曲线
- [ ] 网络请求列表

**推荐默认**: stdout + stdin + 屏幕截图（事件触发）+ 文件系统改动 + git diff

**用户回答**: __________stdout + stdin + 屏幕截图（事件触发）+ 文件系统改动 + git diff______________

---

### [Q-7.I.2] 回放界面  委托 [Could]

**选项**:
- A. 不做
- B. 文本时间线 + 截图缩略
- C. 类 asciinema 回放（终端动画）
- D. C + 屏幕截图视频

**推荐默认**: B（轻量）+ C 作为加分项

**用户回答**: _________B（轻量）+ C 作为加分项_______________

---

## J. 通知系统

### [Q-7.J.1] 通知事件分级  锚定 [Must]

**选项**（每个事件级别）：

| 事件 | INFO | WARN | ERROR | FATAL |
|------|------|------|-------|-------|
| 任务开始 | x | | | |
| 任务完成 | x | | | |
| 任务失败 | | | x | |
| 任务重试 | | x | | |
| Watchdog 介入 | | x | | |
| 注入操作 | x | | | |
| 误报反馈 | x | | | |
| CSV 批次完成 | x | | | |
| 信号源故障 | | x | | |
| Watchdog 自身故障 | | | | x |
| 主进程内存超阈 | | x | | |

**用户回答**（可调整）: ______按照默认填写__________________

---

### [Q-7.J.2] 通知通道  协商 [Should]

**选项**（每级别用哪些通道）：

| 级别 | Toast | 系统通知 | 状态栏 | 邮件 | Webhook | 响铃 |
|------|-------|---------|--------|------|---------|------|
| INFO | x | | x | | | |
| WARN | x | x | x | | x | x |
| ERROR | x | x | x | x | xx | x |
| FATAL | x | x | x | x | x | x |

**用户回答**（可调整）: ____________按照默认填写____________

---

### [Q-7.J.3] 通知聚合  委托 [Could]

**背景**: 短时间内大量通知 → 聚合为"过去 30 秒内 X 个任务完成"。

**选项**: A. 不聚合 / B. 5s 窗口聚合 / C. 用户可调

**推荐默认**: C

**用户回答**: ____________C____________

---

## K. AI 任务模块小结

**Q-7.K.1**: 您预期 R8 完成后，DevHub 在 AI 任务这一块"最强"的能力是？（自由填写）：____________监控____________

**Q-7.K.2**: 您是否希望 DevHub 暴露 OpenAI / Anthropic 的 API key 管理？（A. 是，UI 加密存储 / B. 否，仅依赖 CLI 自身配置 / C. 后期）：_________否_______________

**Q-7.K.3**: 您希望 DevHub 在用户安装 Codex/Claude/Gemini CLI 时自动检测配置并初始化吗？（A. 是 / B. 否 / C. 询问后初始化）：_______________是_________

**Q-7.K.4**: 您是否希望 SKILL 库支持云同步（GitHub Gist / 私有仓库）？（A. 是 / B. 否 / C. 后期）：_______________后期_________

**Q-7.K.5**: 上述以外，AI 任务模块还有什么必须明确的？自由填写：________________________
