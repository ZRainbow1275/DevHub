# 09 — 横切关注点需求表（权限 / IPC / 日志 / 可观测 / 备份 / 遥测 / 数据契约）

> **填写时长**: 约 15–20 分钟
> **重要程度**: 决定整个应用的工程质量基线
> **依赖**: 完成所有功能模块（01–08）后填写

---

## A. 权限模型（Permission Matrix）

### [Q-9.A.1] 权限分级  锚定 [Must]

**选项**:
- A. **二级**（普通 / 管理员）
- B. **三级**（普通 / 提权 / SYSTEM）
- C. **基于操作分类**（每类操作独立授权）
- D. **C + 时效**（权限有有效期）

**推荐默认**: C + D

**用户回答**: ____________C + D____________

---

### [Q-9.A.2] 哪些操作需用户确认  锚定 [Must]

**选项**（每项的确认级别）：

| 操作 | 不确认 | 一次确认 | 每次确认 |
|------|-------|---------|---------|
| 查看进程详情 | x | | |
| 查看进程命令行 | | x | |
| 结束进程（用户态）| | x | |
| 结束系统进程 | | | x |
| 释放端口 | | x | |
| 释放系统端口 | | | x |
| 关闭窗口 | x | | x |
| 重命名窗口 | x | | |
| 截图窗口 | x | | |
| 注入文本到窗口 | | x | |
| 注入文本到 AI 工具 | x（白名单内）| x（白名单外）| |
| 启动 CSV 批次 | | x | |
| Watchdog 自动重启 AI | x（任务模式）| | x（手动模式）|
| 提权运行 DevHub | | | x |
| 修改主题/配置 | x | | |
| 导出/导入备份 | | x | x |
| 卸载 DevHub | | | x |

**用户回答**（可调整）: _______________按照表格内容_________

---

### [Q-9.A.3] 审计日志  锚定 [Must]

**选项**:
- A. **不记录**
- B. **关键操作记录**（kill / 端口释放 / 注入）
- C. **B + 完整可见**（应用内审计面板）
- D. **C + 导出 / 加密 / 不可篡改**

**推荐默认**: C

**用户回答**: ____________C____________

---

## B. IPC 限流与背压

### [Q-9.B.1] 全局 IPC 限流策略  锚定 [Must]

**背景**: R6-P2.1 报告 IPC 高频调用导致内存爆炸。R7 spec/05 已规划 token bucket。

**选项**:
- A. **全局 token bucket**（统一速率）
- B. **每通道独立 RPM 限制**
- C. **A + B 组合**

**推荐默认**: C

**用户回答**: _________C_______________

---

### [Q-9.B.2] 限流命中后的策略  协商 [Should]

**选项**:
- A. **直接拒绝**（返回 RATE_LIMIT 错误）
- B. **排队等待**（FIFO 队列）
- C. **降级到缓存**（不调用主进程，返回最近缓存）
- D. **C + 重试指数退避**

**推荐默认**: C + 用户可见提示

**用户回答**: ____________C + 用户可见提示____________

---

### [Q-9.B.3] 关键通道的 RPM 上限  委托 [Could]

**默认**:
| Channel 类型 | RPM |
|-------------|-----|
| 高频扫描（process/port/window） | 30 |
| 中频查询（topology/flow build） | 60 |
| 低频操作（kill / focus / inject） | 120 |
| 设置 / 元操作 | 600 |

**用户回答**（可调整）: _________按照表格默认填写_______________

---

## C. 日志系统

### [Q-9.C.1] 日志层级  协商 [Should]

**选项**:
- A. **TRACE / DEBUG / INFO / WARN / ERROR / FATAL**
- B. **DEBUG / INFO / WARN / ERROR**（简化）
- C. **B + 用户分类标签**

**推荐默认**: A

**用户回答**: _________A_______________

---

### [Q-9.C.2] 日志输出位置  协商 [Should]

**选项**（可多选）：
- A. 控制台 stdout（开发模式）
- B. 文件（按日 rotate，保留 30 天）
- C. 应用内日志面板（DevObservabilityPanel）
- D. 远程（Sentry / Datadog / 自建）

**推荐默认**: A + B + C（不要 D）

**用户回答**: ____________A + B + C____________

---

### [Q-9.C.3] 日志默认级别  委托 [Could]

**选项**: A. INFO / B. WARN / C. 根据 NODE_ENV 自动

**推荐默认**: C（dev=DEBUG / prod=INFO）

**用户回答**: _________C_______________

---

### [Q-9.C.4] 敏感字段过滤  锚定 [Must]

**背景**: 日志中可能含 cmdline / cwd / token 等。

**选项**:
- A. **不过滤**
- B. **关键字过滤**（API_KEY / TOKEN / PASSWORD 等关键字脱敏）
- C. **B + 用户黑名单**

**推荐默认**: C

**用户回答**: _________C_______________

---

## D. 可观测性面板（DevObservabilityPanel）

### [Q-9.D.1] 面板的可见性  锚定 [Must]

**背景**: R7 spec/06 规划，需明确暴露程度。

**选项**:
- A. **仅 dev 模式可见**
- B. **生产可见但需开关**（设置 → 开发者 → 启用观测面板）
- C. **永远可见**（普通用户友好的简化版 + 开发者完整版）
- D. **状态栏入口 + 完整面板需快捷键唤起**

**推荐默认**: D

**用户回答**: _________D_______________

---

### [Q-9.D.2] 面板内容  协商 [Should]

**选项**（可多选）：
- [x] 主进程 RSS / CPU 实时曲线
- [x] 渲染进程 RSS / CPU 实时曲线
- [x] 子进程数（PowerShell / spawn）
- [x] IPC 调用 RPM Top 10
- [x] 信号融合得分（AI 任务）
- [x] Scanner 状态（每个 scanner 的 last-run / interval / 错误数）
- [x] Watchdog 状态
- [x] 通知队列长度
- [x] 最近 100 条日志（实时）
- [x] 错误堆栈
- [x] React 渲染计数（开发模式）

**推荐默认**: 全选

**用户回答**: _________全选_______________

---

### [Q-9.D.3] 异常告警  协商 [Should]

**选项**（什么阈值触发顶部红条）：
- [x] RSS > 800 MB
- [x] CPU > 30% 持续 10s
- [x] 子进程 > 10
- [x] IPC RPM > 600（任一通道）
- [x] Scanner 连续失败 3 次
- [x] Watchdog 自身故障

**推荐默认**: 全选

**用户回答**: _________全选_______________

---

## E. 数据契约校验（Schema Validation）

### [Q-9.E.1] 校验范围  锚定 [Must]

**选项**:
- A. **仅 IPC 边界**（请求/响应通过 Zod 校验）
- B. **A + 持久化**（电子 store 写入前 + 读取后校验）
- C. **A + B + 渲染层**（组件 props 也校验）
- D. **TypeScript 类型 + 运行时 Zod 双保险**

**推荐默认**: D

**用户回答**: _________D_______________

---

### [Q-9.E.2] 校验失败处理  协商 [Should]

**选项**:
- A. **抛错**（崩溃式）
- B. **降级 + 错误日志**
- C. **降级 + 用户可见提示**

**推荐默认**: B 内部 + C 用户面向

**用户回答**: _________B 内部 + C 用户面向_______________

---

### [Q-9.E.3] 共享类型管理  委托 [Could]

**当前**: `src/shared/` 共享类型定义。

**选项**:
- A. 保持现状
- B. 引入 OpenAPI / TypeSpec 自动生成
- C. 引入 Zod 单一来源（先 Zod schema → 推导 TypeScript 类型）

**推荐默认**: C

**用户回答**: _________C_______________

---

## F. 备份与恢复

### [Q-9.F.1] 备份内容  协商 [Should]

**选项**（要备份的内容）：
- [x] 主题与外观偏好
- [x] 项目列表
- [x] 窗口分组与别名
- [x] AI 任务历史
- [x] CSV 任务模板
- [x] SKILL 库
- [x] 设置项
- [x] 审计日志（最近 30 天）

**推荐默认**: 全选

**用户回答**: _________全选_______________

---

### [Q-9.F.2] 恢复粒度  委托 [Could]

**选项**:
- A. **整体回滚**（一键全量）
- B. **分类恢复**（用户选要恢复哪几类）
- C. **时间点恢复**（指定回到 N 天前）

**推荐默认**: A + B

**用户回答**: _________A + B_______________

---

## G. 崩溃恢复

### [Q-9.G.1] 崩溃恢复策略  锚定 [Must]

**选项**:
- A. **下次启动什么都不做**
- B. **下次启动检测脏状态，提示恢复**
- C. **B + 自动保存最近 10 个状态**
- D. **C + 崩溃报告**（用户可选发送）

**推荐默认**: C

**用户回答**: _________C_______________

---

### [Q-9.G.2] 崩溃报告  委托 [Could]

**选项**:
- A. 不收集
- B. 仅本地保存
- C. 用户主动发送（联系开发者）

**推荐默认**: B

**用户回答**: _________B_______________

---

## H. 遥测（Telemetry）

### [Q-9.H.1] 遥测策略  锚定 [Must]

**选项**:
- A. **完全不收集**
- B. **本地 OpenTelemetry，不外发**
- C. **本地优先 + 用户主动选择上报**
- D. **C + 完全匿名 + 可见原始报文**

**推荐默认**: A 或 B（看 [Q-1.A.2] 的发布策略）

**用户回答**: _________A，绝不可以收集以侵犯用户隐私_______________

---

### [Q-9.H.2] 遥测内容  委托 [Could]

**选项**（仅在 Q-9.H.1 选 B/C/D 时填）：
- [ ] 应用启动 / 退出
- [ ] 模块切换次数
- [ ] 错误堆栈（脱敏）
- [ ] 主进程 / 渲染进程性能指标
- [ ] AI 工具识别准确率（含用户反馈循环数据）

**推荐默认**: 全选（仅本地）

**用户回答**: ____________无____________

---

## I. 安全

### [Q-9.I.1] 自动更新  协商 [Should]

**选项**:
- A. 不自动更新
- B. 检查更新但需用户确认
- C. 后台下载 + 用户确认安装
- D. 完全自动（含静默重启）

**推荐默认**: B

**用户回答**: _______________B_________

---

### [Q-9.I.2] 代码签名  委托 [Could]

**选项**:
- A. 不签名（开发版）
- B. 自签名证书
- C. EV 代码签名证书（避免 SmartScreen 警告）

**推荐默认**: A 内部 / B 私域 / C 公开

**用户回答**: _________B_______________

---

### [Q-9.I.3] 沙箱  委托 [Could]

**选项**:
- A. 不沙箱
- B. 渲染进程开启 Electron sandbox（默认）
- C. B + contextIsolation
- D. C + 严格 CSP

**推荐默认**: D

**用户回答**: _____________D___________

---

## J. 横切小结

**Q-9.J.1**: 您是否希望 DevHub 提供 "诊断包导出"（一键导出脱敏后的日志/状态/审计 + 截图）以便您求助？（A. 是 / B. 否 / C. 仅在崩溃后）：____________是____________

**Q-9.J.2**: 您是否希望 DevHub 在长跑出现内存异常时主动告警 + 自动重启自身？（A. 不告警 / B. 告警不重启 / C. 告警 + 询问后重启 / D. 自动重启）：_____________ C. 告警 + 询问后重启___________

**Q-9.J.3**: 上述以外，横切关注点还有什么必须明确的？自由填写：____________安全和性能必须兼顾，最大程度保护用户安全，该应用不连接云端，一切走本地____________
