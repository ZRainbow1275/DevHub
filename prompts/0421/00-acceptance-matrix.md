# R7 Acceptance Matrix

> 当前真相源：`prompts/0421/spec/*`、`prompts/0421/contracts/*`、`prompts/0421/tests/*`、`.trellis/tasks/04-21-04-21-master-implementation-sweep/prd.md`。
> 状态语义：`[CODE-DONE]` 表示代码链路已实现并通过本轮自动检查，但仍缺专项 E2E、长跑 bench 或用户手测；`[TEST-PASS]` 表示对应专项测试或 bench 已真实通过；`[USER-VERIFIED]` 仅能由用户手测确认后标记。
> 本文件只记录验收状态，不用 mock、不用模拟数据、不把未跑通的真实验收标绿。

## Acceptance Matrix

| ID | 命题 | Given / When / Then 摘要 | Spec / Contract | 关键事件或接口 | E2E | Bench / Unit | Status |
|---|---|---|---|---|---|---|---|
| P1.1 | 脚本下拉菜单无遮挡 | 脚本列表在复杂项目中打开时，菜单不被卡片容器裁切，键盘与鼠标均可操作 | `spec/13` | `project:list-scripts` | `E2E-P1.1-a/b/c` | - | [TEST-PASS] |
| P1.2-a | 项目列表密度可切换 | 切换 compact / comfortable 后，真实项目列表密度、虚拟行高和统计区域同步变化 | `spec/21` `spec/18` | `settings:set-density` | `E2E-P1.2-a` | - | [TEST-PASS] |
| P1.2-b | 项目打开入口可用 | 项目卡片可真实打开 VS Code / Cursor / 资源管理器 / 终端，并保留复制路径入口 | `spec/21` | `project:open-in-editor` | `pnpm exec playwright test e2e/example.spec.ts -g "P1.2-b" --timeout=120000 --workers=1` | - | [TEST-PASS] |
| P2.1 | 长跑资源稳定 | DevHub 运行 60 分钟后主进程与渲染进程 RSS 不超过基线 1.5x，CPU 5 分钟均值不超过 5%，退出后无 PowerShell 子进程残留 | `spec/03` `spec/04` `spec/05` `spec/06` | scanner channels | `E2E-P2.1-longrun` | `BENCH-P2.1-a/b/c/d/e/f` | [TEST-PASS] |
| P2.2 | PID 详情降级可用 | 指定 PID 详情不可完全读取时，仍显示真实基础信息、权限原因和管理员重启入口 | `spec/14` | `process:get-deep-detail` `process:probe-access` `app:relaunch-as-admin` | `E2E-P2.2-a/b` | - | [TEST-PASS] |
| P3.1-a | 端口列表滚动条可见 | 端口列表在真实数据溢出时有稳定滚动条和键盘滚动能力 | `spec/15` | - | `E2E-P3.1-a` | - | [TEST-PASS] |
| P3.1-b | 端口详情面板布局稳定 | 焦点面板打开后主列表和详情面板可伸缩，不挤压不可读 | `spec/15` | - | `E2E-P3.1-b` | - | [TEST-PASS] |
| P3.1-c | 端口详情超时降级 | 增量详情超时时展示缓存时间、重试和轻量模式，不用模拟详情 | `spec/15` | `port:get-detail-incremental` | `E2E-P3.1-c` | - | [TEST-PASS] |
| P4.1 | 长窗口标题可读 | 长标题不撑破卡片，支持 tooltip 和可控 marquee | `spec/16` | real `BrowserWindow` + `windowManager.scan(false)` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.1" --timeout=120000 --workers=1` | `pnpm exec vitest run src/renderer/components/ui/TruncatedText.test.tsx` | [TEST-PASS] |
| P4.2-a-1 | AI 窗口别名可编辑 | AI 窗口别名可以在 UI 中修改并持久化 | `spec/07` | `ai-alias:rename-and-apply` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a" --timeout=120000 --workers=1` | `pnpm typecheck` | [TEST-PASS] |
| P4.2-a-2 | AI 外部窗口标题可应用 | 重命名后通过真实 WinAPI 更新外部窗口标题，并可回滚失败 | `spec/07` | `window:set-title` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a" --timeout=120000 --workers=1` | `pnpm typecheck` | [TEST-PASS] |
| P4.2-a-3 | AI 完成通知带别名 | 任务完成通知历史和 metadata 携带用户可识别别名 | `spec/07` `spec/11` | `notification:task-complete` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a-3" --timeout=120000 --workers=1` | `pnpm build` / `pnpm typecheck` | [TEST-PASS] |
| P4.2-a-4 | AI 别名跨重启恢复 | 重启后别名从真实 electron-store 恢复，且持久化 matchCriteria 不保存 PID、保留 toolType / titlePrefix 等稳定指纹 | `spec/07` | `ai-alias:rename-and-apply` `ai-alias:get-all` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a" --timeout=120000 --workers=1` | `pnpm typecheck` | [TEST-PASS] |
| P4.2-b | AI 感测准确 | 两个并发 Claude Code 实例运行 30 分钟，误报不超过 1，漏报为 0，迟报小于 5 秒 | `spec/08` `spec/11` | `ai-task:status-changed` `ai-task:calibrate` `ai-task:record-completion-oracle` | `E2E-P4.2-b` | `BENCH-P4.2-b` | [TEST-PASS] |
| P4.2-c-1 | 窗口分组可用 | 可创建、重命名、删除工作分组，并基于真实窗口 fingerprint 解析运行时成员 | `spec/09` | `window:create-group` `window:rename-group` `window:remove-group` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-c-1" --timeout=120000 --workers=1` | `pnpm typecheck` | [TEST-PASS] |
| P4.2-c-2 | 窗口布局可用 | 多个真实窗口可保存快照、应用真实布局、恢复快照并撤销布局 | `spec/10` | `window:apply-layout` `window:save-snapshot` `window:restore-snapshot` `window:restore-previous` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-c-2" --timeout=120000 --workers=1` | `pnpm typecheck` | [TEST-PASS] |
| P4.2-d | AI 进度状态一致 | UI 不出现 idle 加百分比等矛盾组合，状态和进度由同一派生契约生成 | `spec/11` | `ai-task:progress` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-d/P5.1" --timeout=180000 --workers=1` | `pnpm typecheck` / `pnpm build` | [TEST-PASS] |
| P4.2-e | 窗口操作目录可用 | 窗口菜单覆盖焦点、最小化、最大化、关闭、置顶、截图、跳转等真实操作 | `spec/12` | window operation channels | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-e" --timeout=120000 --workers=1` | `UNIT-P4.2-e-catalog` / `UNIT-P4.2-e-window-manager` / `TYPECHECK` | [TEST-PASS] |
| P5.1 | AI 进度条行为正确 | thinking / coding / validating / waiting / completed / error 状态展示与动画语义一致 | `spec/11` | `ai-task:get-timeline` | `pnpm exec playwright test e2e/example.spec.ts -g "P4.2-d/P5.1" --timeout=180000 --workers=1` | `pnpm typecheck` / `pnpm build` | [TEST-PASS] |
| P5.2 | 品牌 Logo 统一 | AI 工具图标来自项目 SVG / simple-icons / 已安装图标库，不使用 Emoji | `spec/20` `contracts/25` | - | `E2E-P5.2` | - | [TEST-PASS] |
| P6.1 | 全局拓扑可用 | 进程、端口、窗口和项目关系可以构建全局拓扑 | `spec/17` | `topology:build-graph` / `topology:build-scoped-graph` | `pnpm exec playwright test e2e/example.spec.ts -g "P6.1" --timeout=120000 --workers=1` | `pnpm exec vitest run src/shared/topology/scope.test.ts` | [TEST-PASS] |
| P6.2 | 作用域拓扑可用 | 可按项目、窗口、进程、端口构建 scoped topology | `spec/02` | `topology:build-scoped-graph` / `topology:warm-scope` | `pnpm exec playwright test e2e/example.spec.ts -g "P6.1" --timeout=120000 --workers=1` | `pnpm exec vitest run src/shared/topology/scope.test.ts` | [TEST-PASS] |
| P7.1 | Flow 视图可用 | scoped flow 从真实拓扑数据构建，不依赖 mock 图 | `spec/02` | `flow:build-scoped-flow` | `pnpm exec playwright test e2e/example.spec.ts -g "P6.1" --timeout=120000 --workers=1` | `pnpm exec vitest run src/shared/topology/scope.test.ts` | [TEST-PASS] |
| P8.1 | 布局模式可切换 | 设置中的布局模式影响真实监控 UI 布局 | `spec/18` | `settings:set-layout-mode` | `E2E-P8.1-a/b/c` | `pnpm exec playwright test e2e/example.spec.ts -g "P8.1" --timeout=120000 --workers=1` | [TEST-PASS] |
| P8.2 | 外观设置可切换 | 主题、密度、圆角、动效等设置可以真实保存和应用 | `spec/19` | `settings:set-appearance` | `E2E-P8.2-a/b/c/d` | `pnpm exec playwright test e2e/example.spec.ts -g "P8.2" --timeout=120000 --workers=1` | [TEST-PASS] |
| X1 | Schema 契约完整 | shared schema 覆盖当前核心数据模型并能被测试引用 | `contracts/22` | - | `E2E-X1-schema` | `pnpm exec vitest run src/shared/schemas/contract-models.test.ts && pnpm typecheck` | [TEST-PASS] |
| X2 | Preload 白名单完整 | renderer 只能访问白名单 API，每个公开 invoke 有 main handler 支撑 | `contracts/23` | - | `E2E-X2-preload` | - | [TEST-PASS] |
| X3 | 审计日志覆盖关键操作 | 危险操作、窗口操作、权限事件和错误恢复有审计记录 | `contracts/24` | - | `pnpm exec playwright test e2e/example.spec.ts -g "X3" --timeout=120000 --workers=1` | `pnpm build` | [TEST-PASS] |
| X4 | 无 Emoji | 源码和关键文档通过 no-emoji gate | `contracts/25` | - | `E2E-X4-no-emoji` | - | [TEST-PASS] |
| X5 | Scanner 单例 | scanner runtime 不重复创建主实例 | `spec/03` `spec/04` | - | `E2E-X5-singleton` | - | [TEST-PASS] |
| X6 | IPC 背压 | IPC diff / snapshot 有节流、ACK、resync 和限流保护 | `spec/03` `spec/05` | - | `E2E-X6-semaphore` | `BENCH-X6` | [TEST-PASS] |
| X7 | 退出清理 | app 退出后 watcher、scanner、PowerShell gateway 和 dispose registry 释放干净 | `spec/04` | - | `E2E-X7-quit` | `BENCH-X7` | [TEST-PASS] |
| X8 | Dev Observability | runtime metrics、throttle report、reset 和 diagnostic bundle 可用 | `spec/06` | `dev:get-runtime-metrics` `dev:get-throttle-report` `dev:reset-runtime-metrics` `dev:export-diagnostic-bundle` | `E2E-X8-panel` | - | [TEST-PASS] |

## Current Evidence

- `P2.1`：2026-04-29 正式 60 分钟长跑通过，报告为 `devhub/perf-reports/bench-p2-longrun-2026-04-29T11-19-59-787Z.json`。关键结果：`acceptanceEligible=true`，`maxCpu5m=1.4 <= 5`，`maxIpcRpm=8 <= 10`，`mainRatio=1.0677540986832446 <= 1.5`，`rendererRatio=1.0757544784203639 <= 1.5`，`maxPsChildren=2 <= 2`，`psChildrenAfterExit=0`，`remainingAfter=[]`。
- `P4.2-b`：2026-04-29 完成代码侧抽层和真实 bench harness。新增 `SignalCollector`、`ConfidenceEngine`、`CompletionStateMachine`、`CalibrationSampleStore`、`TaskKey`、`DetectionEngine.test.ts`、`bench:p4.2-b`、`ai-task:calibrate` 和相关 shared/schema/preload/global 契约。已通过 `pnpm exec vitest run src/main/services/detection/DetectionEngine.test.ts src/main/services/AITaskTracker.test.ts src/preload/preloadContract.test.ts`，结果为 48 tests passed；已通过 `pnpm typecheck`、`pnpm lint`、`pnpm build` 和相关文件 `git diff --check`。
- `P4.2-b` 的历史短时 harness 报告包括 `devhub/perf-reports/bench-p4-ai-accuracy-2026-04-29T12-13-40-050Z.json`、`devhub/perf-reports/bench-p4-ai-accuracy-2026-04-29T23-35-04-531Z.json`、`devhub/perf-reports/bench-p4-ai-accuracy-2026-04-30T18-29-45-454Z.json` 与 `devhub/perf-reports/claude-completion-events-smoke-2026-05-01.jsonl`。这些历史报告用于证明 harness 在缺少 30 分钟窗口、第二个真实 Claude Code 实例或 completion-events oracle 时会正确阻塞；当前状态以 2026-05-01 正式 30 分钟通过报告为准。
- `P4.2-b`：2026-05-01 正式 30 分钟 `BENCH-P4.2-b` 已通过，报告为 `devhub/perf-reports/bench-p4-ai-accuracy-2026-05-01T18-22-08-249Z.json`，真实事件源为 `devhub/perf-reports/p4-acceptance-2026-05-01T17-51-31-302Z/claude-completion-events.jsonl`。关键结果：`passed=true`，`acceptanceEligible=true`，completion events `7 >= 6`，`maxClaudeCount=2`，`activeCompletions=6`，`falsePositives=1 <= 1`，`missedEvents=0`，`maxDelayMs=0 < 5000`，`oracleIngestionErrors=0`，`strongReportCount=586`。本轮新增 `ai-task:record-completion-oracle` 窄 IPC / preload wrapper，将真实 Claude Code hook JSONL 在采样过程中摄取进 DevHub history 与 ConfidenceReport；未手写或伪造 completion event。
- 2026-04-30 本轮复跑短时 smoke 生成 `devhub/perf-reports/bench-p4-ai-accuracy-2026-04-29T17-16-02-421Z.json`：`passed=false`、`sampleCount=1`、`maxClaudeCount=0`、`completionEvents=0`，证明 harness 在无可见 Claude 实例和无 oracle 时仍会阻塞验收。
- `P1.1` / `P1.2-a` / `P2.2` / `X2` / `X5` / `X6` / `X7` / `X8`：2026-04-30 全量 Electron E2E `pnpm test:e2e` 通过，结果为 `10 passed (43.2s)`。其中 `E2E-P1.1` 使用真实项目列表打开脚本下拉菜单，验证菜单挂载到 `document.body`、`position: fixed`、不越出视口、z-index 足够且 Escape 可键盘关闭并回焦；`E2E-P2.2` 使用真实 `Get-CimInstance Win32_Process` 候选 PID，验证 exact PID fallback、`permission-notice`、`以管理员身份重启` 和 `detail-error-only` 不出现；`E2E-X2` 覆盖 preload 白名单并已同步新增 `topology` bridge；`E2E-X8` 通过真实热键打开 DevObservabilityPanel 并验证 runtime metrics、throttle report、scanner backpressure 和 diagnostic bundle。
- `P1.2-a`：2026-04-30 新增真实 Electron E2E `P1.2-a 项目列表密度可从设置切换并同步虚拟行高`。验证链路为打开真实设置面板、通过 `信息密度` 原生 select 切换 `compact` / `comfortable`、断言 `<html data-density>`、`project-list-scroll[data-density]`、`data-estimated-row-height=64/144` 和 `electron-store` 中 `appearance.informationDensity` 持久化。验证命令：`pnpm build` 通过；`pnpm exec playwright test e2e/example.spec.ts -g "P1.2-a" --timeout=90000 --workers=1` 为 `1 passed (14.8s)`；`pnpm exec playwright test e2e/example.spec.ts -g "P1.1|P1.2-a" --timeout=90000 --workers=1` 为 `2 passed (6.8s)`；`pnpm test:e2e` 为 `10 passed (43.2s)`；`pnpm typecheck`、`pnpm lint`、`pnpm test` 通过。
- `P3.1-a/b/c`：2026-04-30 新增真实 Electron E2E `P3.1-a/b 端口列表滚动条与详情分栏可用真实端口验证` 与 `P3.1-c 端口详情真实超时后显示缓存降级与轻量模式`。验证链路使用真实 `window.devhub.port.scan()` 和 `port:get-detail-incremental`，本机扫描得到 30 条以上真实端口；先在 1366x768 验证 `port-list-scroll` 真实溢出和滚轮滚动，再在真实扩大后的监控工作区验证 `devhub:port-view-split` 的 640px/360px/560px 边界、拖拽和双击重置；超时候选由真实增量详情查询返回 `source="timeout"` 后再进入 UI 验证 banner、重试和轻量模式。验证命令：`pnpm typecheck`、`pnpm build`、`pnpm exec playwright test e2e/example.spec.ts -g "P3.1" --timeout=90000 --workers=1` 通过，P3.1-a/b/c 状态提升为 `[TEST-PASS]`。
- `P8.1`：2026-04-30 新增真实设置链路 `appearance.layoutMode = auto | split | stacked`，接入 shared type、默认设置、settings IPC 白名单/校验、SettingsDialog 原生 select、App shell reflow 和折叠侧栏设置按钮可访问性。`E2E-P8.1` 启动真实 Electron、通过 BrowserWindow resize 验证 auto 模式 split/stacked 切换，再通过真实设置面板强制 stacked、跨重启恢复 stacked，最后切回 auto 并验证宽屏 split / 窄屏 stacked。验证命令：`pnpm typecheck`、`pnpm build`、`pnpm exec playwright test e2e/example.spec.ts -g "P8.1" --timeout=120000 --workers=1`，结果为 `1 passed (7.7s)`。
- `P4.2-a-3`：2026-04-30 新增真实 Electron E2E `P4.2-a-3 AI 完成通知在真实 Electron 历史中携带别名 metadata`。测试创建真实 `BrowserWindow` 并取得真实 hwnd / pid，通过 dev/test hook 调用真实主进程 `NotificationService.notifyTaskComplete()`，再通过真实 renderer IPC `notification.getHistory()` 读取通知历史，断言标题 `[Claude Code-别名] 任务完成`、body PID、`metadata.aliasOrToolName/displayName/taskId/windowHwnd` 全部存在。验证命令：`pnpm build` 通过；`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a-3" --timeout=120000 --workers=1` 为 `1 passed (4.0s)`；`pnpm typecheck` 通过。
- `P4.2-a-1/a-2/a-4`：2026-04-30 新增真实 Electron E2E `P4.2-a AI 窗口别名可真实应用外部标题并跨重启恢复`。测试创建真实 `BrowserWindow`，通过真实 `windowManager.scan(false)` 取得 hwnd / pid，调用真实 preload/IPC `aiAlias.renameAndApply()` 持久化 alias 并同步 Win32 外部标题为 `[Claude Code-别名] 原标题`，随后重启 DevHub 并从真实 electron-store 恢复 alias，断言持久化 `matchCriteria` 不保存 PID 且保留 `toolType/titlePrefix`。验证命令：`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a" --timeout=120000 --workers=1` 为 `1 passed (9.3s)`；`pnpm typecheck` 通过。
- `P4.2-c-1`：2026-04-30 新增真实 Electron E2E `P4.2-c-1 窗口分组可对真实窗口完成 CRUD 与运行时解析`。测试创建两个真实 `BrowserWindow`，通过真实 `windowManager.scan(false)` 获取 hwnd，调用真实 preload/IPC `createGroup()`、`getGroups()`、`renameGroup()` 与 `removeGroup()`，断言 `memberFingerprints`、`resolvedMembership`、`resolutionReport.matched` 与真实 hwnd 对齐，并确认删除分组后真实窗口仍然存在。验证命令：`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-c-1" --timeout=120000 --workers=1` 为 `1 passed (7.5s)`；`pnpm typecheck` 通过。
- `P4.2-c-2`：2026-04-30 新增真实 Electron E2E `P4.2-c-2 窗口布局可真实保存快照恢复快照并撤销`。测试创建两个真实 `BrowserWindow`，通过真实 `windowManager.scan(false)` 获取 hwnd 与初始 rect，调用真实 preload/IPC `saveSnapshot()`、`applyLayout({ customRects, saveRestorePoint:true })`、`restoreSnapshot()`、再次 `applyLayout()` 与 `restorePrevious()`，并用后续扫描结果按像素容差校验布局移动、快照恢复和撤销恢复。验证命令：`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-c-2" --timeout=120000 --workers=1` 为 `1 passed (15.9s)`；`pnpm typecheck` 通过。
- `P4.2-d / P5.1`：2026-04-30 新增真实 Electron E2E `P4.2-d/P5.1 AI 进度状态由真实 tracker 派生且无矛盾组合`。测试启动真实 Node 子进程并让真实 `AITaskTracker` 扫描为 codex-like AI 任务，再通过 dev/test-only 主进程 hook 推进真实 tracker task，经真实 `ai-task:status-changed` / `task-completed` IPC 进入 renderer；E2E 对真实 DOM `ai-task-card` 采集 1020 次状态/进度快照，覆盖 idle hidden、thinking indeterminate、coding 40..75、validating 92、waiting-input 98、error 100、coding -> completed 100，并验证任务折叠到真实 history。验证命令：`pnpm typecheck` 通过；`pnpm build` 通过；`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-d/P5.1" --timeout=180000 --workers=1` 为 `1 passed (48.1s)`。
- `P4.2-e`：2026-04-30 新增真实 Electron E2E `P4.2-e 窗口操作目录对真实窗口执行关键操作`。测试创建真实 `BrowserWindow`，通过真实 `windowManager.scan(false)` 获取 hwnd，并调用真实 preload/IPC 覆盖 `focus`、`minimize`、`restore`、`maximize`、`setTopmost`、`setOpacity`、`toggleFavorite`、`screenshot`、`setTitle` 与 `close`；截图断言真实 PNG 文件落盘，关闭断言后续扫描中窗口消失。验证命令：`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-e" --timeout=120000 --workers=1` 为 `1 passed (9.8s)`；`pnpm typecheck` 通过。
- `P2.2` 修复补充：`SystemProcessScanner.getProcessDeepDetail()` 现在会在非管理员且 `userName` / `commandLine` / `executablePath` 等特权字段缺失时标记 `requiresElevation=true`；`probeProcessAccess()` 同步把这种真实 partial-read 情况归类为 `access-denied` 并返回 `suggestion='relaunch-as-admin'`，避免只显示可读基础字段却没有权限原因。
- `X1`：2026-04-30 复验补齐 `IScanner` runtime lifecycle schema，并把 P4.2-b 新增的 `DetectionSignalName` / `SignalResult` / `SignalContribution` / `CalibrationSample` 纳入 `CONTRACT_22_TYPE_NAMES` 固定清单；`contract-models.test.ts` 现在双向断言 `Object.keys(contractSchemaRegistry)` 与文档清单完全一致，防止 registry 新增模型后测试漏覆盖。验证命令：`pnpm exec vitest run src/shared/schemas/contract-models.test.ts && pnpm typecheck`，结果为 1 file / 6 tests passed，typecheck 通过。
- `X2`：preload 合同测试已覆盖 `ai-task:calibrate` 与 `topology.buildScopedGraph` / `topology.buildScopedFlow` / `topology.warmScope`，确保新增 renderer bridge、main handler 和 `contracts/23` 白名单一致。
- 2026-04-30 P8.1 / X1 合并复验：`pnpm lint` 通过且 no-emoji 输出 `No emoji found in 216 files.`；`pnpm test` 通过 `42 files / 407 tests`；`pnpm test:e2e` 通过 `14 passed (1.3m)`。GitNexus `detect_changes(scope=all)` 仍因历史累计 63 个 dirty files 报 `risk_level=critical`，按全局积压风险解读，不归因于 P8.1 / X1 切片。
- `X3`：2026-04-30 复跑真实 Electron E2E `X3 审计日志记录真实窗口敏感操作成功与失败结果`。测试创建真实 `BrowserWindow`，经真实 `windowManager.scan(false)` 找到目标 hwnd，调用真实 preload/IPC `windowManager.setTitle()` 覆盖成功与校验失败两条路径，并从 Electron `userData/logs/security-audit.log` 读取结构化 `window:set-title` 审计条目。验证命令：`pnpm build` 通过；`pnpm exec playwright test e2e/example.spec.ts -g "X3" --timeout=120000 --workers=1` 为 `1 passed (4.1s)`。
- `X4`：本轮 `pnpm lint` 内部 no-emoji gate 输出 `No emoji found in 216 files.`。
- 2026-04-30 0421 新增 E2E 组合复验：`pnpm exec playwright test e2e/example.spec.ts -g "P4.2-a|P4.2-a-3|P4.2-c-1|P4.2-c-2|P4.2-e|X3" --timeout=120000 --workers=1` 为 `6 passed (45.1s)`；完整真实 Electron E2E `pnpm test:e2e` 为 `22 passed (2.5m)`。
- 2026-04-30 质量门禁复验：`pnpm typecheck` 通过；`pnpm lint` 通过且 no-emoji 输出 `No emoji found in 216 files.`；`pnpm test` 为 `42 files / 409 tests passed`。
- 2026-04-30 本轮完整验证：`pnpm typecheck` 通过；`pnpm build` 通过；`pnpm exec vitest run src/main/services/SystemProcessScanner.test.ts src/renderer/components/monitor/ProcessDetailDrawer.test.tsx src/main/services/runtime/PowerShellGateway.test.ts src/preload/preloadContract.test.ts` 为 66 tests passed；`pnpm test` 为 42 files / 405 tests passed；`pnpm test:e2e` 为 10 tests passed。

## Full 0421 Closure Status

- 0421 验收矩阵当前 33/33 项均为 `[TEST-PASS]` 自动化状态，覆盖真实 Electron E2E、Vitest、typecheck、lint/no-emoji、build、长跑 bench 与 P4.2-b 30 分钟真实 Claude Code oracle bench。
- 仍不能替代用户手测 `[USER-VERIFIED]`：该状态只允许用户实际操作确认后标记。
- `GitNexus detect_changes(scope=all)` 已在 2026-04-30 重新执行成功：当前根工作树存在大量历史未提交变更，报告 `changed_files=63`、`changed_count=356`、`affected_count=98`、`risk_level=critical`。2026-05-02 本轮最后一次 GitNexus `detect_changes` 因 MCP transport closed 未返回；新增模块的真实状态以本地文件、Vitest/typecheck/lint/build、短 smoke 和正式 bench 报告为准。

- 2026-04-30 P8.2 TEST-PASS：新增真实 Electron E2E `P8.2 外观四轴设置可真实应用并跨重启持久化`，覆盖信息密度独立切换、圆角族 token、reduced motion token、palette 语义色 token、Paper Zen 预设组合、settings store 持久化与重启恢复；首轮测试暴露 `--duration-theme` 被后导入 `animations.css :root` 覆盖，已通过提高 `html[data-motion-level]` specificity 修复并复验通过。
