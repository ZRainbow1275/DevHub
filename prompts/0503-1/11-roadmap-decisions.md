# 11 — 节奏 / 灰度 / 回滚 / Demo 验收决策（提炼版）

> **派生自**: `prompts/0503/11-roadmap-rollback-survey.md`
> **核心目标**: 避免 R5/R6/R7「看似改过实未改」的悲剧重演
> **签字**: ZRainbow 2026-05-03

---

## A. R8 范围

### A.1 Must 优先级排序（[Q-11.A.1] [ACCEPT]）
**用户回答**: 「从上到下：2 8 4 3 7 5 10 9 6 1」

| 序 | 事项 |
|----|------|
| 1 | **集成库引入与封装** |
| 2 | **进程 Card/List API 统一** |
| 3 | **AI 进度真实 CLI 解析** |
| 4 | **AI 任务感测引擎调优** |
| 5 | **监控窗口** |
| 6 | **横切：权限/IPC 限流/日志/可观测** |
| 7 | **端口悬浮卡片 Pop-out** |
| 8 | **拓扑/流程附属化可见性强化** |
| 9 | **收纳系统**（Drawer + Pop-out + 命令面板 + 仪表板） |
| 10 | **主题轴差异强化** |

### A.2 Should（[Q-11.A.2] [ACCEPT]）
**用户回答**: 全选

R8 还纳入：
- 全局命令面板（cmdk）
- 仪表板（react-grid-layout）
- SKILL 内置库 10 项
- 任务录像（轻量）
- DAG 编排（基础）
- 关系视图新布局算法（dagre / radial）
- 进程批量操作
- 端口安全分级
- 窗口跨虚拟桌面
- AI 工具实例消歧 7 维
- always-on-top 操作补齐

### A.3 Won't（[Q-11.A.3] [CHANGE]）
**用户原话**: 「**除去全屏拓扑视图（顶级一级入口）、任务编排可视化编辑器（拖拽节点）以外的全部**」

→ R8 **保留**（与默认相反）的两项：
- **全屏拓扑视图（顶级一级入口）** — 与 08.H.1 一致
- **任务编排可视化编辑器（拖拽节点）** — 与 07.H.1 [Q-7.H.1] 简单依赖结合

→ R8 **延后到 R9**（11 项）：
- 国际化（仅简中）
- 公网遥测
- 自动更新
- 插件市场 / 扩展机制
- 云端 SKILL 同步
- 任务录像可视回放（asciinema 风）
- AI 推荐主题轴
- OCR 启用（默认关闭）
- mitmproxy HTTP 嗅探
- Windows Service 模式
- 集成 Linear / Notion / Slack

---

## B. 批次划分

### B.1 是否分批（[Q-11.B.1] [ACCEPT]）
**用户回答**: B — 3 批（R8.A 救火 + R8.B 主题与体验 + R8.C AI 编排）

### B.2 各批次内容（[Q-11.B.2] [ACCEPT]）
**用户回答**: 默认 + 内含调整（A.1 优先级把"集成库"排第 1 → R8.A 必须含集成库）

#### R8.A — 急修与可见性 + 集成库（约 1-2 周）
- **集成库引入封装**（A.1 #1）
- 进程 Card/List API 统一（A.1 #2）
- 拓扑入口可见性强化（A.1 #8）
- 主题 4 维选择器暴露 + 默认轴拉大（A.1 #10）
- always-on-top 补齐
- 端口卡片改进（不一定 popout，先优化布局）
- 横切：审计日志 + 权限提示（A.1 #6 部分）

#### R8.B — 收纳与体验（约 2-3 周）
- 端口 Pop-out 系统（A.1 #7）
- Drawer 系统（顶/右/底）
- 命令面板（Cmd+K）
- 仪表板自定义
- 进程 Treemap / Tree 视图
- 主题装饰几何扩充
- 状态栏扩展
- 收纳系统完整（A.1 #9）

#### R8.C — AI 编排核心（约 3-4 周）
- AI 信号源扩充（CLI stdout / OCR 接口 / 文件监听）
- **进度真实 CLI 集成**（A.1 #3）
- **AI 感测调优**（A.1 #4）
- **监控窗口**（A.1 #5）
- SKILL 内置库 10 项
- CSV 任务驱动
- Watchdog 引擎（独立子进程）
- 自动注入
- DAG 基础 + 简易编辑器
- 任务录像（轻量）
- 全屏拓扑顶级入口

---

## C. Feature Flag

### C.1 是否引入（[Q-11.C.1] [ACCEPT]）
**用户回答**: B — 每个新功能挂 flag，默认 ON（保留紧急关闭能力）

### C.2 命名规范（[Q-11.C.2] [ACCEPT]）
**用户回答**: `R8.{batch}.{module}.{feature}`

例：
- `R8.B.port.popout`
- `R8.C.ai.watchdog`
- `R8.A.process.unified-vm`

---

## D. 回滚策略

### D.1 回滚条件（[Q-11.D.1] [ACCEPT]）
**用户回答**: 全选

立即回滚触发条件：
- 主进程崩溃率 > 5%
- 主进程 RSS > 800 MB
- CPU > 30% 持续 1 分钟
- 用户手测发现 P0 / P1 Bug
- E2E 测试全绿率 < 90%
- 关键功能（AI 任务感测）误报率 > 30%

### D.2 回滚方式（[Q-11.D.2] [ACCEPT]）
**用户回答**: C — Feature flag 优先 + Git revert 兜底

---

## E. Demo / 验收脚本

### E.1 Demo 形式（[Q-11.E.1] [ACCEPT]）
**用户回答**: E — 录屏 + Playwright 自动 + 用户手测清单（多层保险）

### E.2 验收强制（[Q-11.E.2] [ACCEPT]）
**用户回答**: A — 每个 Q 必须配验收（Spec 文档自带 Given/When/Then）

### E.3 用户手测节奏（[Q-11.E.3] [ACCEPT]）
**用户回答**: D — 每批次结束 + 每天关键路径

---

## F. 用户参与

### F.1 周投入（[Q-11.F.1] [CHANGE]）
**用户原话**: 「**不在乎时间和成本**」

→ 用户深度介入；Spec 编写时不考虑节省用户时间

### F.2 频道（[Q-11.F.2] [ACCEPT]）
**用户回答**: A — 仅 Trellis 任务对话

---

## G. 失败保护

### G.1 R8.A 后用户感知未变（[Q-11.G.1] [ACCEPT]）
**用户回答**: A — 暂停后续批次，开 RCA

### G.2 R8.A 必须达到的"用户感知断言"（[Q-11.G.2] [ACCEPT]）
**用户回答**: 全选

5 条断言：
1. **进程卡片切到列表后，详情字段相同**
2. **进程详情面板第一眼能看到关系视图入口**
3. **切主题时不仅颜色变，密度/字体/装饰至少有一项明显不同**
4. **always-on-top 按钮可点击且生效**
5. **端口面板比之前好看不挤**

---

## H. 文档产出节奏

### H.1 PRD/Spec 形式（[Q-11.H.1] [ACCEPT]）
**用户回答**: A — 每批次一份 PRD + N 份 Spec（同 R7 风格）

### H.2 Spec 必含要素（[Q-11.H.2] [ACCEPT]）
**用户回答**: 全选 — 13 个章节

1. 动机（含用户原话）
2. 受影响源码
3. 数据契约（TS + Zod）
4. IPC 契约
5. 错误矩阵
6. 验收条件（Given/When/Then）
7. E2E 脚本草案（Playwright）
8. 参考实现 / 集成库
9. 影响半径与预计 LoC
10. 检查清单（给实现 Agent）
11. 与 R8 其他 Spec 的依赖关系
12. 失败 fallback 策略
13. 性能预算

### H.3 Spec 数量上限（[Q-11.H.3] [CHANGE]）
**用户原话**: 「**A，不限制，交给 AI 写的**」

→ 默认是 D ≤15/批，用户改为 A 不限
→ R7 是 30 份，R8 可能 50+
→ 不强制压缩 Spec 数量

---

## I. 总节奏

### I.1 总周数（[Q-11.I.1] [CHANGE]）
**用户原话**: 「**D，时间成本不是问题**」

→ R8 不设周数上限
→ 但每批次有"质量门"（D 节回滚条件）

### I.2 R8 元改进任务（[Q-11.I.2] [ACCEPT]）
**用户回答**: A — 是，另开"R8 元改进"长期任务

→ 单独创建 Trellis 任务，专注"避免 R5-R7 失败模式重演"

### I.3 优先回答的核心问题（[Q-11.I.3]）
**用户回答**: 「是」

→ Spec 编写时必须把"用户感知是否会通过"放在每份 Spec 顶部

---

## J. 用户签字

- [x] 已通读 11 份需求表
- [x] 同意以已填项 + 推荐默认作为 R8 PRD/Spec 编撰基础
- [x] 同意 R7 三大硬约束（不删 / 不 Emoji / 不 Mock）继承到 R8
- [x] 同意 R8 三大新约束（不重构大框架 / 冗余开发优先 / 集成而非自研）
- [x] 同意"R8.A 完成后未达用户感知通过 → 暂停后续批次"的失败保护
- [x] 授权 Claude 在 PRD/Spec 阶段并行启动 ≤ 3 个研究 / 编撰 agent
- [x] 同意把本需求表与 PRD/Spec 一并存档于 `prompts/0503/`

签字：**ZRainbow 0503**

---

## K. PRD 信号

1. **批次顺序调整**：A.1 优先级把"集成库"排第 1，必须把它纳入 R8.A
2. **R8.A 必须满足 5 条用户感知断言**：失败 → 暂停 + RCA
3. **保留全屏拓扑 + 任务编排可视化编辑器**：用户没排除
4. **不限时不限 Spec 数**：交给 AI 写
5. **每个 Q 都要 Given/When/Then**：可量化验收
6. **每批次 PRD + N 份 Spec**：R7 风格
7. **三层 Demo 保险**：录屏 + Playwright + 手测
8. **flag 命名固定模式**：`R8.{batch}.{module}.{feature}`
9. **元改进任务并行开**：积累工程文化
10. **质量门优先于时间**：6 条回滚条件任一触发即回滚
