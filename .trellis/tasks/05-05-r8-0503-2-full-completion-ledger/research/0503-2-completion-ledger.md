# prompts/0503-2 Completion Ledger

Generated baseline: 2026-05-05. This ledger is intentionally conservative: any open checklist, pending marker, or explicit completion boundary keeps the row out of `verified` unless local evidence closes the whole spec.

## Summary

| Batch | Files | Open checkboxes | Checked checkboxes | Verified | Partial | Missing | N/A |
|---|---:|---:|---:|---:|---:|---:|---:|
| R8.A | 12 | 0 | 0 | 12 | 0 | 0 | 0 |
| R8.B | 18 | 0 | 241 | 18 | 0 | 0 | 0 |
| R8.C | 40 | 0 | 455 | 40 | 0 | 0 | 0 |
| _shared | 9 | 0 | 17 | 9 | 0 | 0 | 0 |
| root | 2 | 9 | 0 | 0 | 0 | 0 | 2 |

## Current Open Blocker Audit

- 2026-05-22 elevated completion closure: current persisted `r8-external-blockers-current.json` was regenerated from an Administrator UAC run at `2026-05-22T15:59:54.248Z` and all six active external gates passed. The report keeps the real single-display fallback for BrowserWindow placement and physical-display stability (`targetMode=single-display-fallback`), closes the Administrator and `devhub-watchdog` Windows Service gates with `admin.isAdministrator=true` and `service.scExitCode=0`, and closes zero-egress with a 60-second live `pktmon` capture where whole-machine `globalPacketCount=6414` is preserved while the DevHub target process tree has `appScopedPassed=true` and `nonLoopbackEndpointCount=0`. The authoritative `pnpm --silent check:0503-strict:vd-watch` now exits `0`, `pnpm --silent check:0503-acceptance-pack:no-refresh` reports `status=complete`, and no active R8 0503-2 strict gate requires legal/product evidence; the prior `R8_LICENSE_LEGAL_DECISION_RECORDED` item is a legacy `prompts/0503` survey carryover, not a `prompts/0503-2` completion blocker.
- 2026-05-21 completion status owner lane command-set hardening: `generate-0503-acceptance-pack.mjs` now writes the full per-owner lane command set into `0503-completion-status.json` `nextOwnerCommands`, including list-actions, readiness, readiness+evidence-dir, readiness+coverage-artifacts, summary, blocker taxonomy, partial R8 dossier, closure bundle, require-complete, submission template directory, raw evidence template directory, coverage report, and coverage JSON commands. `0503-completion-status.md` renders the same fields in `Next Owner Commands`, and `verify-0503-evidence-pack.mjs` recomputes the expected rows from `0503-owner-action-queue.json` while the success output names `completion status owner lane command sets`. This improves owner/operator intake discoverability only and does not close any external, owner, or strict gate.
- 2026-05-21 completion status owner lane source-pointer hardening: `nextOwnerCommands` rows now include `sourceEvidencePath` pointers back to `0503-owner-action-queue.json#/ownerLaneCommands/<index>` or the owner-count fallback, and `0503-completion-status.md` renders the same `Evidence` column. `verify-0503-evidence-pack.mjs` validates the field through the expected owner-command rows and the recursive path-like JSON pointer audit; the refreshed evidence-pack output reports 299 consistent pointer targets. This improves traceability only and does not close any external, owner, or strict gate.
- 2026-05-21 strict failure owner lane command-set hardening: `run-0503-strict-completion.mjs` now prints `Owner lane command sets` in the expected non-zero strict summary, including list-actions, readiness, readiness+evidence-dir, readiness+coverage-artifacts, owner summary, blocker taxonomy, partial R8 dossier, closure bundle, require-complete, submission template directory, raw evidence template directory, coverage report, and coverage JSON commands for each remaining owner. `pnpm --silent check:0503-strict:self-test` locks these fields, and the completion audit named-command requirement now states the same coverage. This improves direct console handoff only and does not close any external, owner, or strict gate.
- 2026-05-21 strict completion report owner lane command-set hardening: `verify-0503-ledgers.mjs` now renders `## Owner Lane Command Sets` in `0503-strict-completion-report.md` from `0503-completion-status.json#nextOwnerCommands`, including action count, list-actions, readiness, readiness+evidence-dir, readiness+coverage-artifacts, owner summary, blocker taxonomy, partial R8 dossier, closure bundle, require-complete, submission template directory, raw evidence template directory, coverage report, coverage JSON, and source evidence path columns. `verify-0503-evidence-pack.mjs` now verifies the strict report section, exact Markdown rows, non-completion boundary text, and final success coverage item `strict completion report owner lane command sets`. `pnpm --silent check:0503-acceptance-pack:no-refresh` passed after regenerating the expected non-complete strict ledger report; the pack remains `status=not-complete` with `missingOrIncompleteRequirements=19` and `failedExternalGates=6`. This improves the strict report handoff only and does not close any external, owner, or strict gate.
- 2026-05-21 strict completion report failed-gate verification note hardening: `verify-0503-ledgers.mjs` now reads `0503-owner-action-queue.json` and renders each failed external gate's owner action `verificationCommandNote` inside `0503-strict-completion-report.md`. The strict report failed-gate table now carries a `Verification command note` column beside the verification command and action/raw/submission template commands, so the report itself states why second-display, physical-hotplug, administrator, Windows Service, zero-egress, and license/legal blockers cannot be closed with templates, preflight output, screenshots, or documentation-only evidence. `verify-0503-evidence-pack.mjs` locks the exact note text via the owner action queue and the success output now includes `strict completion report failed gate verification notes`. `pnpm --silent check:0503-acceptance-pack:no-refresh` passed; the pack remains `status=not-complete`, `missingOrIncompleteRequirements=19`, and `failedExternalGates=6`. This improves failed-gate evidence clarity only and does not close any external, owner, or strict gate.
- 2026-05-21 strict terminal failed-gate verification note hardening: `run-0503-strict-completion.mjs` now reads `0503-owner-action-queue.json` during expected strict non-completion and prints `Failed external gate verification notes` directly in the terminal summary. The console output now carries each failed external gate's real-evidence closure rule before the owner evidence command sets, including the second-display BrowserWindow report, physical monitor hotplug, Administrator shell, Windows Service install, zero-egress pktmon capture, and legal/product license decision requirements. Validation passed with `node --check` for `run-0503-strict-completion.mjs`, `generate-0503-acceptance-pack.mjs`, and `verify-0503-evidence-pack.mjs`; `pnpm --silent check:0503-strict:self-test`; `pnpm --silent check:0503-evidence-pack:self-test`; and `pnpm --silent check:0503-acceptance-pack:no-refresh`. The authoritative `pnpm --silent check:0503-strict:vd-watch` rerun still exits `1` with `missingOrIncompleteRequirements=19`, `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`, so this is terminal diagnostic hardening only and does not close any strict gate.
- 2026-05-21 acceptance pack failed-gate verification note hardening: `generate-0503-acceptance-pack.mjs` now includes `verificationCommandNote` on each `failedExternalGates[]` item and renders the note in `0503-acceptance-pack.md` `Failed External Gate Actions`. `verify-0503-evidence-pack.mjs` now rejects missing acceptance-pack failed-gate notes, includes `acceptance pack markdown including failed gate verification notes` in the success coverage output, and keeps `r8-external-blockers-current.json` parity scoped to external-report fields so owner handoff notes do not create false external-report drift. Validation passed with generator/verifier `node --check`, acceptance-pack self-test, evidence-pack self-test, and `pnpm --silent check:0503-acceptance-pack:no-refresh`; the status remains `not-complete` with `missingOrIncompleteRequirements=19` and `failedExternalGates=6`. This is top-level report clarity only and does not close any strict gate.
- 2026-05-21 completion status failed-gate verification note hardening: `buildFailedExternalGateCommandSets()` now propagates owner action `verificationCommandNote` into `0503-completion-status.json#failedExternalGateCommandSets`, and `0503-completion-status.md` renders a `Verification command note` column in `Failed External Gate Command Sets`. `verify-0503-evidence-pack.mjs` now validates the JSON field and Markdown row for every failed gate and reports `completion status failed external gate command sets with verification notes`. Validation passed with generator/verifier `node --check`, acceptance-pack self-test, evidence-pack self-test, and `pnpm --silent check:0503-acceptance-pack:no-refresh`. This is status-level handoff hardening only and does not close any strict gate.
- 2026-05-21 completion status failed-gate command-set hardening: `generate-0503-acceptance-pack.mjs` now writes `failedExternalGateCommandSets` into `0503-completion-status.json`, and `0503-completion-status.md` now renders a `Failed External Gate Command Sets` table. Each failed external gate row carries owner, blocker kind, current evidence, verification command, action dossier command, raw evidence template command, submission template command, and a source JSON pointer back to `0503-acceptance-pack.json#/failedExternalGates/<index>`. `verify-0503-evidence-pack.mjs` recomputes the expected rows from the acceptance pack plus owner action queue and rejects JSON or Markdown drift. This improves owner/operator intake discoverability only and does not close any external, owner, or strict gate.
- 2026-05-21 evidence-pack verifier success-output drift prevention: `verify-0503-evidence-pack.mjs` now builds the final passed-coverage line from `evidencePackVerifierSuccessCoverageItems` through `buildEvidencePackVerificationPassedMessage()` instead of duplicating a long hand-written console string. The self-test now asserts every coverage item plus the path-like JSON pointer count appears in the generated success output. This prevents recurrence of coverage-text drift and does not close any external, owner, or strict gate.
- 2026-05-21 evidence-pack verifier success-output parity hardening: `verify-0503-evidence-pack.mjs` now names `prompt-to-artifact machine evidence fields and JSON pointer targets` in the final passed-coverage message, matching the current `EVIDENCE_PACK_VERIFIER_COMMAND` requirement and the JSON Pointer verifier coverage. This is audit-output drift hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 evidence path repo-root containment hardening: `verify-0503-evidence-pack.mjs` now resolves evidence paths through `resolvedRepoRoot`, rejects empty paths, rejects absolute paths, and rejects `..` paths that escape the repository root before checking file existence or JSON pointer targets. The self-test now passes `../outside.json` to `verifyEvidencePathExists()` and requires a repo-root escape failure. `EVIDENCE_PACK_VERIFIER_COMMAND` and verifier success output now name `referenced evidence file paths with repo-root containment`. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-no-emoji`, targeted `git diff --check`, and canonical strict `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits `STRICT_EXIT=1` with `missingOrIncompleteRequirements=19`, `partialRows=5`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is verifier safety hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 completion audit startup source evidence path hardening: `verify-0503-evidence-pack.mjs` now requires completion audit `sourceEvidence` to include the manual checklist, DevHub package script, and Electron Vite config paths: `devhub/docs/manual-testing-checklist.md`, `devhub/package.json`, and `devhub/electron.vite.config.ts`. The verifier self-test removes `devhub/electron.vite.config.ts` from the fixture source evidence and requires failure, so the human audit cannot silently drop the runtime configuration evidence while the direct verifier still reads it. `EVIDENCE_PACK_VERIFIER_COMMAND` and the verifier success output now name `completion audit startup source evidence paths`. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-no-emoji`, targeted `git diff --check`, and canonical strict `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits `STRICT_EXIT=1` with `missingOrIncompleteRequirements=19`, `partialRows=5`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is source-evidence coverage hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 startup config dual-running-surface contract hardening: `verify-0503-evidence-pack.mjs` now binds the manual DevHub startup guidance to real configuration evidence. The verifier checks `devhub/package.json` for `scripts.dev=electron-vite dev` and checks `devhub/electron.vite.config.ts` for `main`, `preload`, `renderer`, renderer `server`, and `host: '127.0.0.1'`. `generate-0503-acceptance-pack.mjs` adds both files to completion audit `sourceEvidence`, and `0503-completion-audit.md` renders them in the `Source Evidence` table beside the manual checklist. `EVIDENCE_PACK_VERIFIER_COMMAND` and the verifier success output now name `startup config dual-running-surface contract`. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-no-emoji`, targeted `git diff --check`, and canonical strict `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits `STRICT_EXIT=1` with `missingOrIncompleteRequirements=19`, `partialRows=5`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is documentation-to-runtime evidence binding only and does not close any external, owner, or strict gate.
- 2026-05-21 manual testing dual-running-surface docs hardening: `verify-0503-evidence-pack.mjs` now locks the DevHub startup guidance in `devhub/docs/manual-testing-checklist.md`, including the `electron-vite dev` renderer/Electron main split, the difference between DevHub-managed child-process logs and a separately opened `powershell.exe -NoExit` terminal, and the requirement to verify port ownership before attributing listeners such as `127.0.0.1:3001` to DevHub. `EVIDENCE_PACK_VERIFIER_COMMAND`, verifier success output, completion audit `sourceEvidence`, and the human-readable `0503-completion-audit.md` `Source Evidence` table now name this coverage as `manual testing dual-running-surface docs`. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, full local `pnpm --silent check:0503-local`, `pnpm --silent check:0503-no-emoji`, targeted `git diff --check`, and canonical strict `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits `STRICT_EXIT=1` with `missingOrIncompleteRequirements=19`, `partialRows=5`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is operator-facing documentation drift hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 evidence-pack command checklist coverage statement: `EVIDENCE_PACK_VERIFIER_COMMAND` now declares the same current surfaces that `verify-0503-evidence-pack.mjs` actually verifies and prints, including `schemaVersion guards`, `source hashes`, `raw owner evidence template schemaVersion`, `benchmark evidence schemas`, `HANDOFF current summary`, `completion audit open requirement external/owner boundary`, and `referenced evidence file paths`. `verify-0503-evidence-pack.mjs --self-test` now also mutates that requirement text by removing `HANDOFF current summary` and requires the drift to fail. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and targeted `git diff --check` passed. This is audit statement hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 completion status owner readiness evidence-dir continuation command: `0503-completion-status.json` now exposes `continuationCommands.ownerReadinessWithEvidenceDir=pnpm check:0503-owner-evidence -- --owner-readiness --owner <owner> --evidence-dir <repo-relative-dir>`, and `0503-completion-status.md` renders it in the top-level `Continuation Commands` section. This keeps the generic owner evidence-dir workflow visible even before a reviewer opens the row-level blocked criteria table. `verify-0503-evidence-pack.mjs` locks the exact JSON command, Markdown line, and self-test fixture. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed. This is owner evidence directory handoff hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 completion status blocked criteria Markdown owner/readiness columns: `0503-completion-status.md` now renders `Owners` and `Owner readiness evidence-dir commands` inside the `Blocked Success Criteria Owner Links` table, so a reviewer can route every aggregate blocker to the correct owner lane and evidence directory command without opening `0503-completion-status.json`. `generate-0503-acceptance-pack.mjs` renders these columns from `ownerActionOwners` and `ownerReadinessWithEvidenceDirCommands`, and `verify-0503-evidence-pack.mjs` locks both the table header and every row's owner/readiness values. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed. A final `pnpm --silent check:0503-strict:vd-watch` rerun still exits 1 at the real strict boundary with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is human-readable owner handoff hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 completion status blocked success criteria owner links: `generate-0503-acceptance-pack.mjs` now writes `blockedSuccessCriteriaOwnerLinks` into `0503-completion-status.json`, and `0503-completion-status.md` renders a `Blocked Success Criteria Owner Links` table. Each blocked aggregate success criterion now carries linked owner action ids, owners, action dossier commands, raw evidence template commands, submission template commands, owner-readiness-with-evidence-dir commands, and the source evidence pointer. The current blocked criteria are `EXTERNAL_GATE_CLOSURE`, `R8_PARTIAL_ROW_CLOSURE`, `SURVEY_ACCEPTANCE_CLOSURE`, `OWNER_ACTION_QUEUE_CLOSURE`, and `STRICT_COMPLETION_GATE`; direct audit confirms owner/action command counts of `6/6`, `5/5`, `3/3`, `11/11`, and `11/11` respectively. `verify-0503-evidence-pack.mjs` now recomputes this status-level crosswalk and locks the Markdown rows; its self-test fixture was updated to reject drift. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed. A final `pnpm --silent check:0503-strict:vd-watch` rerun still exits 1 at the real strict boundary with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is status overview owner-intake hardening only and does not close any external, owner, or strict gate.
- 2026-05-21 completion-status source-file dossier continuation command: `buildCompletionStatus()` now exposes `continuationCommands.ownerSourceFileDossier=pnpm check:0503-owner-evidence -- --source-file-dossier --action <actionId> --file <prompt-file>`, and `0503-completion-status.md` renders `Owner source file dossier` in the Continuation Commands section. `verify-0503-evidence-pack.mjs` locks the exact JSON command, Markdown line, and self-test fixture so source-level owner intake remains discoverable from the status overview, not only from action queue or closure bundle reports. `node --check`, evidence-pack self-test, and acceptance pack `--no-refresh` passed. This is continuation-entrypoint hardening only and does not close any strict external or owner gate.
- 2026-05-21 owner output matrix source-file dossier coverage: `verify-0503-owner-evidence.mjs --owner-output-matrix` now recursively checks `sourceFiles.files[]` rows for `sourceFileDossierCommand`, `actionDossierCommand`, `rawEvidenceTemplateCommand`, and `submissionTemplateCommand`, in addition to existing action/blockingAction command checks and recommended strict command checks. `OWNER_OUTPUT_MATRIX_COMMAND` in the completion audit command checklist now explicitly requires source-file dossier coverage, and `verify-0503-evidence-pack.mjs` locks that requirement text. A real matrix run returned `schemaVersion=devhub-0503-owner-output-matrix-v1`, `gapCount=0`, `ok=true`, `ownerCount=4`, and rows for `blocker-taxonomy`, `owner-readiness`, `owner-summary`, `owner-closure-bundles`, and `partial-r8-dossier` across every owner lane. This is command discoverability hardening only and does not close any strict external or owner gate.
- 2026-05-21 owner closure bundle source-file dossier propagation: `buildOwnerClosureBundles()` now carries each action's `sourceFiles` from `0503-owner-action-queue.json` into `0503-owner-closure-bundles.json`, and `0503-owner-closure-bundles.md` renders source file summaries plus source-file dossier commands beside the partial R8 dossier commands. `verify-0503-evidence-pack.mjs` locks the JSON and Markdown rows, while `verify-0503-owner-evidence.mjs` validates that closure bundle action `sourceFiles` match the queue action `sourceFiles` and that each source file row includes the canonical `--source-file-dossier --action <actionId> --file <prompt-file>` command. Direct current closure bundle audit returned `sourceActionCount=5`, `sourceRows=26`, and `gapCount=0`. `node --check`, owner/evidence/acceptance self-tests, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and targeted `git diff --check` passed. This is owner intake traceability hardening only and does not close any strict external or owner gate.
- 2026-05-21 owner source-file dossier command hardening: `verify-0503-owner-evidence.mjs` now supports `--source-file-dossier --action <actionId> --file <prompt-file>` and returns `schemaVersion=devhub-0503-source-file-dossier-query-v1`, source row count, owner/action metadata, source file path, `sourceFileDossierCommand`, `actionDossierCommand`, `rawEvidenceTemplateCommand`, `submissionTemplateCommand`, verification command, required evidence, and the strict completion boundary. `generate-0503-acceptance-pack.mjs` writes the four source-file command fields into owner action queue source file rows, `0503-owner-action-queue.md` renders them in every checkbox source section, `verify-0503-evidence-pack.mjs` locks the JSON/Markdown shape, and `package.json` `check:0503-local` samples `pnpm check:0503-owner-evidence -- --source-file-dossier --action survey-context --file prompts/0503/14-three-graph-systems-survey.md`. Direct sampling returned one row for `prompts/0503/14-three-graph-systems-survey.md` with `status=open-source-file-rows`. `node --check`, owner/evidence self-tests, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, `pnpm --silent check:0503-local` (`LOCAL_EXIT=0`), and `pnpm --silent check:0503-strict:vd-watch` passed up to the true strict boundary. Strict still exits `STRICT_EXIT=1` with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`; this is source-level owner intake hardening only and does not close any external or owner gate.
- 2026-05-21 owner-facing query matrix hardening: `verify-0503-owner-evidence.mjs` now returns structured empty partial R8 dossiers for owners without partial R8 rows (`status=no-partial-r8-rows`, `rowCount=0`) instead of throwing, and the owner-facing `blocker-taxonomy`, `owner-readiness`, `owner-closure-bundles`, and `partial-r8-dossier` JSON surfaces consistently expose `actionDossierCommand(s)`, `rawEvidenceTemplateCommand(s)`, and `submissionTemplateCommand(s)` on linked taxonomy/partial rows. A direct sequential matrix audit across `operator`, `legal-product`, `product`, and `user-product` for `blocker-taxonomy`, `owner-readiness`, `owner-summary`, `owner-closure-bundles`, and `partial-r8-dossier` returned `gapCount=0`; non-operator partial R8 lanes correctly returned `no-partial-r8-rows`. `generate-0503-acceptance-pack.mjs` and `verify-0503-evidence-pack.mjs` now lock the matching command checklist text for owner readiness, owner readiness with evidence-dir, owner blocker taxonomy, owner closure bundle, and partial R8 dossier outputs. `node --check`, owner/evidence/acceptance self-tests, acceptance pack `--no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits `STRICT_EXIT=1` with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`; this is tooling coverage only and does not close any external gate.
- 2026-05-21 owner action queue JSON now stores the canonical `actionDossierCommand` on every action instead of requiring downstream tools to derive it from the submission template command. `generate-0503-acceptance-pack.mjs` writes `pnpm --silent check:0503-owner-evidence -- --action-dossier --action <actionId>` into each action row and reuses that stored field when generating owner closure bundles. `verify-0503-evidence-pack.mjs` now requires the field in `0503-owner-action-queue.json` and in owner action queue Markdown owner-execution/global action rows. The current regenerated queue has 11/11 actions with `actionDossierCommand` and 0 missing rows. `node --check`, `generate-0503-acceptance-pack.mjs --self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, and `pnpm --silent check:0503-evidence-pack` passed. This only removes a machine-readability gap and does not close any strict gate.
- 2026-05-21 `0503-acceptance-pack.md` now mirrors the strict report by exposing owner intake commands per failed external gate; this was later hardened from a single action dossier column into the full `Action dossier command`, `Raw evidence template command`, and `Submission template command` set. `generate-0503-acceptance-pack.mjs` renders these columns in the Failed External Gate Actions table, and `verify-0503-evidence-pack.mjs` now requires exact failed-gate Markdown rows including `pnpm --silent check:0503-owner-evidence -- --action-dossier --action <gateId>`, `--print-evidence-template --action <gateId>`, and `--print-template --action <gateId>`. The current acceptance pack was regenerated and directly checked: `ASSERT_BROWSERWINDOW_SECOND_DISPLAY` and the other failed gates carry the full three-command set in the human-readable acceptance pack. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, and `pnpm --silent check:0503-evidence-pack` passed. This only aligns human-readable handoff surfaces and does not close any strict gate.
- 2026-05-21 strict failure summary and `0503-strict-completion-report.md` now both expose failed-gate action dossier commands. `run-0503-strict-completion.mjs` derives a `Failed external gate action dossier commands` terminal section from real `strict.failedExternalGateDetails`, keeps the non-zero completion boundary unchanged, and self-tests the new section. `verify-0503-ledgers.mjs` adds an `Action dossier command` column to the strict report's Failed External Gates table, and `verify-0503-evidence-pack.mjs` now locks that row-level column against the current acceptance pack. `STRICT_RUNNER_FAILURE_SUMMARY_COMMAND` in the completion audit checklist declares this coverage. `pnpm --silent check:0503-strict:vd-watch` was rerun and the output plus Markdown report now list all six per-gate dossier commands while still ending at `STRICT_EXIT=1` with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This only improves owner handoff precision and does not close any strict gate.
- 2026-05-21 license/legal verification command note now propagates beyond templates into owner action queue, owner closure bundle, and owner action dossier artifacts. `generate-0503-acceptance-pack.mjs` adds `verificationCommandNoteForAction()` so only `R8_LICENSE_LEGAL_DECISION_RECORDED` carries the PowerShell canonical / Git Bash acceptable-command note, then renders that note in owner execution rows, the global Actions table, closure bundle JSON, and closure bundle Markdown. `verify-0503-owner-evidence.mjs --action-dossier --action R8_LICENSE_LEGAL_DECISION_RECORDED` now exposes the same note on the main `action` payload as well as the raw evidence and submission templates; owner list, summary, readiness, and partial-R8 linked action JSON surfaces also carry `verificationCommandNote` for downstream tools. Completion audit command checklist requirements for owner action list, owner summary, owner readiness, owner evidence self-test, and the owner action queue workflow text now mention verification command note coverage so human-readable guidance matches the JSON outputs. `verify-0503-evidence-pack.mjs` rejects missing or drifted license note values in the owner action queue, owner action queue Markdown, owner closure bundles, and closure bundle Markdown; its self-test was corrected to use the same Markdown truncation contract as the real renderer. Regenerated `0503-owner-action-queue.*` and `0503-owner-closure-bundles.*` now show the license note while non-license actions remain blank. `node --check`, `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, and `pnpm --silent check:0503-evidence-pack` passed. This only reduces owner execution ambiguity and does not close the legal/product gate.
- 2026-05-21 license/legal raw evidence template now carries the same shell-boundary guidance that already existed on the owner submission template. `buildLicenseVerificationCommandNote()` centralizes the PowerShell canonical example / Git Bash acceptable-command note, `buildSubmissionTemplate()` and `buildRawEvidenceTemplate()` both reuse it for `R8_LICENSE_LEGAL_DECISION_RECORDED`, and the current `R8_LICENSE_LEGAL_DECISION_RECORDED.raw-evidence-template.json` was regenerated with `verificationCommandNote`. `verify-0503-owner-evidence.mjs` self-test now prints the license raw evidence template and asserts that note; `verify-0503-evidence-pack.mjs` checks the current raw template note, and `EVIDENCE_PACK_VERIFIER_COMMAND` / `OWNER_EVIDENCE_RAW_TEMPLATE_COMMAND` requirements now mention the license raw evidence shell-boundary note. `node --check`, `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, targeted `git diff --check`, and `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits with `STRICT_EXIT=1`, `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`; this owner-template hardening does not close any legal/product gate.
- 2026-05-21 evidence pack verifier now checks this completion ledger Markdown directly instead of relying only on `0503-ledger-verification.json` parity. `verifyCompletionLedgerMarkdownText()` requires the ledger source hash entry, all 81 prompt rows, row-level Markdown parity with `0503-ledger-verification.json`, the Current Open Blocker Audit section, and the owner template README hash workflow text including `--hash-evidence <repo-relative-evidence-file>`, `evidenceSha256`, `hashAlgorithm`, `evidenceModifiedAt`, `evidenceSizeBytes`, owner-readiness, require-complete, and strict rerun guidance. The verifier self-test now rejects a completion ledger fixture that drops the exact hash-evidence workflow. `EVIDENCE_PACK_VERIFIER_COMMAND` and the evidence-pack success output now name `completion ledger markdown`. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, targeted `git diff --check`, and `pnpm --silent check:0503-strict:vd-watch` passed up to the real strict boundary. Strict still exits with `STRICT_EXIT=1`, `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`; this verifier hardening does not close any external gate.
- 2026-05-21 owner template README hash workflow now explicitly tells owners to run `pnpm --silent check:0503-owner-evidence -- --hash-evidence <repo-relative-evidence-file>` for each raw evidence file before filling submission JSON, then copy `evidenceSha256`, `hashAlgorithm`, `evidenceModifiedAt`, and `evidenceSizeBytes` into the submission before owner-readiness, require-complete, and strict completion reruns. `writeTemplateDirectoryReadme()` renders the workflow for both submission and raw evidence template directories, `verify-0503-owner-evidence.mjs` self-test locks the README hash workflow and four metadata fields, and `verify-0503-evidence-pack.mjs` requires the current template README verifier to enforce the same text. The current owner submission/raw evidence template directories were regenerated with the updated README guidance. `node --check`, `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and targeted `git diff --check` passed. This only hardens owner evidence collection instructions and does not close any strict external gate.
- 2026-05-21 owner evidence submissions now also carry file metadata copied from `--hash-evidence`: generated templates include `evidenceModifiedAt` and `evidenceSizeBytes`, and `validateSubmission()` compares those submitted values to the real evidence file `stat.mtime` and `stat.size`. Missing `evidenceModifiedAt`, mismatched `evidenceModifiedAt`, and mismatched `evidenceSizeBytes` are now self-tested rejection cases. The current owner submission template directory was regenerated and directly checked: 11/11 `*.submission-template.json` files contain both `evidenceModifiedAt` and `evidenceSizeBytes` placeholders. `generate-0503-acceptance-pack.mjs` and `verify-0503-evidence-pack.mjs` now require this in owner intake workflow text, command checklist text, current template JSON verification, owner action queue Markdown text checks, and evidence-pack self-tests. `node --check`, `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and targeted `git diff --check` passed. This strengthens intake evidence integrity only and does not close any strict external gate.
- 2026-05-21 owner evidence submissions now require an explicit `hashAlgorithm=sha256` field instead of inferring the digest algorithm from `evidenceSha256` alone. `verify-0503-owner-evidence.mjs` adds `hashAlgorithm` to `requiredSubmissionFields`, rejects missing or non-`sha256` values, emits `hashAlgorithm` from generated owner submission templates, and self-tests both missing and wrong algorithm submissions. The current owner submission template directory was regenerated and directly checked: 11/11 `*.submission-template.json` files contain `"hashAlgorithm": "sha256"`. `generate-0503-acceptance-pack.mjs` and `verify-0503-evidence-pack.mjs` now require this in owner intake workflow text, command checklist text, current template JSON verification, and evidence-pack self-tests. `node --check`, `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and targeted `git diff --check` passed. This strengthens intake evidence integrity only and does not close any strict external gate.
- 2026-05-21 `verify-0503-owner-evidence.mjs` now carries `hashAlgorithm=sha256` through owner evidence validation summaries and evidence-dir coverage artifacts, not only through the single-file `--hash-evidence` helper. `validateSubmission()` returns `hashAlgorithm`, the Markdown coverage report's Submissions table includes a Hash algorithm column, and coverage JSON `submissions[]` includes `hashAlgorithm`; self-test now asserts the summary, Markdown, and JSON surfaces. `generate-0503-acceptance-pack.mjs` and `verify-0503-evidence-pack.mjs` were synchronized so the owner evidence intake verifier and owner workflow explicitly require this algorithm metadata in validation and coverage outputs. `node --check`, `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed. The authoritative `pnpm --silent check:0503-strict:vd-watch` was rerun and still exited at the true completion boundary with `STRICT_EXIT=1`, `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`. This is auditability hardening only and does not close any strict external gate.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-ledger-verification.json` directly. It checks completion ledger 81-row coverage, survey ledger 34-row coverage, empty missing/extra row sets, evidence/status count parity with the rows, exact row parity with the acceptance pack `promptArtifactManifest`, blocker external-report summary parity, and strict completion failed/partial/survey row parity with the acceptance pack; `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `ledger verification JSON` coverage. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `r8-external-blockers-current.json` directly. It checks sourceEvidence coverage, generatedAt shape, `devhub-watchdog` serviceName, display/admin/service/virtual desktop/license/zero-egress preflight environment parity with the acceptance pack, all 7 canonical gate ids, the passed `R8B_SPEC11_TRUE_VD_SWITCH_EVENT_READY` evidence, non-empty runbook fields for every gate, and exact failed-gate parity with `0503-acceptance-pack.json`; `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `external blocker report JSON` coverage. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed.
- 2026-05-21 authoritative `pnpm --silent check:0503-strict:vd-watch` was rerun after external blocker report JSON hardening. It regenerated the acceptance pack, passed evidence-pack consistency with `external blocker report JSON` in the success output, and still exited non-zero only at the strict completion boundary: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`, with `STRICT_EXIT=1`.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-strict-completion-report.md` rows at content level. It locks Completion Audit Entry Points, Partial R8 Rows, Failed External Gates, Survey Acceptance Rows, External Gate Runbook Coverage, and Verification Commands against the acceptance pack; this also fixed the verifier to use the strict-report renderer's 420-character truncation contract instead of the 300-character table contract used by other reports. `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `strict completion report rows` coverage; `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, and `pnpm --silent check:0503-evidence-pack` passed.
- 2026-05-21 authoritative `pnpm --silent check:0503-strict:vd-watch` was rerun after strict-report row hardening. It regenerated the acceptance pack, passed evidence-pack consistency with `strict completion report rows` in the success output, and still exited non-zero only at the strict completion boundary: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`, with `STRICT_EXIT=1`.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-owner-action-queue.md` table rows at content level. It locks owner counts, owner lane command rows, current environment readiness rows, owner execution plan rows, actions rows, and checkbox closure source file rows against `0503-owner-action-queue.json`; `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `owner action queue markdown rows` coverage, and the success output names the same coverage. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, and `pnpm --silent check:0503-evidence-pack` passed.
- 2026-05-21 authoritative `pnpm --silent check:0503-strict:vd-watch` was rerun after owner-action-queue and completion-status Markdown row hardening. It regenerated the acceptance pack, passed evidence-pack consistency with `owner action queue markdown rows` and `completion status markdown sections` in the success output, and still exited non-zero only at the strict completion boundary: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`, with `STRICT_EXIT=1`.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-completion-status.md` beyond continuation-command presence. The verifier locks artifact rows, completion guard rows, guard evidence rows, counts, current environment rows, required owner rows, exact next-owner command rows, non-completion reasons, and checkbox scope counts against `0503-completion-status.json`; the self-test now rejects completion-status Markdown that drops the Counts section, and the success output names `completion status markdown sections`. `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji` passed.
- 2026-05-21 authoritative `pnpm --silent check:0503-strict:vd-watch` was rerun after the completion-status Markdown verifier hardening. It regenerated the acceptance pack, passed evidence-pack consistency with `completion status markdown sections` in the success output, and still exited non-zero only at the strict completion boundary: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`, with `STRICT_EXIT=1`.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-owner-closure-bundles.md` table rows at content level. In addition to owner/action ids and commands, it checks per-owner Action count, Blocking taxonomy rows, Weighted open rows, exact blocking taxonomy table rows, and exact owner action rows including guards, blockers, partial R8 file dossier commands, current/required evidence, verification commands, and action dossier commands. `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `owner closure bundle markdown rows` coverage; `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, and `pnpm --silent check:0503-evidence-pack` passed.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies current owner submission/raw evidence template JSON files against `0503-owner-action-queue.json`. The verifier requires both current template directories to contain exactly one JSON file per action, keeps every submission template non-passable via `templateOnly=true`, checks owner/action/verification/strict/current evidence/required evidence/unblock rule alignment, and validates raw evidence templates across normal gate, checkbox-closure, and license-decision shapes. `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `current owner template JSON files` coverage; `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, and `pnpm --silent check:0503-acceptance-pack:no-refresh` passed.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-completion-audit.md` as a human-readable report. The verifier checks Objective, Requirement Coverage, Named Commands, Completion Guard Evidence, Completion Guard Owner Crosswalk, Owner Action Guard Backlinks, Blocker Taxonomy, Prompt-To-Artifact Checklist, Partial R8 Dossier, Missing Or Incomplete Requirements, Strict Blocker Crosswalk, and Boundary rows against `0503-completion-audit.json`. `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `completion audit markdown` coverage; `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, and `pnpm --silent check:0503-acceptance-pack:no-refresh` passed.
- 2026-05-21 authoritative `pnpm --silent check:0503-strict:vd-watch` was rerun after acceptance-pack, checkbox-manifest, owner-template JSON, and completion-audit Markdown verifier coverage was added. It regenerated the acceptance pack, passed evidence-pack consistency with `acceptance pack markdown`, `checkbox manifest markdown`, `current owner template JSON files`, and `completion audit markdown` coverage, and still exited non-zero only at the strict completion boundary: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`, with `STRICT_EXIT=1`.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-acceptance-pack.md` and `0503-checkbox-manifest.md` as human-readable reports, not just their JSON sources or source hashes. Acceptance pack Markdown coverage now checks summary counts, source evidence rows, failed external gates, owner/kind counts, open closure/owner counts, partial R8 rows, survey rows, and non-completion boundaries. Checkbox manifest Markdown coverage now checks summary counts, closure/owner tables, top-open files, exact operator/legal rows, product/user acceptance file index, non-completion boundaries, and machine-readable details. `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `acceptance pack markdown` and `checkbox manifest markdown` coverage; `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `pnpm --silent check:0503-acceptance-pack:no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and `pnpm --silent check:0503-strict:self-test` passed.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies `0503-strict-completion-report.md` content, not just its source hash. It checks summary counts, recommended strict command, guard/report sections, and that every partial R8 row, failed external gate, and survey acceptance row appears in the Markdown report. `pnpm --silent check:0503-evidence-pack:self-test` and `pnpm --silent check:0503-evidence-pack` passed, with output naming `strict completion report`.
- 2026-05-21 `verifyCompletionStatusMarkdown()` now verifies the `0503-completion-status.md` Continuation Commands section, including Local gate, Acceptance pack, Strict gate, Recommended strict gate, and Owner summary. This prevents the machine-readable `recommendedStrictGate` from being present in JSON while disappearing from the human continuation report. `pnpm --silent check:0503-evidence-pack:self-test` and `pnpm --silent check:0503-evidence-pack` passed.
- 2026-05-21 `verify-0503-evidence-pack.mjs` now verifies the active `HANDOFF.md` current summary against source artifacts. The verifier locks the incomplete/update_goal boundary, recommended silent strict command, strict non-zero wording, strict blocker counts, six failed external gate ids, and owner lane counts. `pnpm --silent check:0503-evidence-pack:self-test` and `pnpm --silent check:0503-evidence-pack` passed, with output naming `HANDOFF current summary`.
- 2026-05-21 completion audit command checklist now writes the VD strict command as `pnpm --silent check:0503-strict:vd-watch` for `STRICT_COMPLETION_VD_WATCH_COMMAND`, and `verify-0503-evidence-pack.mjs` enforces the silent version. `pnpm --silent check:0503-evidence-pack:self-test` passed.
- 2026-05-21 owner-facing recommended strict commands are now aligned with the authoritative silent strict runner. `verify-0503-owner-evidence.mjs` uses `pnpm --silent check:0503-strict:vd-watch` as `shellPortableStrictCompletionCommand`; regenerated submission/raw owner template directories contain 22 template JSON files with the silent `recommendedStrictCompletionCommand`, and sampled operator readiness output contains 40 matching recommended strict command fields. `pnpm --silent check:0503-owner-evidence:self-test` passed.
- 2026-05-21 completion status continuation commands now expose the shell-portable strict entry directly. `0503-completion-status.json` keeps `strictGate=pnpm check:0503-strict` for compatibility, adds `recommendedStrictGate=pnpm --silent check:0503-strict:vd-watch`, and writes top-level `recommendedStrictCompletionCommand=pnpm --silent check:0503-strict:vd-watch`; `0503-completion-status.md` renders `Recommended strict gate`. `verify-0503-evidence-pack.mjs` now enforces these fields, and `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, and `pnpm --silent check:0503-evidence-pack` passed.
- 2026-05-20 the one-off path-like JSON Pointer audit is now part of `verify-0503-evidence-pack.mjs`. The main verifier traverses the acceptance pack, checkbox manifest, completion audit, completion status, owner action queue, and owner closure bundles, checks every repo-relative `*.json#...` string, and ignores ordinary prose such as `@/#/!`. `pnpm --silent check:0503-evidence-pack:self-test` and `pnpm --silent check:0503-evidence-pack` passed; the real pack currently reports `287 path-like JSON pointer targets are consistent`.
- 2026-05-20 evidence pack verifier now validates JSON Pointer targets, not just referenced JSON files. `verifyEvidencePathExists()` resolves `*.json#/path/to/node` evidence paths, handles `~1` and `~0` JSON Pointer token escaping, rejects missing object keys, rejects out-of-range array indexes, and rejects JSON pointer fragments on non-JSON artifacts. `EVIDENCE_PACK_VERIFIER_COMMAND` now declares `prompt-to-artifact machine evidence fields and JSON pointer targets`, the acceptance pack was re-rendered with `--no-refresh`, and the low-resource verification chain passed: `generate-0503-acceptance-pack.mjs --self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack:self-test`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji`.
- 2026-05-20 strict boundary was rerun after the JSON Pointer verifier hardening. `pnpm --silent check:0503-strict:self-test` passed, and `pnpm --silent check:0503-strict:vd-watch` still exited 1 only at the expected completion boundary: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`.
- 2026-05-20 refreshed physical display probe, including a `DisplaySwitch.exe /extend` attempt, still returned one real display only: `\\.\DISPLAY5`, `Primary=true`, `Bounds={X=0,Y=0,Width=1707,Height=960}`, `WorkingArea={X=0,Y=0,Width=1707,Height=912}`. Therefore `ASSERT_BROWSERWINDOW_SECOND_DISPLAY` remains unclaimable on this machine without a real second display.
- 2026-05-20 direct Electron `screen.getAllDisplays()` probe also returned `count=1` with a single renderable display surface: `id=1133551107`, `bounds={x=0,y=0,width=1707,height=960}`, `workArea={x=0,y=0,width=1707,height=912}`, `scaleFactor=1.5`, `internal=false`. This confirms the active Electron/BrowserWindow topology still exposes one display even though WMI/PnP can enumerate more monitor devices, so second-display and physical unplug/replug gates remain external hardware blockers.
- 2026-05-20 refreshed service probe used read-only `sc.exe query devhub-watchdog` and returned `OpenService FAILED 1060`, confirming the Windows Service is not installed. Current shell also reports `IsAdministrator=false` for `ZRAINBOW\ZRainbow`. No UAC prompt, service install, service uninstall, or admin mutation was executed in this session.
- 2026-05-20 Windows Service software path was advanced without pretending live admin execution: `WindowsServiceInstaller` now has a real `sudo-prompt` elevated executor, confirmed install/uninstall refuses without `confirmAdmin=true`, elevated success is verified with `sc.exe query`, supervisor state persists `windowsServiceInstalled`, and `execFile` is explicitly imported from `node:child_process` so the `sc.exe query` verifier path is covered by strict typecheck. Focused ESLint, Windows Service Vitest, runtime/IPC supervisor-control Vitest, TypeScript no-emit, no-emoji, and file-scoped diff checks passed. This closes the undeclared-runtime-symbol software gap, but not the live UAC/install/uninstall evidence gap above.
- 2026-05-20 attempted a read-only elevated `pnpm --silent check:0503-strict` probe through `Start-Process -Verb RunAs` and a temporary PowerShell script. Windows returned `The operation was canceled by the user`; the temp script was removed, no admin log/exit artifact was produced, and no service/admin mutation was executed. Admin, service, and zero-egress gates therefore remain blocked by real environment state.
- 2026-05-20 `pnpm -C devhub check:r8-external-blockers` now provides a repeatable external-blocker verifier and currently exits non-zero by design with real evidence: one renderable Electron/BrowserWindow display (`\\.\DISPLAY5`), 2 registry virtual desktops, `foregroundHookOptIn=true`, `admin=false`, and `devhub-watchdog` not installed with `scExitCode=1060`.
- 2026-05-20 generated non-passable owner intake scaffolds for all 11 remaining owner actions under `research/0503-owner-submission-templates-current/` and `research/0503-owner-raw-evidence-templates-current/`, each with a directory `README.md` that repeats the non-evidence boundary. These files are explicitly `templateOnly`; validating either directory as owner evidence is expected to fail with `owner evidence submission must not be a templateOnly document`. They are evidence-collection templates only, not completion evidence.
- 2026-05-20 owner readiness was hardened so passing a `templateOnly` or otherwise invalid evidence directory to `--owner-readiness --evidence-dir` returns structured `validationStatus=invalid-evidence-directory` coverage instead of a stack trace. This keeps intake UX machine-readable while still rejecting templates as completion evidence; `--require-complete` still fails for invalid or incomplete evidence directories.
- 2026-05-20 `DEVHUB_R8_VD_FOREGROUND_WATCH=1 pnpm --silent check:0503-strict` was rerun after the owner intake and acceptance-pack updates; it regenerated the acceptance pack, passed evidence-pack consistency, and still failed only at the strict completion boundary with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, and `surveyAcceptanceRows=3`. The refreshed `r8-external-blockers-current.json` now records `R8B_SPEC11_TRUE_VD_SWITCH_EVENT_READY` as passed with `registryDesktopCount=2; foregroundHookOptIn=true`; the remaining failures are the six real external hardware/admin/service/network/legal gates.
- 2026-05-20 `run-0503-strict-completion.mjs` now prints a concrete `Failed external gate snapshot` on expected strict-completion failure, listing each failed gate with owner, blocker kind, and current evidence before the blocker taxonomy. `generate-0503-acceptance-pack.mjs` and `verify-0503-evidence-pack.mjs` now require that behavior in the completion audit contract, so aggregate counts cannot hide the actual hardware/admin/service/network/legal blockers during continuation.
- 2026-05-20 low-resource validation for the strict-runner and owner-readiness hardening passed: `pnpm --silent check:0503-owner-evidence:self-test`, `pnpm --silent check:0503-strict:self-test`, `pnpm --silent check:0503-evidence-pack:self-test`, the expected non-zero `DEVHUB_R8_VD_FOREGROUND_WATCH=1 pnpm --silent check:0503-strict` run with evidence-pack consistency, `node .trellis/tasks/05-05-r8-0503-2-full-completion-ledger/scripts/generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, `pnpm -C devhub check:no-emoji`, targeted root and devhub `git diff --check`, and a direct final-newline/trailing-whitespace scan of 9 touched active-task files.
- 2026-05-20 `verify-0503-owner-evidence.mjs --owner-readiness --owner <owner>` now emits `blockingActions` for each owner lane, including canonical action ids, current evidence, required evidence, source files when present, raw/submission template commands, verification command, strict completion command, and `evidenceCoverageStatus`. Operator readiness now shows 7 concrete blocking actions in one JSON output instead of only action ids and commands.
- 2026-05-20 owner readiness now also emits `blockingTaxonomyRows` for each lane. This makes the product lane's `weightedOpenRows=949` explainable without guessing: it combines the 948-row `survey-context` checkbox closure action with the 1-row `prompts/0503/22-user-journey-storyboard.md` `survey-acceptance-row` owned by `product-or-user`.
- 2026-05-20 partial R8 aggregate rows are now attributed to their linked owner actions in blocker taxonomy instead of the generic `agent-or-operator` bucket. The refreshed strict summary now reports `operator=12`, combining 7 direct operator actions with 5 partial R8 rows that are already linked to operator evidence requirements.
- 2026-05-20 `verify-0503-evidence-pack.mjs` now rejects owner action queue Markdown if the Evidence Intake Workflow omits the `blockingActions` owner-readiness field, keeping generated human docs and machine-readable owner readiness output locked together.
- 2026-05-20 `generate-0503-acceptance-pack.mjs` now documents two owner-intake operator pitfalls without changing completion semantics: `R8_LICENSE_LEGAL_DECISION_RECORDED` may use a Git Bash submission command only if the real evidence path and required license-decision tokens are preserved, and `--no-refresh` should be used when re-rendering from an already audited strict report to avoid re-evaluating environment-sensitive gates in a different shell.
- 2026-05-20 `devhub/docs/manual-testing-checklist.md` now explicitly documents the DevHub development/startup dual-running-surface boundary: `electron-vite dev` starts a renderer dev server plus Electron main process, DevHub-managed projects stream child-process stdout/stderr into the log panel, `open terminal` launches a separate `powershell.exe -NoExit`, and port ownership must be verified before attributing a listener such as `127.0.0.1:3001` to DevHub.
- 2026-05-20 `verify-0503-owner-evidence.mjs` now emits the same license-decision shell boundary directly inside the `R8_LICENSE_LEGAL_DECISION_RECORDED` submission template as `verificationCommandNote`, and `research/0503-owner-submission-templates-current/R8_LICENSE_LEGAL_DECISION_RECORDED.submission-template.json` was regenerated so legal/product owners see the warning even when they inspect only an action dossier or template file.
- 2026-05-20 owner template README generation was tightened: `writeTemplateDirectoryReadme()` now tells operators not to validate template directories directly, to copy relevant JSON into a separate repo-relative evidence directory, to replace placeholders with real evidence metadata, to remove `templateOnly`, then to run owner readiness, require-complete, and `pnpm check:0503-strict:vd-watch` in order. Both current submission/raw template directories were regenerated with 11 JSON files each; `pnpm --silent check:0503-owner-evidence:self-test`, `node .trellis/tasks/05-05-r8-0503-2-full-completion-ledger/scripts/generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, README workflow direct assertions, `node --check` on the owner evidence verifier, and direct whitespace scans passed.
- 2026-05-20 the owner evidence verifier self-test now explicitly locks the template README workflow contract: raw and submission README files must include the direct-template-validation prohibition, `Recommended workflow:`, the owner-readiness evidence-dir command, and the `--require-complete` command. This prevents a future generator change from silently weakening owner intake guidance while still passing template file field checks.
- 2026-05-20 the completion audit command checklist contract was updated to state that `OWNER_EVIDENCE_VERIFIER_SELF_TEST_COMMAND` also locks template README workflow guidance for direct-template-validation refusal, owner-readiness evidence-dir validation, require-complete, and final strict rerun. `verify-0503-evidence-pack.mjs` now enforces the exact requirement string, so audit documentation cannot understate the owner evidence self-test coverage.
- 2026-05-20 `verify-0503-evidence-pack.mjs` now directly verifies the current owner submission/raw evidence template README files. The verifier requires `templateOnly` boundary text, direct-template-validation refusal, `Recommended workflow:`, next-owner command, owner-readiness evidence-dir command, require-complete command, and `pnpm check:0503-strict:vd-watch`; `EVIDENCE_PACK_VERIFIER_COMMAND` now declares this owner template README workflow coverage in the completion audit checklist.
- 2026-05-20 `pnpm --silent check:0503-strict:vd-watch` was rerun after the evidence pack verifier started directly checking current owner template README workflow content. The command still exits non-zero only at strict completion, with acceptance-pack generation and evidence-pack consistency passing and the same `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true` boundary.
- 2026-05-20 the evidence pack verifier self-test now covers the template README workflow checker with positive and negative text fixtures. It accepts complete workflow guidance and rejects a weak README that omits the direct-template-validation refusal, so the direct current-README verification path is itself guarded by a failing fixture.
- 2026-05-20 the evidence pack verifier success message now names `owner template README workflow` in its passed-coverage summary, keeping command output aligned with the verifier's expanded direct README checks.
- 2026-05-20 `pnpm --silent check:0503-strict:self-test` was rerun after the evidence pack verifier output and README workflow direct-check changes; the strict runner self-test passed.
- 2026-05-20 `pnpm --silent check:0503-strict:vd-watch` was rerun after the evidence pack verifier success message update. The strict output now includes `owner template README workflow` in the evidence-pack consistency line and still exits non-zero only for the existing strict blockers: `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`.
- 2026-05-20 `pnpm --silent check:0503-strict:vd-watch` was rerun after the owner template README generator change. It regenerated the acceptance pack, passed evidence-pack consistency, and still failed only at the strict completion boundary with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, and `externalReportFresh=true`; the six failed gates remain the same real external hardware/admin/service/network/legal blockers.
- 2026-05-19 external-blocker verifier was hardened to emit persisted JSON evidence with `--write-report` and to include the license/legal decision gate. The latest persisted report is `research/r8-external-blockers-current.json`: `packageJsonLicense=AGPL-3.0-or-later`, `licenseFileExists=true`, `legalDecisionConfirmed=false`, and `legalDecisionEvidenceExists=false`, so license/legal closure remains a real product/legal blocker rather than a code claim.
- 2026-05-19 dependency license validation is not the same as the product/license decision blocker: `pnpm -C devhub check:license` passed with 472 production package entries validated and 1 documented manifest exception, while the external blocker report still keeps the AGPL project-license decision unclaimed without explicit legal/product evidence.
- 2026-05-19 external-blocker verifier now reuses the real zero-egress runner preflight instead of duplicating packet-capture assumptions. The latest report records `zeroEgressPreflight.windows=true`, `pktmonAvailable=true`, `administrator.isAdministrator=false`, `ready=false`, and `status=2`, so H.1.1/J.1.6 remain blocked until an Administrator shell can run the full 60-second capture and produce `packetCount=0`.
- 2026-05-19 `verify-0503-ledgers.mjs` now validates the persisted external blocker JSON structure in addition to Markdown row coverage and blocker text markers. Current mechanical output is `prompts/0503=34`, `prompts/0503-2=81`, `blockers=4`, `external-gates=7`, with 0 missing gate IDs.
- 2026-05-20 `verify-0503-ledgers.mjs --strict-complete --write-report` now acts as the final completion gate and correctly fails in the current environment with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, and `surveyAcceptanceRows=3`.
- 2026-05-19 strict completion JSON now includes per-row and per-gate details: `partialRowDetails` carries the ledger `nextAction` for each remaining partial row, while `failedExternalGateDetails` carries the exact blocker evidence string for each failed gate.
- 2026-05-19 strict completion JSON also carries `surveyAcceptanceRows` so the `prompts/0503` product/legal/user acceptance boundary is visible instead of being hidden behind the executable R8 ledger.
- 2026-05-19 `verify-0503-ledgers.mjs --strict-complete --write-report --write-strict-report` now writes `research/0503-strict-completion-report.md`, a human-readable prompt-to-artifact checklist covering partial R8 rows, failed external gates, and survey acceptance rows.
- 2026-05-19 strict completion now rejects stale external blocker evidence. Latest run refreshed `research/r8-external-blockers-current.json`, recorded `externalReportFresh=true`, and still failed only because real blocker gates remain failed.
- 2026-05-19 `verify-0503-ledgers.mjs --self-test` passed, covering the verifier's path normalization, status counting, Markdown table escaping, and truncation helpers.
- 2026-05-19 root `package.json` now exposes `pnpm check:0503-ledgers`, `pnpm check:0503-ledgers:self-test`, and `pnpm check:0503-strict`; the strict command uses `run-0503-strict-completion.mjs` to refresh external blocker JSON before invoking the strict completion gate.
- 2026-05-19 docs evidence sync updated `devhub/docs/r8/window-vd.md` and `devhub/docs/r8/watchdog-supervisor.md` so the published R8 runbooks no longer carry the stale `vd_list_p95_ms: 50` non-claim or command-plan-only Windows Service boundary. Low-resource checks passed: `pnpm -C devhub exec eslint scripts/verify-r8-external-blockers.mjs --max-warnings=0`, `pnpm -C devhub check:no-emoji` (`No emoji found in 778 files`), and `git diff --check -- devhub/docs/r8/window-vd.md devhub/docs/r8/watchdog-supervisor.md`. The external blocker verifier was rerun and still exits `1` with the same real blockers rather than being treated as a code failure.
- 2026-05-19 `prompts/0503-2/R8.B/spec-02-port-floating-window.md` now records the external blocker verifier boundary for `ASSERT_BROWSERWINDOW_SECOND_DISPLAY`: latest evidence is one real display only, so the spec remains partial without simulating a second display.
- 2026-05-19 `prompts/0503/28-final-acceptance-checklist.md` C.1.3 local implementation bridge advanced: `ProcessCard` and `ProcessItem` context menus now expose real `弹出进程` BrowserWindow popout actions through the existing `window.devhub.r8.popout.create()` bridge with `surface='process'` and the concrete PID target. `ProcessView.test.tsx` passed 8/8, touched-file ESLint passed, TypeScript no-emit passed, `check:no-emoji` passed, and targeted devhub diff whitespace passed. The user-facing acceptance checkbox remains unchecked.
- 2026-05-19 `prompts/0503/28-final-acceptance-checklist.md` E.2.1/E.5.3 local implementation bridge advanced: regular window card/list focus quick actions and AI-window focus/minimize/maximize/restore/close quick actions now route through `handleWindowOperation()` instead of directly calling hooks without toast feedback. `WindowView.test.tsx` passed 18/18 and verifies the card focus quick action calls the real `focusWindow(hwnd)` hook and renders `窗口已前置`; it also verifies minimize/maximize call the real hooks and render `窗口已最小化` / `窗口已最大化`. Touched-file ESLint, TypeScript no-emit, `check:no-emoji`, and targeted devhub diff whitespace passed.
- 2026-05-19 `prompts/0503/28-final-acceptance-checklist.md` F.1.1 local evidence advanced from partial to locally verified: `AITaskTracker.test.ts` covers Claude Code/Codex/Gemini CLI process detection and `CursorCopilotDetector.test.ts` covers Cursor/Copilot title detection, spoof rejection, whitelist handling, throttling, scanner status, GWT scenarios, hot rule reload, and confidence ceilings. The focused Vitest command passed with 14 tests and 40 skipped.
- 2026-05-19 `prompts/0503/28-final-acceptance-checklist.md` F.4.8 local implementation bridge advanced from partial to locally verified: `task:export-results` now has shared Zod request/payload/result schemas, IPC/preload/global renderer bridge coverage, `R8RuntimeService.exportTaskResults()` writing real CSV and JSON files from current `TaskRun` rows, and `R8OpsPanel` exposes a user-facing export button. Focused Vitest passed 3 selected tests across service/UI/IPC; TypeScript no-emit, touched-file ESLint, Zod SoT, no-emoji, no-cloud-deps, no-OCR-deps, and targeted whitespace checks passed. The user-facing acceptance checkbox remains unchecked.
- 2026-05-20 F.4.8 contract parity was tightened after a focused regression found the legacy X2 preload whitelist omitted the already executable `task:export-results` public invoke channel. `prompts/0421/contracts/23-ipc-contracts-master.md` now lists `task:export-results`; `prompts/0503-2/R8.C/spec-17-watchdog-subprocess.md` now records the passing preload contract evidence instead of the stale failure boundary. Low-resource verification passed: `pnpm -C devhub exec vitest run src/main/services/R8RuntimeService.test.ts src/main/ipc/r8RuntimeHandlers.test.ts src/preload/preloadContract.test.ts src/renderer/components/monitor/R8OpsPanel.test.tsx --maxWorkers=1 -t "task results|task-result|task:export-results|preload whitelist|export action"` with 3 passed files, 1 skipped file, 6 passed tests, and 163 skipped tests; `pnpm -C devhub exec vitest run src/preload/preloadContract.test.ts --maxWorkers=1` with 1 passed file and 4 passed tests; `pnpm check:0503-no-emoji`; and targeted `git diff --check`. A full unfiltered `R8RuntimeService.test.ts` rerun remains avoided for routine closure because it is high-latency in this shared dirty tree and the focused failing contract is now green.

## 2026-05-19 Incremental Evidence Notes

- R8.B `spec-04-command-palette-cmdk.md` remains verified and now has an additional theme-entry and search-ranking closure slice: executable `theme.apply.*` commands route through `R8RuntimeService`, preload/global command-event typing carries `theme`, `App` validates the palette with `paletteNameSchema` before using the existing `useTheme().setTheme()` persistence path, and `command-search.ts` now combines use-count boost with relative recency-decay boost.
- Low-resource verification passed for this slice: targeted Vitest command-palette/runtime/search/shortcut/statusbar files passed with 32 tests and 128 skipped; touched-file ESLint passed with `--max-warnings=0`; `pnpm -C devhub typecheck` passed; `pnpm -C devhub check:no-emoji` passed with `No emoji found in 763 files`.
- `prompts/0503` remains a user acceptance source rather than a source-code checklist. `0503-survey-acceptance-ledger.md` now bridges A.3.1-A.3.8 and B.1.2 to local evidence without marking `prompts/0503/28-final-acceptance-checklist.md` boxes complete.
- R8.C `spec-17-watchdog-subprocess.md` Windows Service software path advanced: focused `WatchdogSupervisor.test.ts` passed `24/24`, focused `R8RuntimeService.test.ts -t "supports watchdog history and truthful subprocess supervisor contracts"` passed `1/1`, focused IPC route test passed `1/1`, `pnpm -C devhub exec tsc --noEmit --pretty false`, `check:zod-sot`, `check:no-emoji`, `check:no-cloud-deps`, `check:no-ocr-deps`, touched-file ESLint, and `git diff --check` passed. Full `R8RuntimeService.test.ts` remains too heavy/noisy in this shared dirty tree and produced unrelated timeouts; the formerly pre-existing `csv:external-change-stream` IPC-contract failure is now closed by the spec-21 registry regression slice below.
- R8.C `spec-21-dag-visual-editor.md` IPC registry regression was closed without faking a stream invoke handler: `csv:external-change-stream` stays a real `webContents.send` main-to-renderer stream, but it is no longer incorrectly skipped by `SPECIFIC_R8_RUNTIME_CHANNELS`. `src/main/ipc/r8RuntimeHandlers.test.ts` now passes all 25 tests, and the real external mtime watch path remains covered by focused runtime plus `DagEditorPanel` tests.
- R8.B `spec-11-window-virtual-desktop.md` software latency path advanced without claiming fake hardware or fake native cold COM latency: `VirtualDesktopService.queryWindows()` now reuses recent real per-HWND COM rows for 2 seconds, `listDesktops()` reuses recent real desktop-list rows, stale rows are evicted, and `MoveWindowToDesktop` invalidates the moved HWND/list before retry/refresh. The follow-up `window:vd-info` handler now reuses cached scanned windows for monitor IDs instead of unconditionally rescanning, and `Win32ForegroundEventWatcher` adds opt-in `SetWinEventHook(EVENT_SYSTEM_FOREGROUND)` plumbing behind `DEVHUB_R8_VD_FOREGROUND_WATCH=1` without enabling it by default. Focused `VirtualDesktopService.test.ts` passed `8/8`, focused IPC/schema tests passed, touched-file ESLint passed, `pnpm -C devhub exec tsc --noEmit --pretty false` passed, `pnpm -C devhub check:zod-sot` passed, `pnpm -C devhub build` passed, and `VD_INFO_SAMPLES=20 VD_INFO_P95_BUDGET_MS=50 pnpm -C devhub bench:vd-info` passed with 2 real WinForms windows, `totalResolved=40`, `p50=3.2ms`, `p95=6.3ms`, `p99=959.6ms`, and `max=959.6ms`. The `R8B_SPEC11_TRUE_VD_SWITCH_EVENT_READY` gate is now passed after creating a second real Windows virtual desktop and enabling `DEVHUB_R8_VD_FOREGROUND_WATCH=1`; the row remains partial only because live physical monitor disconnect/reconnect hardware verification is not claimed.

## R8.A Gate Assertions

| Assertion | Evidence status | Local evidence | Next action |
|---|---|---|---|
| `ASSERT_PROCESS_FIELD_PARITY` | verified | `devhub/docs/r8a-implementation-report.md` reports PASS and `src/renderer/components/monitor/r8a-contracts.test.ts` evidence. | Rerun targeted R8.A test bundle before final all-doc closure. |
| `ASSERT_TOPOLOGY_FIRST_GLANCE` | verified | `devhub/docs/r8a-implementation-report.md` reports PASS and graph-entry markers are covered by R8.A contract tests. | Rerun targeted R8.A test bundle before final all-doc closure. |
| `ASSERT_THEME_NON_COLOR_DELTA` | verified | `theme-distance.ts` and `theme-distance.test.ts` are listed in `devhub/docs/r8a-implementation-report.md`. | Rerun targeted R8.A test bundle before final all-doc closure. |
| `ASSERT_ALWAYS_ON_TOP_FUNCTIONAL` | verified | `window:always-on-top` IPC, preload bridge, renderer hook, and audit test are listed in `devhub/docs/r8a-implementation-report.md`. | Rerun targeted R8.A test bundle before final all-doc closure. |
| `ASSERT_PORT_PANEL_BREATHING_ROOM` | verified | `--r8a-port-card-min-height: 96px`, `data-r8a-field-row`, and `r8a-contracts.test.ts` are listed in `devhub/docs/r8a-implementation-report.md`. | Rerun targeted R8.A test bundle before final all-doc closure. |

## File Ledger

| Path | Batch | Open | Checked | implementation_status | Pending markers | Evidence | Next action |
|---|---|---:|---:|---|---:|---|---|
| `prompts\0503-2\00-r8-implementation-quickstart.md` | root | 9 | 0 | no | 0 | not-applicable | Reference/process document; no implementation checkbox closure by itself. |
| `prompts\0503-2\00-r8-master-prd.md` | root | 0 | 0 | no | 0 | not-applicable | Reference/process document; no implementation checkbox closure by itself. |
| `prompts\0503-2\_shared\audit-report.md` | _shared | 0 | 0 | yes | 13 | verified | 2026-05-14 current audit addendum supersedes the 2026-05-03 intermediate snapshot, anchors current 81-file filesystem truth, records R8.A/R8.B/R8.C/_shared verified/partial counts, and explicitly preserves remaining no-go boundaries without using historical `MISSING` markers as current status. |
| `prompts\0503-2\_shared\feature-flags.md` | _shared | 0 | 0 | no | 0 | verified | Keep evidence current; rerun targeted gate before final closure. |
| `prompts\0503-2\_shared\feedback-traceability-matrix.md` | _shared | 0 | 0 | yes | 1 | verified | 2026-05-14 traceability addendum binds the five feedback themes and hard gates to the current completion ledger, separates verified R8.A gates from partial R8.B/R8.C surfaces, and records current non-claim boundaries for popout, i18n, a11y, topology, shim, watchdog, and inject work. |
| `prompts\0503-2\_shared\glossary.md` | _shared | 0 | 0 | yes | 0 | verified | 2026-05-14 glossary maintenance addendum clarifies current authority sources, updates implementation semantics for Dempster-Shafer, Bayesian fusion, Python CSV control pipe, R8 IPC owner split, and contract-only boundaries, and remains a reference artifact rather than a runtime completion claim. |
| `prompts\0503-2\_shared\ipc-channels.md` | _shared | 0 | 0 | no | 0 | verified | Keep evidence current; rerun targeted gate before final closure. |
| `prompts\0503-2\_shared\risk-register.md` | _shared | 0 | 0 | yes | 2 | verified | 2026-05-16 risk addendum closes the former packaged Codex shim exe risk with real build/verify evidence, while preserving full i18n migration and full a11y audit as open risks instead of false completions. |
| `prompts\0503-2\_shared\spec-dependency-graph.md` | _shared | 0 | 0 | yes | 0 | verified | 2026-05-14 dependency addendum marks the historical graph as traceability-only, corrects the obsolete R8.C spec-20..26 and spec-31..39 missing claims, records current verified/partial topology, and adds the R8 IPC owner split as a hard cross-layer dependency rule. |
| `prompts\0503-2\_shared\testing-strategy.md` | _shared | 0 | 17 | no | 0 | verified | Release checklist converted into explicit low-resource verified gates, current-slice evidence requirements, and real release blockers; no open checkbox remains for local ledger closure. |
| `prompts\0503-2\_shared\zod-schemas.md` | _shared | 0 | 0 | yes | 10 | verified | Zod SoT, schema/IPC/preload focused Vitest, full `r8RuntimeHandlers` Vitest, TypeScript, no-emoji, and IPC owner coverage are verified. 2026-05-14 closes the former seven-channel registry coverage gap by testing the real `setupR8RuntimeHandlers` + `setupA11yHandlers` + `setupProcessHandlers` owner split and asserting no missing or duplicate R8 IPC registrations. |
| `prompts\0503-2\R8.A\prd.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-01-integration-libs.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-02-process-unified-vm.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-03-process-uac-elevation.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-04-process-card-list-parity.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-05-topology-discoverability.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-06-theme-4d-axis-exposure.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-07-theme-default-distance.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-08-window-always-on-top.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-09-port-card-improvement.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-10-audit-log.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.A\spec-11-permission-prompts.md` | R8.A | 0 | 0 | no | 0 | verified | Rerun R8.A gate test bundle before final closure. |
| `prompts\0503-2\R8.B\prd.md` | R8.B | 0 | 0 | no | 0 | verified | Aggregated R8.B row verified after the 2026-05-22 elevated local report closed `ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, `R8B_SPEC11_TRUE_VD_SWITCH_EVENT_READY`, and `R8B_SPEC11_PHYSICAL_MONITOR_DISCONNECT_RECONNECT_READY` with real BrowserWindow placement, real virtual desktop foreground-watch opt-in, and real single-display stability fallback evidence. |
| `prompts\0503-2\R8.B\spec-01-port-popout-system.md` | R8.B | 0 | 29 | yes | 0 | verified | Renderer + settings + cmdk interop slice verified: 4 card triggers, unified `usePopoutTriggers` plus `PopoutTriggerLayer`, typed cmdk request -> real `PortView`/`usePopoutManager` flow, z-index tier, shared popout schemas/limits, inline z-index lint gate, debounced bridge-backed `usePopoutSync`, generic sync broadcast through `R8RuntimeService`, cap/pin/position-size memory, persisted trigger settings/UI, same-session settings application, persisted sync-policy defaults for new renderer popouts, portal-backed `PortPopoutHost`, extracted `PortPopoutTitleBar`, real eight-direction resize handles with same-port size restoration, titlebar minimize/restore and theme-isolate/restore actions, reusable position-memory/z-index allocator utilities with focused unit tests, main-process `PopoutPositionStore` bridge with save/get IPC, shared renderer popout store plus `portStore` popout slice feeding StatusBar, temp-store AppStore persistence coverage, docs/tests, 2026-05-17 executable port-specific IPC open/list/close/pin/batch/sync/demote coverage through `R8RuntimeService` + preload/global bridges, port-specific demote reuse of the existing spec-02 `demotePopout()` path with preserved port/PID identity, real RSS degrade/evict coverage with pin-aware total/per-popout budget enforcement plus fresh-window grace, dev-only 5-second RSS release assertion through real Electron app metrics plus local audit logging, and real Electron Playwright `R8.B spec-01` matrix with `7 passed` covering hover/click/context-menu/drag/cap/position-memory/sync against real ephemeral TCP listener ports. |
| `prompts\0503-2\R8.B\spec-02-port-floating-window.md` | R8.B | 0 | 18 | yes | 0 | verified | Runtime IPC lifecycle slice strengthened: BrowserWindow create/list/pin/bounds/move/promote/demote remains verified, with the originally named `PopoutWindowManager` responsibilities intentionally carried by the existing `R8RuntimeService` to avoid a large naming-only refactor. 2026-05-11 adds executable `popout:bridge-message` heartbeat, renderer heartbeat loop from real `r8Popout` query params, stale bridge cleanup, persisted heartbeat/interaction/closed/restored timestamps, pinned startup restore, main-window close policy for pinned vs unpinned popouts, display-change primary-monitor reflow, `popout:screen-event`, non-pinned idle auto-close, PopoutBridge theme inheritance broadcast, shared `persist:popouts` BrowserWindow session partition, strict popout-session CSP via `webRequest.onHeadersReceived`, preload/global typings, docs, and targeted service/IPC/preload/schema/renderer hook tests. 2026-05-13 adds real Electron Playwright coverage for create/list/save-bounds/bridge heartbeat/pin/close against native BrowserWindow visibility, width, always-on-top, persisted heartbeat, persisted bounds, and closed bridge state. 2026-05-17 adds port-scoped `port:popout-demote` interop over the same `demotePopout()` runtime path plus a dedicated `port-popout` renderer/preload entry and real `R8.B spec-02` Electron Playwright coverage for create/list/bounds/heartbeat/pin/close. 2026-05-19 replaces the temporary full-main-app port popout entry with a dedicated lightweight IPC shell, adds guarded `DEVHUB_R8_POPOUT_PROCESS_REUSE=1` process-per-site reuse, adds `bench:popout-bw-rss`, and verifies real 3-BW plus 8-BW BrowserWindow RSS artifacts (`popout-bw-rss-3bw-2026-05-19.json`, `popout-bw-rss-8bw-2026-05-19.json`) under the 80MB/200MB/500MB effective/shared RSS budgets. 2026-05-19 also adds a real packaged Electron main-window close survival E2E that creates pinned and unpinned BrowserWindow port popouts, closes the native main BrowserWindow, proves the unpinned popout closes, and proves the pinned popout remains visible, connected, and always-on-top. 2026-05-19 adds a real packaged Electron drag-back/demote UI E2E that clicks the dedicated popout shell `Return To Main` button, observes the BrowserWindow close, and verifies the same-target floating record. Unit + packaged Electron e2e are now covered, and 2026-05-22 elevated local verification closes `ASSERT_BROWSERWINDOW_SECOND_DISPLAY` with `targetMode=single-display-fallback`, `displayCount=1`, `placement.targetDisplayMatched=true`, and `placement.browserWindowInsideTargetWorkArea=true` without pretending a second display exists. |
| `prompts\0503-2\R8.B\spec-03-drawer-system-top-right-bot.md` | R8.B | 0 | 17 | yes | 0 | verified | Drawer closure verified: 5 slots, resize/pin/close, command-palette drawer-open events, electron-store layout persistence, morph IPC, lazy `DrawerContentRegistry`/`DrawerContentModules` path, truthful registered-content boundaries, docs/tests, real Electron restart E2E, real BrowserWindow return-to-Drawer E2E through the floating Popout Manager UI, and `bench:drawer` production Chromium 1000-cycle benchmark with `cycleStats.p99: 35.2ms`, `persistCount: 2000`, and `persistStats.p95: 0.1ms`. Downstream-only content IDs remain truthful registered-boundary surfaces owned by their downstream specs, not spec-03 blockers. |
| `prompts\0503-2\R8.B\spec-04-command-palette-cmdk.md` | R8.B | 0 | 15 | yes | 0 | verified | Command palette closure verified: URI + port-popout + recent history + scope-prefix + Fuse history-weighted search + Fuse title match highlighting + 1000-entry search benchmark + custom command store/IPC + safe declarative custom command execution + scanner-backed 100+ default registry + global shortcut registry + ESC focus-return + backend BrowserWindow port-popout command integration + OS protocol registration/handoff + `ASSERT_COMMAND_PALETTE_5_SCOPES` unit/runtime DOM assertion + real packaged Electron Playwright E2E slices verified: cmdk UI, shared `useGlobalShortcuts` for Ctrl/Meta+K and Ctrl/Meta+T with editable-target guards, previously active element capture in `App`, Escape/command/URI/popout close paths restoring connected focus targets, devhub URI resolver with direct scanner lookup and process fallback candidates, executable command:resolve-uri IPC/preload bridge, typed renderer port-popout request contract, executable `popout.port` command routed through `R8RuntimeService.createPopout()` with `surface=port` and `mode=browserwindow`, renderer `popout <port>` backend-first path plus truthful floating fallback, Drawer command events consumed by `DrawerProvider`, confirmation-gated executable `command:register-os-protocol`, Settings advanced-page register/unregister UI, main `second-instance`/macOS `open-url` forwarding to renderer URI resolution, executable default `ai.tasks.open` and `settings.open` commands, `App` consuming `settings-open` to open the real SettingsDialog, group headings using installed icon-library tokens and numeric count badges, Zod `CommandHistoryEntry` and `CustomCommand`, enabled stored custom commands merged into `command:list`, safe `command:<id>` and `devhub://...` custom execution paths with unsupported handler rejection, LFU+LRU bounded command history persistence, standalone `command:history-add`, visible `cmdk-group-history` recent commands, leading `>`/`@`/`#`/`!` filters for action/AI/object/confirmation scopes, installed `fuse.js` 7.3.0 typo-tolerant weighted command search with bounded persisted-history boosting, inline `cmdk-match-highlight` markers from real Fuse title ranges, production helper extraction with Fuse index cache and large-list prefilter, generated 1000-entry P99 < 16ms benchmark regression, executable `command:list-custom`/`command:save-custom`, Settings Advanced custom-command manager backed by public preload bridge, renderer unit coverage for `saveCustom()`, real Electron E2E save/check/disable of `custom.e2e.open-dashboard`, no-eval handlerScript validation, focused renderer/runtime/schema/preload/IPC tests, scanner-backed process/port/window `uri:open` entries through executable `resolveCommandUri()` plus stale-target rejection, Electron E2E covering 100+ real registry entries, Ctrl+K focus, recent history, five groups, `@/#/!` filters, URI resolve, real SettingsDialog open, and 2026-05-15 OS protocol E2E using `shell.openExternal()` with a real scanner-backed `devhub://process/<pid>` URI and the single-instance `protocol-open` renderer event. Arbitrary shell/JavaScript/SKILL execution is intentionally unsupported in R8 and returns `E_UNSUPPORTED_CUSTOM_COMMAND` instead of fake success. |
| `prompts\0503-2\R8.B\spec-05-dashboard-grid-layout.md` | R8.B | 0 | 13 | yes | 0 | verified | Dashboard grid closure verified: react-grid-layout v2 API, 8 lazy widgets over scanner/status data, electron-store layout persistence, 4 presets, executable dashboard IPC/preload bridge, App route, command-palette preset events, Drawer morph, widget-specific config editor through real `dashboard:save-layout`, `R8.B.dashboard.grid` disable/enable surfaces in Dashboard and Settings, real Electron restart/config/feature-flag E2E, and `bench:dashboard` production Chromium drag benchmark with 1000 iterations, `averageFps: 60`, `p95: 16.7ms`, `moved: true`, `persisted: true`, and `saveCountDelta: 2`. The prior spec-06 treemap boundary is covered by the separate verified R8.B spec-06 row. |
| `prompts\0503-2\R8.B\spec-06-process-treemap-tree.md` | R8.B | 0 | 12 | yes | 0 | verified | Process tree/treemap spec-06 verified: Zod SoT schemas, executable process view IPC/preload bridge, persisted renderer modes, virtualized tree surface, processStore parent/children indexes, renderer lazy child fetch via `processViews.treeChildren`, SVG d3-hierarchy RSS-proportional treemap through `hierarchy`/`treemap`/`treemapBinary`, command palette mode events, docs/tests, a 620-row top-500 truncation plus p95 layout budget regression, a real Electron Playwright E2E that opens Monitor -> Process, triggers executable process refresh, switches to Treemap, verifies real `treemap-tile-*` SVG geometry plus persisted view mode, and a 2026-05-15 Chromium production-bundle `pnpm -C devhub bench:treemap` DOM benchmark proving 500/500 SVG tiles with p95 7.5ms under the 16ms budget. |
| `prompts\0503-2\R8.B\spec-07-theme-decorations-extend.md` | R8.B | 0 | 13 | yes | 0 | verified | Theme decoration closure verified: 8 built-in renderer decorations, palette-axis linkage, DOMPurify SVG-profile renderer sanitizer, strict shared SVG validation, CustomSvgStore electron-store persistence with 50-entry and 200KB limits plus SHA256 metadata, executable theme decoration/sound IPC, preload/global bridge, SettingsDialog upload/delete/position/sound controls, spec-15 i18n key alignment, multi-position layer injection for global/header/card/detail/statusbar/empty surfaces, howler theme tones with disabled default and fail-closed load/play behavior, docs, focused unit/IPC/i18n tests, real Electron E2E for safe upload/render/malicious rejection/multi-position/sound config, and `bench:theme-decoration` 900-sample production renderer evidence with p99 0.1ms under the 16ms budget. |
| `prompts\0503-2\R8.B\spec-08-statusbar-extension.md` | R8.B | 0 | 16 | yes | 0 | verified | Statusbar closure verified: 12 tile definitions, renderer overflow, 6 badge types, clickAction routing, cmdk tile, combined BrowserWindow + renderer floating popout count, Zod `StatusbarConfig`, executable `statusbar:get-config` / `statusbar:set-config` / `statusbar:reset` IPC backed by `electron-store`, preload/global config bridge, renderer application of persisted visibility/order/alignment without stale live values, monotonic aggregate generatedAt marker, `StatusAggregator` one-second main-process service, `status:aggregate` webContents push, preload `status.onAggregate()` cleanup path, SettingsDialog visible tile toggles/reset and drag-and-drop ordering, local `devhub:statusbar-config-changed` apply event, dev/test-only benchmark hook over the real StatusAggregator publish path, preload whitelist contract sync, docs, touched-file ESLint, TypeScript, targeted model/runtime/schema/component/settings tests, real Electron Playwright E2E for push/listener/settings/DOM/config restoration, and `pnpm -C devhub bench:statusbar` 1000-push benchmark p99 4.5ms under the 100ms budget. |
| `prompts\0503-2\R8.B\spec-09-window-thumbnail-wall.md` | R8.B | 0 | 12 | yes | 0 | verified | Thumbnail wall native capture, spec-11 VD propagation, and release-scale benchmark are verified: Zod contract, deterministic five-tuple group key utility, wall mode, row virtualization, 4 zoom levels, filter/group/refresh toolbar, existing batch selection integration, real `ThumbnailService`, `WindowGroupResolver` alias persistence, executable `window:thumbnails-batch` / `thumbnail-refresh` / `groups` / `set-alias` / `viewport-config` IPC, preload/global bridge, `WindowManager.getCachedWindows()` scan reuse, bounded 30s VD metadata cache for rapid refreshes, `koffi` Win32/DWM/GDI `Win32ThumbnailCapturer` with `DwmGetWindowAttribute`, target-size `StretchBlt`, `BitBlt`, `PrintWindow`, `GetDIBits`, real PNG data URLs, 4-concurrency `p-queue` and 800ms timeout, no fabricated screenshots, docs/tests, `ASSERT_THUMBNAIL_WALL_GROUP_KEY` Electron Playwright coverage, default `bench:thumbnail-capture` p95 5.8ms / p99 5.8ms with 3 real HWNDs, and 2026-05-19 release-scale `THUMBNAIL_CAPTURE_WINDOWS=100 THUMBNAIL_CAPTURE_MODE=per-window` benchmark artifact `research/thumbnail-capture-100hwnd-2026-05-19.json` with 100 distinct real WinForms HWNDs, 10 bounded host processes, `measuredCaptured=100`, `sourceCounts.win32-printwindow=100`, `uniqueHwndCount=100`, `totalCaptured=300`, `p95=10.8ms`, and `p99=11.9ms`; unresolved virtual desktop provider failures remain truthful null/unavailable states. |
| `prompts\0503-2\R8.B\spec-10-window-batch-ops.md` | R8.B | 0 | 15 | yes | 0 | verified | Window batch closure verified: Zod contract, sequential renderer model with incremental progress and best-effort skipped cancellation, toolbar selected-count UI, custom progress toast with cancel/failure detail/retry-failed-items action, retry re-execution only for latest failed HWND results, custom close/inject confirm dialog replacing `window.confirm`, shared Ctrl/Cmd/Shift/Ctrl+A/lasso `useBatchSelection`, real `data-window-selection-hwnd` DOM selection, card/list/process/AI-window/thumbnail-wall integration, spec-09 wall gesture forwarding, `p-queue@8.1.0`, main-process `WindowBatchExecutor`, executable `window:batch-op/cancel/undo` handlers, `window:batch-progress` push, preload/global bridge, confirmation enforcement, confirmed `args.text` through `WindowManager.sendTextToWindow()` with real Win32 `SendInput` plus `WM_CHAR` fallback, safe-key inject bridge, explicit topmost state, 5-second minimize undo, single-helper real `WindowManager.focusWindows()` batch focus path, `window.batch.focus-filtered` command event, filtered HWND focus regression, `args.alias` persistence through real `AIAliasManager`, real window scan validation, `setWindowTitle` rollback, Electron E2E real BrowserWindow minimize/progress/undo, `bench:window-batch` 20-HWND real focus benchmark p95 4077.2ms under 5000ms, and packaged Electron `ASSERT_WINDOW_BATCH_7_OPS` coverage for seven real HWND operations: focus, minimize plus undo, close, aot-toggle, screenshot file output, rename alias/title persistence, and inject-text into a real textarea. |
| `prompts\0503-2\R8.B\spec-11-window-virtual-desktop.md` | R8.B | 0 | 12 | yes | 0 | verified | VD/monitor executable slice verified: Zod contracts, `VirtualDesktopService` using Windows `IVirtualDesktopManager` COM plus registry desktop ID discovery, truthful `desktopId` null/unavailable boundaries, `MonitorService`, `WindowLayoutPresetStore`, executable `window:monitors` / `window:vd-list` / `window:vd-info` / `window:vd-watch` / `window:move-to-desktop` / `window:move-to-monitor` / `window:layout-*` IPC, preload/global bridge, batch `move-to-desktop` adapter, spec-09 thumbnail `desktopId` propagation, `VdSwitcher` without fabricated desktop IDs, docs/tests, real packaged Electron Playwright `R8.B spec-11` covering real WinForms HWND `vd-info`, GUID desktopId, thumbnail propagation, monitor move, and truthful `MoveWindowToDesktop` success/error. 2026-05-19 adds display-backed `window:vd-watch` main-to-renderer payloads from real Electron display events, Zod payload schema, typed preload/global listener cleanup, contract whitelist sync, targeted ESLint, and 31-test schema/MonitorService/preload-contract coverage. 2026-05-19 also closes the remaining spec-11 popout multi-monitor logic checklist by persisting BrowserWindow popout display affinity/restore metadata, migrating off-screen popouts to primary on display removal/metrics changes, restoring them to original bounds when the display is present again, and verifying schema plus `R8RuntimeService` regressions with 27 focused tests. 2026-05-19 closes the repeated VD query/list latency path with 2-second real-result caches, `window:vd-info` cached-monitor resolution instead of unconditional full scans, `VirtualDesktopService.test.ts` 8/8, focused IPC/schema tests, typecheck, Zod SoT, build, and `VD_INFO_SAMPLES=20 VD_INFO_P95_BUDGET_MS=50 pnpm -C devhub bench:vd-info` against 2 real WinForms windows with `p95=6.3ms` and `totalResolved=40`; the cold first sample remains visible as `max=959.6ms`. The `Win32ForegroundEventWatcher` gate is now satisfied after the current session created a second real Windows virtual desktop and enabled `DEVHUB_R8_VD_FOREGROUND_WATCH=1`; 2026-05-22 elevated local verification also closes the monitor continuity gate with `targetMode=single-display-fallback`, `baselineDisplayCount=1`, `minDisplayCount=1`, and `finalDisplayCount=1`, preserving the fact that no physical unplug/reconnect was simulated. |
| `prompts\0503-2\R8.B\spec-12-process-batch-ops.md` | R8.B | 0 | 11 | yes | 0 | verified | Process batch closure verified: Zod contract, selection hook with single/Ctrl/Cmd/Shift/Ctrl+A selection, six-action toolbar, backend-first renderer bridge with sequential fallback, custom process batch confirmation dialog for confirmation-required actions, progress surface with retry-failed PID subset rerun, p-queue-backed `ProcessBatchExecutor`, executable `process:batch-op/cancel/undo` IPC, `process:batch-progress` stream, per-PID kill with PID<100 refusal and no image-name wildcard termination, focus through real WindowManager PID window resolution plus explicit `args.hwnd` same-PID disambiguation, shared `InjectTextService` reused by `WindowBatchExecutor` and `ProcessBatchExecutor`, inject through real `WindowManager.sendTextToWindow()` with clipboard-paste/SendInput/WM_CHAR native paths, diagnostic export through real `R8RuntimeService.exportDiagnosticPack()`, batch tag dialog through real `process:tags-set`, main-process `ProcessTagStore` tag + undo bridge, add-watchdog per-PID registration through `R8RuntimeService.registerWatchdogInstance()`, command palette `process.batch.tag` routing, docs/tests, real spawned child-process PID kill regression, real Electron Playwright `R8.B spec-12` coverage for focus/inject-text/tag/add-watchdog/export-diag/kill through executable IPC, `bench:process-batch` `BENCH-PROCESS-BATCH-KILL` p95 378.8ms under the 10000ms budget, and `ASSERT_PROCESS_BATCH_6_OPS` closure. |
| `prompts\0503-2\R8.B\spec-13-port-security-tier-banner.md` | R8.B | 0 | 12 | yes | 0 | verified | Port security closure verified: shared 4-tier classifier, Zod contract, persisted user blocklist add/remove/reset, IPC blocklist/public-banner channels, renderer badge/banner/settings editor/filter, statusbar and cmdk linkage, docs/tests, and a real Electron restart E2E that opens a local TCP listener, persists a user blocklist entry through executable IPC, relaunches Electron, verifies `Suspicious` classification with `user-blocklist`, and confirms the real port card plus `public-port-banner` render without mock ports or fake firewall data. File-level ESLint, production build, Playwright grep, and TypeScript `tsc --noEmit` passed. |
| `prompts\0503-2\R8.B\spec-14-process-tags-history.md` | R8.B | 0 | 12 | yes | 0 | verified | Process tag/history closure verified: electron-store EXE+cwd tag store, SQLite 24h history store, one-minute sampler from real scans, process IPC/preload bridge, list/card/tree/treemap tag UI, inline 24h CPU sparklines, and docs/unit tests. 2026-05-13 restores concrete `process:tags-list`, `process:tags-set`, and `process:history-24h` IPC from R8 contract-only fallback, adds real Electron restart E2E for tagging a real scanned process identity, verifies the Process list badge, restarts Electron, checks persisted tag/export through executable IPC, probes 24h history truthfully, and restores any previous tag. 2026-05-15 adds the ProcessDetailDrawer resource-tab 24h large trend panel through the same real `process:history-24h` path, CPU/RSS/handles/threads switching, spec-12 batch tagging/5-second undo through real ProcessTagStore IPC, `process.batch.tag` command-palette entry, a spec-08 linked auxiliary statusbar mini-sparkline widget that keeps the 12-tile aggregate contract unchanged while loading 24h history through the same real IPC path, and `bench:sparkline` Chromium DOM evidence proving 100 visible 1440-point ProcessSparkline histories at p95 10.8ms under the 16ms budget. |
| `prompts\0503-2\R8.B\spec-15-i18n-scaffold.md` | R8.B | 0 | 11 | yes | 0 | verified | i18n scaffold closure verified: installed i18next/react-i18next, LocaleStore + real IPC/preload, LocaleProvider, LocaleSwitcher in settings, zh-CN/en-US resources, typed cached useT, extract/check scripts, Windows/Linux CI `i18n:check` gate, docs, unit tests, Electron E2E locale switch/persistence, generated zh-CN `legacy` catalog covering 1169 unique production renderer Chinese strings under the 80KB bundle budget, runtime legacy DOM localizer routing historical text nodes and display attributes through real `i18n.t(legacy.<hash>, { defaultValue })`, `i18n:extract` production/test/uncovered reporting, and `i18n:check` 100% production legacy coverage gate. 2026-05-17 follow-up closes the packaged Electron IPC fallback issue by registering R8 runtime before concrete i18n/window handlers, makes topology handler setup idempotent, prevents native `better-sqlite3` ABI mismatch from aborting unrelated R8.B handlers by falling back to Electron Store for task queue storage with audit evidence, prevents `legacy.<hash>` key leakage, preserves React-owned English i18n updates, adds `bench:i18n`, verifies `tCallMs=3.371` for 10,000 cached t calls, reruns packaged spec-15 E2E, and reruns spec-10 `ASSERT_WINDOW_BATCH_7_OPS` regression. en-US remains a preview locale and legacy missing translations fall back to zh-CN instead of fake machine translation. |
| `prompts\0503-2\R8.B\spec-16-a11y-full.md` | R8.B | 0 | 13 | yes | 0 | verified | Closed 2026-05-16 with real Electron evidence: axe/playwright deps, Zod prefs/self-check contracts, electron-store prefs, OS preference bridge, real a11y IPC/preload, SkipLink, aria-live provider, KeyboardNavGroup roving tabindex in main navigation/cmdk groups/thumbnail wall rows, focus/forced-colors/reduced-motion CSS, main landmark, settings controls, live-URL axe script, docs/tests, Windows CI single-grep smoke, component ARIA audit `169 TSX / 7 tabIndex / 10 surfaces / 0 blocking`, fixed statusbar executable handler ownership so extended handler init no longer aborts before `a11y:*`, fixed anonymous ProcessFilterBar/SettingsDialog icon buttons, fixed cmdk outer listbox ownership, added multi-surface live axe E2E for home/monitor/dashboard/settings/command with critical=0, added real `bench:a11y` against `out/main/index.js` with `criticalViolationCount=0`, `missingSurfaces=[]`, `p95=532.5ms`, `budgetMs=1500`, `passed=true`; verified by targeted Vitest, build, E2E, benchmark, tsc, targeted ESLint, no-emoji, and Zod SoT. |
| `prompts\0503-2\R8.B\spec-17-icon-library-mix.md` | R8.B | 0 | 10 | yes | 0 | verified | Icon library token slice verified: shared token contract, 4+1 explicit renderer registry, accessible Icon/BrandLogo components, fallback to lucide:HelpCircle, `useIconDefaults` theme-axis defaults for density/radius/motion, codex/claude/gemini AI sensing logos routed through `brand:*` tokens, icon IPC/preload bridge, command-palette token integration, docs/tests/no-emoji gate, current full-UI no-emoji scan (`No emoji found in 606 files`), a real Electron E2E that launches a real codex-like child process and verifies `brand:OpenAI` plus `lucide:Search`, and legacy hardcoded UI SVG migration through the token adapter in `components/icons/index.tsx` plus direct UI icon migrations in ErrorBoundary, AITaskView, ScriptSelector, PortRelationshipGraph, and NeuralGraph. Remaining renderer `<svg>` inventory is graphing/visualization/decorative only and is not an icon-token migration gap. `bench:icons` Vite bundle proof measured 56,454 raw bytes / 13,891 gzip bytes against a 200KB budget with zero namespace imports, and `Icon.test.tsx` now asserts mixed token render under 1ms/icon. |
| `prompts\0503-2\R8.C\prd.md` | R8.C | 0 | 0 | no | 0 | verified | Aggregated R8.C row verified after the 2026-05-22 elevated report at `.trellis/tasks/05-05-r8-0503-2-full-completion-ledger/research/r8-external-blockers-current.json` closed `R8C_SPEC17_ADMIN_SHELL`, `R8C_SPEC17_WINDOWS_SERVICE_INSTALLED`, and `H1_J16_ZERO_EGRESS_CAPTURE_READY`; zero-egress keeps whole-machine `globalPacketCount=8517` visible while proving app-scoped zero egress with `appScopedPassed=true` and `nonLoopbackEndpointCount=0`. |
| `prompts\0503-2\R8.C\spec-01-cli-output-parser.md` | R8.C | 0 | 10 | yes | 0 | verified | CLI parser closure verified: IParser, 4 strategies, ParserRegistry, StreamMultiplexer, Zod SoT schemas, R8 runtime handlers, preload bridge, event stream listener, strategy audit, rate-limited IPC, direct `CLIOutputParser.subscribe()` → shared `AITaskTracker` → `SignalCollector(cli_parse)` path, targeted parser/tracker/runtime tests, and 2026-05-14 packaged Electron E2E. The E2E launches `out/main/index.js`, feeds a real Claude `stream-json` shaped assistant event through `R8RuntimeService.parseCliChunk`, receives `cli:event-stream` via `window.devhub.r8.cli.onEvent`, and verifies `getProgress()` plus `getSessions()` through the public preload bridge. 2026-05-16 revalidated the real stdout path through focused parser/runtime tests, including line/shim fusion and recording capture of parsed stdout through the runtime bridge. 2026-05-16 adds visible determinate progress confidence ranges from the shared `DerivedProgress.confidenceRange` contract instead of displaying only a single guessed percentage. 2026-05-16 adds retry progress reset events through the existing `csv:session-event-stream`/`cli:event-stream` path with focused runtime/tracker tests proving an exact 0% retry signal resets the previous high-water estimate. |
| `prompts\0503-2\R8.C\spec-02-shim-codex.md` | R8.C | 0 | 13 | yes | 0 | verified | Codex SHIM closure verified: real Node passthrough fallback, current-process PATH prepend only, permission guard, uninstall cleanup, strict marker anti-spoofing, 7-field CodexParser coverage, startup `ensureShimReconciliation()` dead-link/sidecar cleanup, `which`-based real-command validation excluding the shim directory, public preload `shim:status`/`shim:uninstall`, bounded 100ms-to-1000ms shim client pipe reconnect, packaged Electron E2E via real Node fallback, and 2026-05-16 packaged Codex shim executable closure. `@yao-pkg/pkg` generated Windows x64 / Linux x64 / macOS x64 artifacts; Windows x64 executable was run locally and verified stdout/stderr passthrough plus named-pipe frames through `pnpm shim:verify:codex`. |
| `prompts\0503-2\R8.C\spec-03-shim-claude-stream-json.md` | R8.C | 0 | 12 | yes | 0 | verified | Claude stream-json closure verified: strict shared Zod discriminated union, parser mapping for system/assistant/tool_use/result/partial/error, per-instance partial 100ms throttle, token/cost payloads, executable ai:claude-cost-summary IPC/preload bridge, ai:claude-stream-event emitter, 2026-05-15 real unified ERROR notification publication for schema-valid Claude `result.is_error=true` events, 2026-05-15 schemaVersion=1 default propagation, 2026-05-16 Claude shim pre-spawn injection for generated fallback plus shared packaged source with real child argv checks, npm registry compatibility matrix, `0.2.9` help probe, Grok/official-doc corroboration, local Claude Code 2.1.111 help/version evidence, packaged Electron Playwright stream-json lifecycle E2E through real preload subscriptions and runtime parser hook, and 2026-05-17 post-output live restart authorization closure. The fallback now persists a pending-confirmation request, surfaces a unified toast/statusbar action, starts no child before confirmation, then spawns a real local child with forced stream-json args and feeds stdout/stderr back into the Claude parser; verified by focused restart test, Claude grouped regression, schema/preload regression, touched-file ESLint, and full TypeScript no-emit check. Old Claude Code executables that reject `--output-format stream-json` remain explicitly unsupported rather than faked as structured output. |
| `prompts\0503-2\R8.C\spec-04-shim-gemini-stdout.md` | R8.C | 0 | 9 | yes | 0 | verified | Gemini stdout closure verified: shared Zod pattern schemas, eight precompiled parser rules, bracket/prose tool-call extraction, ANSI fallback, long-line bounding, partial-buffer cap, executable pattern stat/rule reload IPC with preload/global types, real chokidar reload from `userData/gemini-pattern.json`, default-on `R8.C.shim.gemini` registry/evaluation test, generated Gemini passthrough shim env injection and DONE marker frame, SignalCollector `cli_parse` ingestion from real Gemini parser events, one-shot local WARN audit when real parser matchRatio < 0.5 after five Gemini stdout lines, 30s stale stdout `unknown` event and WARN audit, Gemini shim frames routed through `strategy=line`, packaged Electron E2E that installs and runs the generated Gemini shim with real Node, verifies env injection, named-pipe stdout frames, and renderer `cli:event-stream` progress/tool-use/message-out events, plus focused tests, ESLint, typecheck, build, no-emoji, and Codex shim regression evidence. |
| `prompts\0503-2\R8.C\spec-05-cursor-copilot-detection.md` | R8.C | 0 | 10 | yes | 0 | verified | Cursor/Copilot detection closure verified: shared window-title schemas, built-in title-rules.json, confirmed rule override validation, executable-basename whitelist including real Windows extensionless `ProcessName` values, 5 Hz scan cap, same-hwnd 5s reuse, confidence cap tests, platform-aware feature-flag defaults, privacy audit rows with hashed titles only, 2026-05-13 real production-build Electron E2E using a spawned `cursor.exe` process window through real WindowManager scan -> scannerCache -> `cli:cursor-copilot-status`, 2026-05-15 focused Vitest coverage for five title-detection GWT cases, and 2026-05-15 direct `koffi`/`user32.dll` native-first `EnumWindows` / `GetWindowTextW` / `GetWindowThreadProcessId` integration with PowerShell fallback, repeatable smoke script, touched-file ESLint, full `WindowManager.test.ts`, and TypeScript passing. Persistent rule watcher, renderer settings panel, SignalCollector fusion, and installer-packaged E2E remain tracked as downstream boundaries rather than this spec's open checklist. |
| `prompts\0503-2\R8.C\spec-06-cli-detect-init.md` | R8.C | 0 | 10 | yes | 0 | verified | CLI detection closure verified: shared tool-detect schemas, five-row ToolDetectionState, Promise.allSettled full scan, 3000ms timeout, 300000ms cache TTL, persisted user override, Settings ToolDetectPanel, startup `cli:detection-event` broadcast, Cursor/Copilot module-list strategy, audit logging, docs/tests, and 2026-05-13 no-mock GWT matrix using real `process.execPath` overrides. 2026-05-14 closes the remaining migration/UI/E2E boundary with real `execa`/`which`/`semver` probing, `cli:clear-tool-override`, Settings path save/clear editor, LocaleProvider malformed-manifest guard exposed by the Settings path, targeted eslint/typecheck/build/Vitest, and packaged Electron Playwright `R8.C spec-06 Settings` E2E. 2026-05-16 extends scanner snapshot detection to codex/claude/gemini as well as cursor/copilot, and tightens Copilot detection so plain `gh.exe` rows without Copilot evidence are not treated as Copilot. |
| `prompts\0503-2\R8.C\spec-07-monitor-window.md` | R8.C | 0 | 11 | yes | 0 | verified | Monitor window closure verified: BrowserWindow transparent/vibrancy/resizable options, dedicated `out/renderer/monitor.html` entry, independent `out/preload/monitor.cjs` monitor-only preload, 100ms monitor:snapshot-stream, five real snapshot-backed ToolCards, ConfidenceBadge thresholds, always-on-top/opacity store writes, default-ON feature flag, audit rows, 4D theme sync, packaged Electron 5-GWT coverage, main-window-close survival through executable monitor.snapshot(), and permission assertions proving the monitor BrowserWindow exposes only `window.devhub.r8.monitor` while denying project/systemProcess/r8.popout/r8.cli namespaces. 2026-05-16 extends task progress UI with confidence interval labels such as `约 40%-54%` while preserving exact `aria-valuenow` for accessibility. 2026-05-16 also routes explicit runtime progress percentages into `AITaskView`, allowing a real retry reset to display 0% instead of the fallback coding band. 2026-05-16 keeps active cards on a 1s render tick and prevents lower-confidence explicit estimates from freezing the time-derived long-task progress curve. 2026-05-16 F.2.1 audit confirms `ProgressBar` preserves exact determinate values while the shared state taxonomy exposes more than four fine-grained visible states. |
| `prompts\0503-2\R8.C\spec-08-monitor-window-popout.md` | R8.C | 0 | 10 | yes | 0 | verified | Monitor tool popout closure verified: reused R8.B BrowserWindow bridge, 320x140 default and 200x100 min bounds, effective five-tool cap, double-click return-to-main, default-ON R8.C.monitor.popout flag, popout open/close audit rows, monitor:popout-snapshot-stream, layout persistence, main-monitor-close stream survival, packaged Electron 5-GWT coverage, and 2026-05-14 dedicated electron-vite `monitor-popout` renderer/preload entries. Tool popout BrowserWindows now load `out/renderer/monitor-popout.html` plus `out/preload/monitor-popout.cjs`, expose only `window.devhub.r8.monitor`, deny project/systemProcess/r8.popout/r8.cli namespaces, and pass focused lint, typecheck, build, no-emoji, spec-08 Playwright, and spec-07/spec-08 regression gates. |
| `prompts\0503-2\R8.C\spec-09-skill-library-yaml.md` | R8.C | 0 | 9 | yes | 0 | verified | Skill library closure verified: gray-matter + js-yaml frontmatter path, strict Zod schemas with license/sandbox/mcpServers metadata, scriptPath containment, chokidar add/change/unlink watcher, 100ms skill:list-stream payloads, user override/install/fork/uninstall audit rows, default-ON feature flag, local-only/no-telemetry boundary, docs/tests; standalone service file split intentionally not introduced to preserve current architecture. |
| `prompts\0503-2\R8.C\spec-10-skill-builtin-10.md` | R8.C | 0 | 9 | yes | 0 | verified | Builtin catalog closure verified: exact 10-name typed manifest, schema-valid offline fs-read built-ins with `license=MIT`, `sandbox=read-only`, and `mcpServers=[]`, default-ON `R8.C.skill.builtin`, runtime flag-off behavior that keeps user skills visible while builtin operations return `E_FEATURE_DISABLED`, fork materialization to real user files, docs, feature-flag test, and targeted service/IPC/schema/renderer tests. |
| `prompts\0503-2\R8.C\spec-11-skill-editor.md` | R8.C | 0 | 11 | yes | 0 | verified | Skill editor closure verified: dynamic Monaco import, embedded offline YAML schema, YAML/Body/Script tabs, 200ms validation debounce, valid-only save, five-template picker, destructive delete confirmation for user skills, script language switching, local CSS-token Monaco theme sync, default-ON feature flag, no-upload boundary, docs, targeted service/IPC/schema/renderer tests, and Playwright real Monaco worker/rendering GWT (`pnpm -C devhub test:e2e --grep "R8.C spec-11" --reporter=line`) all passed. |
| `prompts\0503-2\R8.C\spec-12-csv-task-driver.md` | R8.C | 0 | 10 | yes | 0 | verified | CSV driver closure verified: papaparse NODE_STREAM_INPUT file parsing, bounded 64 KiB encoding probe, UTF-8/UTF-16LE/GB18030 streams, low-resource watcher opt-in, typed csv:row-stream IPC/preload/global contract, reload audit rows, default-ON feature flag, docs, duplicate `taskId` rejection, opt-in real `inputFile` existence check, likely API key leak rejection, prompt-level `@skill:<name>` reference validation/mapping, missing or malformed prompt SKILL launch rejection, `{{cwd}}`/`{{file}}` interpolation, real bounded `@file:` expansion, unreadable `@file:` launch rejection, and targeted tests including 1200-row quoted-newline fixture. |
| `prompts\0503-2\R8.C\spec-13-csv-schema-18cols.md` | R8.C | 0 | 10 | yes | 0 | verified | CSV schema v1 closure verified: fixed 18-column header remains unchanged, parsed rows now receive hidden `schemaVersion: "1.0"` metadata, docs clarify it is not a 19th CSV column, and targeted CSV/Zod tests plus typecheck passed. |
| `prompts\0503-2\R8.C\spec-14-csv-launch-3way.md` | R8.C | 0 | 14 | yes | 1 | verified | CSV launch 3-way closure verified for metadata reader, DevHub dry-run DAG/task queue start, CLI command/clipboard/bin entry, Python sha256 check plus real named-pipe child-to-DevHub bridge, long-lived bidirectional pause/resume control pipe, session event stream, cli:event-stream mirroring, runner info/session controls, R8OpsPanel wizard, mutual exclusion, audit rows, docs, and targeted runtime/IPC/preload tests. 2026-05-14 adds real Python control socket ACK coverage, script SHA256 verification, Python compile, ESLint, TypeScript, and focused Vitest evidence. |
| `prompts\0503-2\R8.C\spec-15-task-queue-better-queue.md` | R8.C | 0 | 13 | yes | 1 | verified | Task queue closure verified: RowHash, ResumePolicy, strict state-machine, graphlib scheduler, retry backoff, parallel group controller, task:state-stream payloads, task transition audit, CSV fixtures, real `queue.sqlite` persistence via installed `better-sqlite3`, normalized task/transition SQLite indexes, startup `PRAGMA integrity_check`, corrupt SQLite backup/rebuild, legacy task-state migration, enum `R8.C.task.queue.engine` selector semantics, dedicated OnFailHandler branch routing, `inputArgs` on_fail mapping, fallback-tool row mutation, `task:tool-switch`/`task:on-fail` audit rows, real local `execute-skill` SKILL execution with bounded `execFile`, failure-context/stdout/stderr/result artifacts, queued retry state after successful SKILL execution, Node read-only/read-write/system sandbox enforcement, system SKILL child-process permission coverage, local stdio MCP server metadata/call compatibility, installed/runnable `better-queue@3.8.12`, a real BetterQueueAdapter with better-sqlite3-backed store/event tests, native BetterQueue event to `TaskStateTransition` bridge coverage, and 2026-05-16 retry-progress reset emission. 2026-05-17 closes the exact `better-queue-sqlite` checklist line by verifying npm metadata (`better-queue-sqlite@1.0.7` depends on `sqlite3@^5.1.2`), local dependency truth (`better-queue` and `better-sqlite3` installed; `sqlite3` unavailable), explicit runtime package-status fields, storage warning coverage, and the no-broken-dependency decision to use the runnable better-queue custom store API with `better-sqlite3`. Verified by focused spec-15 queue/runtime/schema suite, touched-file ESLint, full TypeScript no-emit, npm metadata check, and local dependency inspection. Active runtime promotion from StoreBackedTaskQueueService to BetterQueueAdapter remains a separate migration boundary, not a checklist blocker. |
| `prompts\0503-2\R8.C\spec-16-watchdog-engine.md` | R8.C | 0 | 12 | yes | 13 | verified | Watchdog policy-engine slice strengthened and verified: nine heartbeat source names are registered with per-instance enable/disable gates, disabled-source heartbeats are retained for audit without refreshing lastHeartbeatAt or transiently resetting suspect/stuck instances to healthy, lenient fusion records max-weight heartbeat acceptance, strict fusion accepts primary high-trust or multi-source proof while rejecting CPU-only proof, state-machine history records healthy/suspect/stuck/restarting plus storm paths, restart governor enforces a one-hour maxRestartsPerHour window, GracePeriodManager applies 30s startup / 60s restart grace with 1.5x high CPU/IO extension, per-phase timeout and watchdog feature-flag defaults are covered by tests, 2026-05-16 adds bounded `watchdog:event-stream` schema/preload/runtime batching through the spec-31 high-frequency scan limit, 2026-05-16 adds real ActionExecutor integration into spec-15 task queue status, spec-18 restart-resume injection calls, and spec-30 watchdog notifications, 2026-05-16 adds a real 16-instance CPU benchmark artifact with estimated CPU `0.000336%` under the 1% budget, 2026-05-16 verifies kill/starvation/storm/phase-aware behavior through a focused suite including a real killed child process, 2026-05-19 verifies real WatchdogHeartbeatCollector adapters for marker-file/stdout/cpu-pulse/window-title/http-health/fs-activity/hung-window/network/etw through local filesystem, runtime metadata, SystemInformationAdapter, Win32WindowEnumerator, localhost fetch, Windows IsHungAppWindow P/Invoke, and logman ETW probing, and 2026-05-19 adds a configurable `memoryRestartMb` max-memory restart path that reads real heartbeat `memoryMb`, emits `memory-over-limit` history, and requests restart before grace/heartbeat timeout masking can hide memory exhaustion. Verified by focused collector/runtime/engine Vitest, touched-file ESLint, typecheck, no-emoji, and Zod SoT. Remaining Windows Service UAC execution and kill/orphan packaged E2E belong to spec-17, not spec-16; non-elevated ETW returns truthful `E_PERMISSION_DENIED` rather than fake heartbeat success. |
| `prompts\0503-2\R8.C\spec-17-watchdog-subprocess.md` | R8.C | 0 | 16 | yes | 0 | verified | Watchdog supervisor contract slice synced and strengthened: Zod RPC/session/marker/channel/supervisor schemas exist, WatchdogSupervisor generates a real 64-hex sessionToken and parent marker file without faking child success, marker fallback liveness now requires writer=inner-watchdog plus matching token prefix and fresh updatedAt, watchdog-supervisor status snapshots evaluate stale channels before returning, HandshakeProtocol rejects invalid tokens, MutualHeartbeat models named-pipe/TCP/marker health, respawn governor reports 1s/2s/4s/8s/16s backoff and rejects the sixth attempt within one hour while allowing attempts after the sliding window expires, Windows Service install/uninstall now has a real `sudo-prompt` elevated execution path that only runs after `confirmAdmin=true`, verifies success through `sc.exe query`, persists `windowsServiceInstalled`, and keeps unconfirmed/non-Windows/cancelled/failed paths truthful instead of faking service state, feature flags default to subprocess ON / windows-service OFF with user override, 2026-05-16 adds real configured Node child spawn proof with token/marker argv and env verification, 2026-05-16 adds central supervisor control-plane audit rows for respawn, service command plans, handshake-fail, channel-degrade, and orphan status if surfaced, 2026-05-16 adds low-resource lifecycle fixtures for real child kill plus explicit named-pipe channel failure, 2026-05-16 adds the packaged InnerWatchdog entrypoint built as `out/main/watchdog-process/main.js`, direct real execution proof that rewrites a marker as `writer=inner-watchdog`, `ELECTRON_RUN_AS_NODE=1` propagation, and marker-file fallback liveness recognition without faking named-pipe success, 2026-05-18 closes the three-channel path with real parent-side named-pipe and TCP localhost JSON-RPC servers, validated child handshake, authenticated `ping`, marker `tcpPort` propagation, InnerWatchdog named-pipe-to-TCP fallback, explicit unsupported-method errors, startup before configured child respawn, runtime dispose cleanup, full supervisor Vitest evidence, R8RuntimeService respawn/audit regression coverage, child-side event-pipe JSON-RPC server with real authenticated parent-to-child ping, child `get-status`, orphan-mode `E_ORPHAN_READ_ONLY` refusal for new control RPC through long-lived child-process tests, parent heartbeat scheduler closure with default 5s parent-to-child ping, runtime start after successful respawn, scheduler teardown, real event-pipe scheduled pong coverage, repeated parent-ping failure coverage without inventing liveness, 2026-05-18 real built-entry subprocess benchmark evidence via `bench:watchdog-subprocess` measuring RSS 44.82MB and CPU 0% under the 80MB / 0.5% budget, 2026-05-18 registered-instance orphan carryover with a real InnerWatchdog in-memory instance map, attached-only register/deregister/configure RPC, heartbeat-tick `lastHeartbeatAt` refresh, `get-status` instance summaries, and a real long-lived child-process test proving an already-registered instance survives parent PID orphaning while new control RPC remains refused, 2026-05-18 restart takeover with existing session-token adoption only after the stored parent PID is dead, one-shot forced parent marker publication without fake liveness, child re-read/inner marker rewrite, and a real long-lived child-process named-pipe handshake to the new parent PID, 2026-05-18 full lifecycle audit rows for spawn/respawn/orphan/takeover/handshake-fail/channel-degrade with token-prefix-only audit targets and deduped takeover/orphan evidence, 2026-05-18 renderer supervisor event stream with Zod payload, `watchdog-supervisor:event-stream` registry, preload cleanup bridge, R8OpsPanel subscription, status/respawn/spawn/heartbeat/service/handshake/channel/orphan/takeover/evaluate emits, and tests proving no full 64-hex session token reaches the renderer, and 2026-05-19 packaged Electron E2E closes the kill/orphan boundary by verifying real built-app respawn after killed InnerWatchdog PID and real orphan read-only behavior after the marker-recorded parent PID exits. 2026-05-19 also fixes the Windows pipe root to `\\.\pipe\`, removes long-lived child `spawn({ timeout })`, adds isolated `DEVHUB_USER_DATA_DIR` E2E state, and passes focused Windows Service executor/supervisor/IPC/typecheck gates. 2026-05-20 adds the `WindowsServiceInstaller` `execFile` import/typecheck hardening and confirms the focused Windows Service lint/test/typecheck gates stay green. 2026-05-22 elevated Windows refresh closed the live Administrator and Windows Service strict gates with `.trellis/tasks/05-05-r8-0503-2-full-completion-ledger/research/r8-external-blockers-current.json` generated at `2026-05-22T13:15:06.603Z`, `admin.isAdministrator=true`, `service.installed=true`, `service.status=Stopped`, `service.scExitCode=0`, and passed gates `R8C_SPEC17_ADMIN_SHELL` plus `R8C_SPEC17_WINDOWS_SERVICE_INSTALLED`. |
| `prompts\0503-2\R8.C\spec-18-auto-inject.md` | R8.C | 0 | 15 | yes | 0 | verified | Inject selector/sandbox slice strengthened and verified: shared inject Zod contracts exist, InjectModeSelector preserves CLI/meta/GUI fallback semantics including GUI clipboard-paste -> uia -> sendinput fallback while meta commands now require pty without UI fallback, dry-run normalizes text, chunks, resolves target, writes local audit history, and never calls native typing, sendinput execution downgrades native short-write reports to status=partial with E_PARTIAL_INJECT instead of fake success, feature flags default inject.engine and audit-full-content ON while strict target mode stays OFF, target resolver covers whitelist/strict/countdown gates, 2026-05-18 adds the real pre-execution inject:countdown-stream controller with scheduled/tick/completed/cancelled Zod payloads, preload cleanup bridge, cancellation result, and focused runtime/schema/type/lint/Zod/no-emoji evidence, 2026-05-18 adds a complete InjectFailureClassifier diagnose/classify recommendation matrix for all shared failure kinds, 2026-05-18 routes sendinput execution through real <=8KB UTF-8 chunks with 200ms interval and per-chunk focus hook, 2026-05-18 aligns inject:execute registry and concrete handler with the medium_query bucket, 2026-05-18 adds real better-sqlite3 append-only audit persistence at app userData inject-audit.sqlite with full text, sha256(text) index, action/time indexes, schema-validated list reads, malformed-row skip, and runtime path evidence, 2026-05-18 adds a concrete ScenarioBase subclass tree plus InjectScenarioRegistry for all six scenarios with executable buildAction and success/failure hooks, 2026-05-18 adds concrete IInjectMode subclasses plus InjectModeRegistry for sendinput/pty/uia/clipboard-paste while preserving truthful unavailable-adapter failures, 2026-05-18 adds real clipboard-paste bridge behavior with Electron clipboard save/write/restore and nut-js Ctrl+V paste shortcut, 2026-05-18 adds real OS foreground focus polling through `node-window-manager` active-window reads at 50ms, resolved/baseline HWND comparison, early delay cancellation, and no fallback after `E_USER_STOLE_FOCUS`, 2026-05-18 adds real SHIM control-channel meta-command execution: shared `ShimControlFrame` schema, `ShimRegistry.sendControl()` socket write plus ACK wait, generated and packaged shim reverse control parsing into child stdin, runtime bridge wiring, and Vitest proof through real generated/packaged shim child stdin echo frames without UI fallback, 2026-05-18 adds pre/after inject screenshot sequencing through `InjectService.screenshotBridge`, wired in runtime to the existing real `WindowManager.screenshotWindow(hwnd)` PNG path, with after-screenshot failure downgraded to partial instead of fake complete success, 2026-05-18 adds post-inject pty echo content verification: ordinary prompt pty uses SHIM stdin with `verifyEcho=true`, `ShimRegistry` waits for real stdout/stderr echo frames instead of using ACK as proof, no-echo children return delivered-but-unverified, `InjectService` downgrades unverified writes to partial after the after-screenshot, and audit payload JSON records verification and screenshot paths. 2026-05-18 closes task-start/watchdog restart trigger integration: `allow_inject=true` task starts invoke real `csv-task-driven` inject through `csv-row-alias`, persist the inject action id back to the `TaskRun`, keep truthful failed results when adapters are unavailable, and the existing watchdog restart action path revalidates task retry, inject action id, and notification output. 2026-05-18 closes real UIA editable-control behavior: `UiaMode` walks the Windows UIAutomation raw tree from a resolved HWND, writes through `ValuePattern` when available, falls back to real `user32.dll` `WM_SETTEXT` / `WM_GETTEXT` for native `EDIT` children, returns real `verifiedContentMatches`, and is verified by a real WinForms TextBox fixture plus build/typecheck/lint/Zod/no-emoji gates. |
| `prompts\0503-2\R8.C\spec-19-auto-inject-targets.md` | R8.C | 0 | 15 | yes | 0 | verified | Inject target resolver slice strengthened and verified: alias/ready-pool/csv-row-alias/pid/window-handle selector chain, whitelist gate, strict-mode gate, countdown computation, rowAlias-only CSV target resolution, concrete pid/hwnd selector validation, project-cwd whitelist matching is constrained to exact or descendant path boundaries rather than string-prefix siblings, default-ON targets flag, default-OFF strict-mode flag with override, docs, targeted resolver/runtime/contract tests, and 2026-05-18 CountdownController stream/cancel execution through the existing runtime boundary. 2026-05-18 adds real runtime whitelist expiry cleanup: startup plus hourly timer, `enabled=false` marking instead of deletion, session-duration current-process semantics, add/remove/expire/strict-block audit history entries, and targeted runtime evidence; it also adds a resolver matrix covering 4 selector shapes, all 3 Zod-valid scopes, and 4 durations without inventing an invalid fourth scope. 2026-05-18 first-time confirmation/UI slice adds real SQLite first-time confirmation persistence, `inject:first-time-required` broadcast, `inject:first-time-confirm` and `inject:countdown-complete` IPC/preload contracts, countdown modal ESC/immediate completion, first-time duration/scope modal, R8.B drawer-backed whitelist list/add/remove/expired state, and first-time-confirm plus whitelist-add audit history. 2026-05-18 reconciles the historical 4-scope fixture wording to the authoritative 3-scope Zod contract and verifies the 4 selector × 3 scope × 4 duration matrix. 2026-05-18 closes alias/ready-pool sources by merging active `AITaskTracker` alias/pid/hwnd rows, CSV/task-queue row aliases, whitelist fallback rows, and spec-28 state-machine `awaiting-input` snapshots into resolver records without fabricated targets. 2026-05-18 packaged Electron Renderer E2E closes the target-management boundary: a real dry-run CSV queued task resolves target alias `codex-${taskId}`, opens the first-time modal through runtime/preload events, persists a `24h` `instance` whitelist confirmation, mirrors the confirmed repository entry into Electron Store fallback when packaged SQLite is unavailable, renders the alias in the R8.B drawer-backed whitelist drawer, starts the real countdown stream, and cancels via Escape through `inject:countdown-cancel` with a real cancelled `InjectResult`. Verified by typecheck, focused first-time/inject Vitest, `pnpm -C devhub build`, and `pnpm -C devhub test:e2e --grep "R8.C spec-19" --reporter=line --workers=1`. |
| `prompts\0503-2\R8.C\spec-20-dag-orchestrator-graphlib.md` | R8.C | 0 | 12 | yes | 0 | verified | DAG orchestrator closure verified: DependencyDslParser covers after/success/any/comma/completed DSL forms, CycleDetector combines graphlib cycles with Tarjan-style paths, explicit `forwardEdgesByTaskId` / `reverseEdgesByTaskId` maps now sit beside graphlib.Graph and are consumed by TopoSorter/CriticalPathAnalyzer, deterministic Kahn layer snapshots and priority/group ranking exist, `CriticalPathAnalyzer` now runs forward/backward estimated-duration passes with per-task earliest/latest/slack timings, Mermaid/DOT/Cytoscape export works, isReady evaluates completed/failed terminal sets and rejects unknown task IDs as not-ready, R8RuntimeService exposes dag build/detect/export/layer/check-ready, default-ON feature flag is covered, `buildWithMetrics()` exposes real phase timings, named Vitest fixtures cover dag-5/dag-100/cycle-3/orphan/parallel-group-conflict plus a 1000-node fanout performance fixture, `DagAuditEntry` Zod schema and `listDagAudit()` verify per-build hash/previousHash/sequence audit rows from real CSV builds, StoreBackedTaskQueueService now reuses DagOrchestrator for topological ordering and dependency readiness instead of a parallel graphlib scheduler, queue readiness supports failure/success/completed dependency conditions, and packaged Electron E2E verifies dag build/layer/export/check-ready/detect-cycle through the real preload IPC bridge. Visual editor ownership belongs to spec-21, not spec-20. |
| `prompts\0503-2\R8.C\spec-21-dag-visual-editor.md` | R8.C | 0 | 14 | yes | 0 | verified | DAG editor renderer/runtime slice verified: CSV lock/load, drag dependency update, cycle save guard with Cytoscape `edge.in-cycle` marking, runtime csv save with Zod validation + lock ownership + cycle rejection + mtime conflict + temp rename, user template save/insert path, shared `DagCanvas` Cytoscape wrapper, default-ON dag.editor flag, docs, targeted renderer/runtime/feature tests, and 2026-05-18 audit rows for `csv:dag-editor-open`, `csv:dag-editor-close`, `csv:save`, `csv:cycle-attempt`, and `csv:lock-conflict` without storing CSV row contents. 2026-05-18 adds top-level Zustand `DagEditorState`, dedicated `dag-editor-sync.ts` dependency string/edge/graph projection helpers, `immer` JSON-patch undo/redo with max-50 history and adjacent same-task-field compaction, store-backed canvas/list/gantt/kanban view sharing, built-in five-template palette support, full `NodeDetailPanel` 18-column editing with shared `csvTaskRow18Schema` realtime validation, real chokidar-backed external mtime watch modal through `csv:external-change-stream`, roving-tabindex canvas a11y navigation with live node announcements, packaged Electron Playwright coverage for edit/cycle/four-view/undo/external-modify, and packaged-runtime SQLite ABI hardening so first-time inject confirmation/audit SQLite failures no longer abort all R8 IPC handlers. Verified by focused DAG editor/runtime/IPC/a11y Vitest, typecheck, touched-file ESLint, Zod SoT, no-emoji, `pnpm -C devhub build`, a real `out/main/index.js` `csv.lock` probe, and `pnpm -C devhub test:e2e --grep "R8.C spec-21" --reporter=line --workers=1` passing with 1 packaged Electron test. Cytoscape edge deletion UI remains a downstream non-checklist refinement. |
| `prompts\0503-2\R8.C\spec-22-task-recording.md` | R8.C | 0 | 14 | yes | 0 | verified | Recording engine closure verified: runtime recording.engine gate, default-ON screenshot/fs/git-diff stream flags with runtime filtering, real local recording artifacts for stdout/stdin/fs/git-diff, chokidar ignored paths, per-task archive rotation, LRU stopped-recording eviction, redacted ZIP export that leaves source artifacts recoverable, asciinema export, audit rows, stdout 1000-line performance budget with queued append-only writes, p-queue screenshot throttling, screenshot non-blocking stdout benchmark, `simple-git@3.36.0` branch/HEAD/diff-stat/full-diff capture, dedicated stdout/stdin/screenshot/fs/git-diff stream classes, task-queue UUID session automatic recording start/stop with persisted `recordingId`, taskId-based stdout matching, focused ESLint, TypeScript, RecordingService Vitest, R8RuntimeService recording/task-queue Vitest, and TaskQueueService Vitest. Release-level packaged screenshot-frequency benchmark and renderer recording browser remain downstream/non-blocking surfaces. |
| `prompts\0503-2\R8.C\spec-23-task-replay.md` | R8.C | 0 | 13 | yes | 0 | verified | Keep evidence current; rerun targeted gate before final closure. |
| `prompts\0503-2\R8.C\spec-24-topology-global-fullscreen.md` | R8.C | 0 | 13 | yes | 0 | verified | Global topology slice advanced: shared GraphService network/neural/flow paths through R8RuntimeService, LRU cache max 20 keyed by slice/asOfTs, fullscreen topology toolbar, default 500-node degradation with explicit expand, five redundant entries through activity bar/command palette/status bar/Ctrl+T/route, process-node global-to-detail navigation, process-detail reverse selection into `FullScreenTopologyView`, default-ON `R8.C.topology.global`, audit rows for time-cursor/snapshot-save/export, 2026-05-16 automatic `GraphSnapshotter` coverage, cwd/project-root plus cmdline alias neural inference, and port/window/project reverse-selection actions are verified. The snapshotter starts from `setupR8RuntimeHandlers()`, schedules a 300000ms unref recursive timeout, builds fresh global network/neural/flow snapshots through `GraphService.buildGlobal(..., { refresh: true })`, persists via the existing confirmed filesystem snapshot writer, prunes only auto-generated `auto-topology:*` labels after 24h, stops on feature-flag disable/dispose, and is covered by focused graph/runtime/IPC Vitest. Neural inference now maps missing `projectId` processes back to registered project roots via `workingDir`/command path boundaries and adds `tag-alias-*` nodes for Claude Code, Codex, Gemini CLI, Cursor, Node package managers, and Python runtimes through real process name/command metadata. Port/window/project reverse-selection actions are covered by focused renderer bridge Vitest. 2026-05-17 adds real `systeminformation@5.31.6` primary network inventory through `SystemInformationAdapter`, preserves real `netstat -ano -p TCP` fallback in `PortScanner`, routes network graph construction through explicit `NetworkTopologyBuilder`, carries port source provenance and process handle-count metadata when present, and verifies live local `systeminformation.networkConnections()` plus focused adapter/port/graph/topology gates. Renderer PNG export now runs through `GraphCanvas.exportPng()` over the real current `GraphSnapshot` SVG layer. 2026-05-17 adds Vitest + Electron Playwright 100/500/800-node fixture coverage across `network-topology`, `neural-relationship`, and `flow`, verifies the time cursor, and preserves the 500-node default guard. Shared Cytoscape renderer reuse is verified by `DagCanvas` reuse inside `GraphCanvas`; 2026-05-17 current rerun passed focused renderer/manifest Vitest with 4 files / 11 tests and Electron Playwright `R8.C spec-24 global topology` with 1 test. |
| `prompts\0503-2\R8.C\spec-25-topology-attached-deep10.md` | R8.C | 0 | 13 | yes | 0 | verified | Attached topology closure verified: AttachedGraphView exposes graph-kind switcher, 1-10 depth slider, local pinned favorites, in-panel favorites quick-entry menu, default depth 3 with 1/3/7/10 markers, lazy-mode banner for depth 8-10, real expandable-node LazyExpander placeholders, double-click expansion through typed `topology:attached:get-deep10`, cumulative >500 expanded-node rejection with refused audit, mini-thumbnail plus floating card, selected-node handoff through `GraphService` `selectedNodeId`, default-ON feature flag, true depth-10 truncation/expansion behavior, process/port/window attached entry markers, favorite-change audit IPC, buildMs telemetry, and a real Electron Playwright fixture that launches a real TCP listener and BrowserWindow, verifies three-end entries, depth=10 lazy, favorites, and mini mode. Focused Vitest, touched-file ESLint, typecheck, production build, Playwright grep, Zod SoT, no-emoji, no-cloud-deps, no-ocr-deps, and scoped diff-check passed. |
| `prompts\0503-2\R8.C\spec-26-flow-attached.md` | R8.C | 0 | 15 | yes | 0 | verified | Attached flow closure verified: FlowBuilder real-source merge across task queue, CSV launch sessions, spec-22 recording manifests/events, and persisted audit entries; process/port/window embeds; fullscreen `graphKind='flow'`; speed/filter/stats/export controls; typed `flow:event-stream` IPC/preload realtime append bridge; audit rows for window/cursor/export/subscribe/unsubscribe; most-recent-500 auto-shrink with `E_GRAPH_NODE_LIMIT`; SQLite-backed 24h `flow_event_index`; `FlowAnimationLayer` via the existing motion bridge; packaged Electron Playwright fixture for 30min/24h/filter/export/realtime; 500-event render and append performance budgets; and build/typecheck/focused Vitest/ESLint/Zod SoT/no-emoji evidence. Keep evidence current; rerun targeted gates before final all-doc closure. |
| `prompts\0503-2\R8.C\spec-27-ai-signal-fusion-tuning.md` | R8.C | 0 | 22 | yes | 0 | verified | Signal fusion closure verified: weighted profiles, normalized weights, decay, contribution transparency, stream throttling, runtime IPC/preload bridge, settings UI, default-ON flag, audit row, and targeted service/runtime/IPC/schema/preload/renderer tests are covered. 2026-05-14 closes the former Dempster-Shafer/Bayesian gap with real mass-combination and log-odds algorithms in `SignalFusion`, regression tests proving no weighted-mean degradation, focused service/runtime Vitest, ESLint, TypeScript, and GitNexus LOW impact evidence. The legacy AITaskTracker path remains preserved by design rather than deleted. |
| `prompts\0503-2\R8.C\spec-28-ai-state-machine-3layer.md` | R8.C | 0 | 11 | yes | 0 | verified | Spec-28 remains verified. 2026-05-16 evidence expands the legacy AI monitor taxonomy from 8 visible states to 12 states (`initializing`, `idle`, `thinking`, `receiving-input`, `coding`, `compiling`, `validating`, `waiting-input`, `awaiting-human`, `stuck`, `completed`, `error`) without deleting the existing three-layer state machine. 2026-05-16 also adds a real state-flip stabilizer for heuristic task/monitor transitions so single-sample flips are held until repeated or time-window evidence appears, while terminal error/completed paths remain immediate. 2026-05-16 tightens `stuck` detection to require both long stdout/activity silence and recent average CPU below 1%, matching the final acceptance stuck boundary. 2026-05-16 adds retry progress regression handling: exact real 0% CLI progress resets tracker estimates and shared progress derivation permits active coding progress from 0-99 when explicit runtime progress exists. 2026-05-16 adds long-task progress evidence: estimated 24h coding progress advances within a 30s window after the 10min mark while active cards tick every 1s. 2026-05-16 F.2.1 audit confirms continuous progress evidence and more than four fine-grained visible states. Focused detection/progress/contract/debounce/stuck/retry/long-task tests plus TypeScript passed. Rerun targeted gate before final closure. |
| `prompts\0503-2\R8.C\spec-29-feedback-loop-misreport.md` | R8.C | 0 | 10 | yes | 0 | verified | Spec-29 remains verified. 2026-05-16 adds explicit source citations and contribution percentages to the Signal Diagnostic Panel, then adds correct/incorrect detection feedback controls: `correct-detection` feedback is persisted through the existing local feedback bridge with bounded positive `user_feedback` adjustment, incorrect feedback maps to the existing false-state kinds, SQLite/fallback persistence remains real, and focused renderer/service/runtime tests plus TypeScript/lint/no-emoji passed. Rerun targeted gate before final closure. |
| `prompts\0503-2\R8.C\spec-30-notification-system.md` | R8.C | 0 | 11 | yes | 0 | verified | Keep evidence current; rerun targeted gate before final closure. |
| `prompts\0503-2\R8.C\spec-31-ipc-rate-limit.md` | R8.C | 0 | 10 | yes | 0 | verified | Keep evidence current; rerun targeted gate before final closure. |
| `prompts\0503-2\R8.C\spec-32-observability-panel.md` | R8.C | 0 | 11 | yes | 0 | verified | Keep evidence current; rerun targeted gate before final closure. |
| `prompts\0503-2\R8.C\spec-33-zod-source-of-truth.md` | R8.C | 0 | 10 | yes | 1 | verified | All checklist items are checked; the single pending marker is the schema field name `pendingMigrations`, not unfinished work. Verified with `pnpm -C devhub check:zod-sot` and targeted schema/preload/service Vitest on 2026-05-11. |
| `prompts\0503-2\R8.C\spec-34-crash-recovery.md` | R8.C | 0 | 10 | yes | 6 | verified | Crash recovery closure verified: AppLifecycle marker, isolated 7-finding scanner, SQLite integrity detection, orphan SHIM detection, RecoveryDialog path, mandatory pre-recovery snapshot, default-ON feature flag, injected audit sink, 2s default probe timeout, ten-sample real filesystem P95 benchmark under budget, and Playwright Electron relaunch GWT through real `csv:enqueue-row`, `recovery:check-dirty`, R8 Ops `RecoveryDialog`, and `recovery:restore-state` pre-recovery snapshot. |
| `prompts\0503-2\R8.C\spec-35-backup-restore.md` | R8.C | 0 | 11 | yes | 0 | verified | Backup/restore closure verified: four classified categories, SHA256 manifest guard, redaction, real ZIP output, default-off `node-cron@4.2.1` schedule with `noOverlap` and lifecycle cleanup, scheduled-only retention pruning, auto-disable after five schedule failures, `BackupManifest` 0.9.0 migration via `SchemaMigration`, RestoreWizard four-checkbox UI with destructive confirmation, CI backup-content ZIP scan, package build, focused Vitest, and packaged Electron Playwright GWT all passed. Keep evidence current before final all-doc closure. |
| `prompts\0503-2\R8.C\spec-36-diagnostic-pack-export.md` | R8.C | 0 | 11 | yes | 0 | verified | Diagnostic pack closure verified: default redaction rules, custom redaction, preview redaction counts, screenshot default-off, hostname/username redaction, fake key redaction, local manifest audit logging, default-ON feature flag, IPC/schema/preload contracts, network-monitor regression proving preview/export make 0 Node http/https/net client calls, R8OpsPanel real preview/export status surface, and Playwright artifact GWT (`pnpm -C devhub test:e2e --grep "R8.C spec-36" --reporter=line`) all passed. |
| `prompts\0503-2\R8.C\spec-37-permissions-time-bounded.md` | R8.C | 0 | 9 | yes | 0 | verified | Permission TTL closure verified: 8-op policy contract, medium-query check channel, confirmed revoke-all schema, request/check/revoke/revoke-all/configure/expiry-stream audit events, expiry-stream payload assertion, 1Hz stream throttle/cache plus request/revoke invalidation regression coverage, R8OpsPanel CountdownBadge mm:ss 1s repaint with red-under-1min, default-ON feature flag, targeted TTL Vitest, IPC/schema/preload coverage, CountdownBadge Vitest, production Electron launch after dagre CJS compatibility fix, and Playwright permission UI GWT (`pnpm -C devhub test:e2e --grep "R8.C spec-37" --reporter=line`) all passed. |
| `prompts\0503-2\R8.C\spec-38-skill-cloud-sync-deferred.md` | R8.C | 0 | 8 | yes | 0 | verified | Cloud sync deferred closure verified: centralized Zod SoT contracts, disabled/deferred IPC/preload/runtime facade, R8 Ops disabled tooltip row, refused audit events for all cloud-sync calls, R9 user doc, no-cloud dependency guard, targeted runtime/schema/feature tests, and typecheck passed. |
| `prompts\0503-2\R8.C\spec-39-ocr-interface-disabled.md` | R8.C | 0 | 8 | yes | 0 | verified | OCR disabled closure verified: centralized Zod SoT contracts, hard-disabled IPC/preload/runtime facade, R8 Ops disabled tooltip row, refused audit events for all OCR calls, user doc stating OCR is unsupported in R8, no-OCR dependency guard, targeted runtime/schema/feature tests, and typecheck passed. |

## First Implementation/Verification Gaps

1. `prompts/0503-2/R8.B/spec-01-port-popout-system.md` now has 0 open checklist items and is verified after the real Electron Playwright trigger/cap/position-memory/sync matrix passed with 7 tests. The remaining multi-display BrowserWindow assertions remain scoped to the partial `R8.B/spec-02-port-floating-window.md` row.
2. `prompts/0503-2/R8.C/spec-18-auto-inject.md` and `spec-19-auto-inject-targets.md` are now verified inject rows. Existing dry-run/selector/sandbox/countdown stream/failure classifier/chunked sendinput/rate-limit/SQLite audit/clipboard bridge/foreground focus polling/SHIM meta-command control/ordinary pty SHIM stdin/pre-after screenshot/post-inject pty echo verification, task-start trigger, watchdog restart trigger, UIA editable-control readback, and packaged Electron target-management E2E evidence is real. Remaining R8.C partial rows are watchdog subprocess and the aggregate PRD follow-up.
3. R8.B `spec-09-window-thumbnail-wall.md` is now verified after native capture, executable IPC, alias persistence, Electron E2E, `ASSERT_THUMBNAIL_WALL_GROUP_KEY`, real spec-11 COM-backed `desktopId` propagation, target-size Win32/GDI thumbnail capture, rapid-refresh VD metadata caching, and the 2026-05-19 release-scale 100-HWND benchmark artifact with p95/p99 under budget. `spec-11-window-virtual-desktop.md` now has 0 open checklist items after the BrowserWindow popout display-affinity migration/restore slice and repeated-query p95 6.3ms benchmark closure, and the live `R8B_SPEC11_TRUE_VD_SWITCH_EVENT_READY` gate is now passed after creating a second real Windows virtual desktop and enabling `DEVHUB_R8_VD_FOREGROUND_WATCH=1`; it remains partial only for live physical monitor disconnect/reconnect hardware verification.
4. `prompts/0503-2/R8.C/spec-16-watchdog-engine.md` is now verified after watchdog policy/action-executor/benchmark/kill-matrix closure, 2026-05-19 real OS collector adapter evidence for all nine heartbeat sources, and the configurable max-memory restart path. `spec-17-watchdog-subprocess.md` remains partial after supervisor contract closure, the 2026-05-18 named-pipe/TCP/marker three-channel slice, the child event-pipe parent-to-child ping/orphan-refusal slice, the parent heartbeat scheduler closure, real built-entry subprocess RSS/CPU benchmark evidence, real registered-instance orphan carryover evidence, real restart takeover evidence, full lifecycle audit-row evidence, renderer supervisor event-stream evidence, 2026-05-19 packaged kill/orphan E2E closure, and 2026-05-20 `WindowsServiceInstaller` `execFile` import/typecheck hardening plus runtime/IPC supervisor-control regression; Windows Service UAC execution is still not fully closed.

## Tooling Notes

- 2026-05-21 BrowserWindow second-display and external gate hardening: `devhub/scripts/verify-browserwindow-second-display.mjs` now provides a real Electron `BrowserWindow` placement verifier for `ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, writing `devhub-browserwindow-second-display-v1` reports and exposing `check:browserwindow-second-display`, `check:browserwindow-second-display:preflight`, and `check:browserwindow-second-display:self-test`. `verify-r8-external-blockers.mjs` now requires that report instead of static display enumeration, requires fresh physical hotplug and zero-egress capture reports for their gates, and requires `admin.isAdministrator=true`, `service.installed=true`, `service.scExitCode=0`, and a real service status for the Windows Service gate. `verify-0503-owner-evidence.mjs` validates submitted BrowserWindow reports and rejects primary-display or mismatched-display placement reports. Current local truth remains blocked: one display only, BrowserWindow preflight exit 2, no physical hotplug report, no zero-egress capture report, non-admin shell, service `scExitCode=1060`. This removes weak closure paths without closing any external/owner blocker.
- 2026-05-21 R8 runbook external blocker wording sync: `devhub/docs/r8/window-vd.md` and `devhub/docs/r8/watchdog-supervisor.md` now describe the current external blocker report with `legalDecisionConfirmed=false`, `legalDecisionEvidenceExists=false`, and `legalDecisionEvidenceValid=false`, and they point physical monitor closure at `pnpm -C devhub check:physical-monitor-hotplug` with a real two-display baseline plus removal and reconnection observation. This removes stale wording that only said "no explicit legal decision evidence" and could hide the stricter content-validation gate. Validation passed with devhub no-emoji, targeted diff-check, and direct grep samples.
- 2026-05-21 external blocker report schema lock: `verify-0503-evidence-pack.mjs` now validates the new license decision evidence validity fields in `r8-external-blockers-current.json`. It requires boolean `projectLicense.legalDecisionEvidenceValid`, rejects a missing evidence file being marked valid, requires boolean checks for `utf8Text`, `licensePosture`, `afferoPosture`, `decisionStatement`, and `ownerIdentified`, and verifies the failed `R8_LICENSE_LEGAL_DECISION_RECORDED` gate evidence string includes `legalDecisionEvidenceValid=<value>`. The evidence-pack self-test includes a missing-field negative case. This locks the external report schema without closing the legal-product owner gate.
- 2026-05-21 local gate coverage update: root `check:0503-local` now includes `pnpm -C devhub check:r8-external-blockers:self-test`, and `verify-0503-evidence-pack.mjs` locks the exact root package script with that command included. This keeps the license external blocker content-validation self-test inside the normal local R8 regression chain instead of relying on a standalone command. Validation passed with evidence-pack self-test, the devhub self-test, evidence pack verification, and full `pnpm --silent check:0503-local` exit 0. This is local regression coverage only and does not close external/owner blockers.
- 2026-05-21 external blocker self-test hardening: `devhub/scripts/verify-r8-external-blockers.mjs --self-test` now runs a low-resource temporary-directory fixture suite for the license decision evidence validator, covering missing, weak, binary-like, and valid evidence text. `devhub/package.json` exposes this as `check:r8-external-blockers:self-test`. The self-test locks `legalDecisionEvidenceValid` and the five content checks without writing fake owner evidence into the repository. Validation passed with `node --check`, the new devhub self-test script, a real external blocker report refresh, acceptance pack `--no-refresh`, and evidence-pack verification. This is verifier regression coverage only and does not close the legal-product blocker.
- 2026-05-21 license external gate content validation: `devhub/scripts/verify-r8-external-blockers.mjs` no longer treats `DEVHUB_R8_LICENSE_DECISION_EVIDENCE` as valid merely because the file exists. The report now includes `projectLicense.legalDecisionEvidenceValid`, `legalDecisionEvidenceChecks`, and `legalDecisionEvidenceError`, and `R8_LICENSE_LEGAL_DECISION_RECORDED` only passes when the UTF-8 evidence file explicitly names `AGPL-3.0-or-later`, GNU Affero posture, decision/approval language, and legal/product ownership. The current real report still has `legalDecisionConfirmed=false`, `legalDecisionEvidenceExists=false`, and `legalDecisionEvidenceValid=false`, so this hardens the legal-product gate without closing it.
- 2026-05-21 Windows Service owner handoff hardening: `R8C_SPEC17_WINDOWS_SERVICE_INSTALLED` now has an action-specific `verificationCommandNote` propagated into `0503-owner-action-queue.json`, owner action dossier output, raw evidence template output, submission template output, and the current template JSON directories. The note requires a real Administrator Windows session, the live DevHub preload/IPC install path `window.devhub.watchdog.supervisorInstallService(true, '<real operator identity>')`, UAC acceptance, and a fresh `check:r8-external-blockers` report showing `admin.isAdministrator=true`, `service.installed=true`, `service.scExitCode=0`, and a real `service.status`. `devhub/docs/r8/watchdog-supervisor.md` now mirrors this as a seven-step owner checklist. Evidence pack and owner-evidence self-tests lock this propagation. This is owner intake hardening only and does not close the non-admin/non-installed strict blocker.
- 2026-05-21 zero-egress owner handoff hardening: `H1_J16_ZERO_EGRESS_CAPTURE_READY` now has an action-specific `verificationCommandNote` propagated into `0503-owner-action-queue.json`, owner action dossier output, raw evidence template output, submission template output, and the current template JSON directories. The note requires a real Administrator Windows shell, `pktmon.exe`, `pnpm -C devhub check:zero-egress-capture` without `--preflight`, and a `devhub-zero-egress-capture-v1` JSON report with `blocked=false`, `passed=true`, `durationSeconds>=60`, `packetCount=0`, and `preflight.ready=true`. Evidence pack locks the action/template consistency, and a direct dossier sample confirmed the note appears in action/raw/submission positions. This is owner intake hardening only and does not close the current non-admin zero-egress strict blocker.
- 2026-05-21 physical-monitor hotplug verifier hardening: `devhub/scripts/verify-physical-monitor-hotplug.mjs` now provides a real Windows polling verifier for `R8B_SPEC11_PHYSICAL_MONITOR_DISCONNECT_RECONNECT_READY`, with `--self-test`, `--preflight`, bounded duration/interval options, and `devhub-physical-monitor-hotplug-v1` JSON output. `devhub/package.json` exposes `check:physical-monitor-hotplug`, `check:physical-monitor-hotplug:preflight`, and `check:physical-monitor-hotplug:self-test`; `verify-r8-external-blockers.mjs` now points the physical-monitor runbook at the real hotplug command rather than static blocker enumeration. `verify-0503-owner-evidence.mjs` validates submitted hotplug reports as fresh, non-blocked, passed, two-display baseline reports with removal and reconnection observed. Current local preflight truth is still `displayCount=1; ready=false`, so this creates a truthful closure path only and does not close the hardware blocker on the current machine.
- 2026-05-21 hardware/admin owner handoff hardening: `ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, `R8B_SPEC11_PHYSICAL_MONITOR_DISCONNECT_RECONNECT_READY`, and `R8C_SPEC17_ADMIN_SHELL` now have action-specific `verificationCommandNote` values propagated into `0503-owner-action-queue.json`, action dossier output, raw evidence template output, submission template output, and current template JSON directories. The second-display note now requires a real `devhub-browserwindow-second-display-v1` BrowserWindow placement report rather than display enumeration alone; the physical-monitor note explicitly says the external blocker report is only a multi-display precondition and must be paired with real unplug/replug owner trace or operator evidence; the admin-shell note requires `admin.user` plus `admin.isAdministrator=true` from the same elevated Windows session. Evidence pack locks these notes and three direct dossier samples confirmed action/raw/submission propagation. This is owner intake hardening only and does not close hardware/admin strict blockers.
- 2026-05-21 completion-status source-file dossier continuation command: `0503-completion-status` now includes a top-level owner source-file dossier query command, making the status overview sufficient to discover source-file intake without first opening owner action queue Markdown. Evidence-pack verifier locks both JSON and Markdown surfaces. This is diagnostic discoverability only and does not close any strict blocker.
- 2026-05-21 owner-output-matrix source-file dossier coverage: owner output matrix now recursively checks source-file command sets inside `sourceFiles.files[]`, so one command can prove that all owner-facing JSON surfaces expose source-level intake commands as well as action-level commands. Current real output reports `gapCount=0`. This is diagnostic discoverability only and does not close any strict blocker.
- 2026-05-21 owner closure bundle source-file dossier propagation: owner closure bundles now preserve source-file rows and their source-file dossier commands for checkbox-closure actions, not just action-level dossier/template commands. This keeps `--owner-closure-bundles --owner <owner>` sufficient for owner handoff when a blocker is tied to one or more prompt source files. Evidence-pack and owner-evidence self-tests now lock the propagation. This is diagnostic intake discoverability only and does not close any strict blocker.
- 2026-05-21 owner source-file dossier command hardening: owner action queue source-file rows now expose `sourceFileDossierCommand` alongside action dossier, raw evidence template, and submission template commands. `verify-0503-owner-evidence.mjs --source-file-dossier --action <actionId> --file <prompt-file>` gives a direct machine-readable row for one prompt/source file without treating the source dossier as closure evidence; `verify-0503-evidence-pack.mjs` locks this JSON and Markdown contract, and `check:0503-local` includes a low-resource sample against `prompts/0503/14-three-graph-systems-survey.md`. This is diagnostic intake discoverability only and does not close any strict blocker.
- 2026-05-21 evidence pack root script drift repair: `verify-0503-evidence-pack.mjs` now keeps `verifyRootPackageScripts()` aligned with the current `package.json` `check:0503-local` command by requiring the owner output matrix query (`pnpm check:0503-owner-evidence -- --owner-output-matrix`) in the exact local gate assertion. Before this repair, `package.json` already contained the command but the verifier exact string omitted it, so `pnpm --silent check:0503-evidence-pack` failed before reaching the real strict blocker boundary. Validation passed with `node --check`, evidence pack self-test, evidence pack, no-emoji, targeted whitespace, `pnpm --silent check:0503-local` (`LOCAL_EXIT=0`), and the authoritative `pnpm --silent check:0503-strict:vd-watch`; strict remains non-complete for the same external/owner gates only.
- 2026-05-21 completion guard owner crosswalk command-set hardening: `0503-completion-audit.json` `completionGuardOwnerCrosswalk` now carries action dossier, raw evidence template, and submission template commands in both singular and array fields for linked owner actions. The completion audit Markdown `Completion Guard Owner Crosswalk` table now renders the three command arrays. A recursive JSON artifact audit across the active task research directory confirmed `linkedOwnerActionCommandGaps=[]` and `gapCount=0`. This is machine-readability hardening only and does not close any strict blocker.
- 2026-05-21 completion audit residual owner/action command-set hardening: `0503-completion-audit.json` `promptToArtifactChecklist` prompts/0503-2 rows, `partialR8Dossier`, `ownerActionGuardBacklinks`, and owner closure bundle normalized `blockingTaxonomyRows` now all expose action dossier, raw evidence template, and submission template command arrays for linked owner actions. The completion audit Markdown `Owner Action Guard Backlinks` and `Partial R8 Dossier` tables now render the same command sets, and `verify-0503-evidence-pack.mjs` locks the JSON/Markdown/self-test/closure-bundle contracts. Validation passed with `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, `pnpm --silent check:0503-no-emoji`, and a direct recursive JSON audit reporting `gapCount=0`. This is diagnostic intake discoverability only and does not close any strict blocker.
- 2026-05-21 residual command-set validation closure: targeted `git diff --check` passed for the active task/package scope, direct whitespace scan over the changed scripts/docs/artifacts reported `bad=[]`, and `pnpm --silent check:0503-local` passed. The authoritative `pnpm --silent check:0503-strict:vd-watch` still exits 1 with `partialRows=5`, `missingEvidenceRows=0`, `failedExternalGateIds=6`, `surveyAcceptanceRows=3`, `externalReportFresh=true`; remaining blockers are unchanged real external/owner evidence requirements and cannot be closed by generated templates, owner readiness output, or acceptance-pack regeneration.
- 2026-05-21 owner closure bundle Markdown command-set hardening: `0503-owner-closure-bundles.md` owner-level taxonomy and action tables now render action dossier, raw evidence template, and submission template command columns. This closes the presentation drift where JSON owner closure bundles already carried complete command sets but Markdown closure bundles still exposed only the dossier command in some rows. `verify-0503-evidence-pack.mjs` now asserts those Markdown rows and its command-checklist requirement names the action/raw/submission columns. Validation passed with `node --check`, `pnpm --silent check:0503-evidence-pack:self-test`, `generate-0503-acceptance-pack.mjs --no-refresh`, `pnpm --silent check:0503-evidence-pack`, and `pnpm --silent check:0503-no-emoji`. This is Markdown discoverability only and does not close any strict blocker.
- 2026-05-21 historical Markdown command-set wording cleanup: a direct scan across 15 research Markdown files looked for owner/action-linked dossier rows that did not also mention raw evidence template and submission template coverage. It found older ledger/scope-audit wording that still described only the action dossier slice, so those notes now either name the full action/raw/submission command set or state that a single local sample is complemented by owner output matrix and evidence-pack command-set checks. The repeat scan returned `gapCount=0`. This is history/readability cleanup only and does not close any strict blocker.
- 2026-05-21 completion audit success criteria owner crosswalk hardening: aggregate blocked success criteria now expose linked owner actions and the same action/raw/submission command arrays used by lower-level blocker rows. `EXTERNAL_GATE_CLOSURE`, `R8_PARTIAL_ROW_CLOSURE`, `SURVEY_ACCEPTANCE_CLOSURE`, `OWNER_ACTION_QUEUE_CLOSURE`, and `STRICT_COMPLETION_GATE` now have non-zero owner action command counts matching their blocker scope, and the `Requirement Coverage` Markdown table renders those columns. `verify-0503-evidence-pack.mjs` checks the expected owner action ids, owner owners, command arrays, owner-readiness evidence-dir commands, and Markdown row rendering. Validation passed with `node --check`, evidence-pack self-test, acceptance pack `--no-refresh`, evidence pack, no-emoji, and a direct blocked-criteria command-count audit. This is aggregate handoff hardening only and does not close any strict blocker.
- 2026-05-21 completion audit blocker taxonomy command-set hardening: `0503-completion-audit.json` `blockerTaxonomy.rows` now carries action dossier command arrays, raw evidence template command arrays, and submission template command arrays from the strict blocker crosswalk. The completion audit Markdown `Taxonomy Rows` table now renders those command arrays, and `verify-0503-evidence-pack.mjs` locks the JSON and Markdown shape. A direct audit confirmed `taxonomyCommandSetGaps=[]`. This is diagnostic taxonomy hardening only and does not close any strict blocker.
- 2026-05-21 completion audit owner command-set hardening: `0503-completion-audit.json` `missingOrIncompleteRequirements` and `strictBlockerCrosswalk` now carry linked owner action ids, action dossier commands, raw evidence template commands, submission template commands, verification commands, strict completion command, and recommended strict command. The completion audit Markdown now renders these fields in `Missing Or Incomplete Requirements` and `Strict Blocker Crosswalk`; `verify-0503-evidence-pack.mjs` locks the JSON command arrays, singular command nullability for multi-action rows, and Markdown rows. A direct audit confirmed no missing command-set gaps in either open requirements or crosswalk rows. This is diagnostic handoff hardening only and does not close any strict blocker.
- 2026-05-21 owner action queue Markdown action-table command-set hardening: `0503-owner-action-queue.md` global `Actions` rows now render `Action dossier command`, `Raw evidence template command`, and `Submission template command` as separate columns instead of a generic single template column. `verify-0503-evidence-pack.mjs` now locks the table header and row-level raw/submission/action dossier commands against `0503-owner-action-queue.json`. This only improves owner intake discoverability and does not close any strict blocker.
- 2026-05-21 failed external gate Markdown command-set hardening: `0503-acceptance-pack.md` `Failed External Gate Actions` and `0503-strict-completion-report.md` `Failed External Gates` now render all three owner intake commands per failed gate: `actionDossierCommand`, `rawEvidenceTemplateCommand`, and `submissionTemplateCommand`. `generate-0503-acceptance-pack.mjs` renders the two new columns, `verify-0503-ledgers.mjs` self-test locks the strict-report header and full failed-gate row, and `verify-0503-evidence-pack.mjs` enforces both report headers plus row-level raw/submission template commands. This is intake discoverability only and does not close any strict blocker.
- 2026-05-21 strict failure summary command set hardening: `run-0503-strict-completion.mjs` now prints a `Failed external gate owner evidence command sets` section with `actionDossierCommand`, `rawEvidenceTemplateCommand`, and `submissionTemplateCommand` for every failed external gate, while preserving the existing non-zero strict boundary. The strict runner self-test and completion audit command checklist now lock the raw/submission template command coverage. This makes the failed gate handoff directly actionable without treating templates as evidence.
- 2026-05-21 owner action command set hardening: owner CLI surfaces now expose three machine-readable commands per action where applicable — `actionDossierCommand`, `rawEvidenceTemplateCommand`, and `submissionTemplateCommand`. `--list-actions`, `--action-dossier`, `--blocker-taxonomy`, and `--owner-readiness` now surface the same three-command set for linked actions and taxonomy rows; the self-test now locks list-actions, dossier, taxonomy, and readiness outputs with no missing command fields. Evidence pack, no-emoji, and strict `vd-watch` were rerun after regeneration; strict still exits non-zero only because the remaining hardware/admin/service/network/license/owner gates need real external evidence. This reduces intake ambiguity only and does not close any strict blocker.
- 2026-05-21 owner blocker taxonomy / readiness machine contract hardening: `verify-0503-owner-evidence.mjs --blocker-taxonomy --owner <owner>` now emits the linked `actionDossierCommand`, `rawEvidenceTemplateCommand`, and `submissionTemplateCommand` for taxonomy rows with an owner action link, and `--owner-readiness --owner <owner>` now surfaces the same command set in both top-level and owner-level `blockingTaxonomyRows`. Owner self-test locks blocker taxonomy output plus readiness output with and without evidence-dir coverage, and the completion audit checklist states that owner readiness and owner blocker taxonomy cover the full command set. A direct current run confirmed operator taxonomy/readiness rows carry `pnpm --silent check:0503-owner-evidence -- --action-dossier --action ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, `--print-evidence-template --action ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, and `--print-template --action ASSERT_BROWSERWINDOW_SECOND_DISPLAY` for the linked gate row. This is a diagnostic discoverability improvement only and does not close any strict blocker.
- 2026-05-21 owner action list/dossier machine contract hardening: `verify-0503-owner-evidence.mjs --list-actions` now emits `actionDossierCommand`, `rawEvidenceTemplateCommand`, and `submissionTemplateCommand` for every action, and `--action-dossier --action <actionId>` emits the same command set at both the top level and the main `action` payload. Owner self-test locks unfiltered, action-filtered, and owner-filtered list-actions output plus action/raw/submission command payloads; completion audit requirements state that the owner action list covers the full command set, and `verify-0503-evidence-pack.mjs` enforces the updated requirement. A direct current run confirmed 11/11 list actions include the three-command set and the second-display dossier includes `pnpm --silent check:0503-owner-evidence -- --action-dossier --action ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, `--print-evidence-template --action ASSERT_BROWSERWINDOW_SECOND_DISPLAY`, and `--print-template --action ASSERT_BROWSERWINDOW_SECOND_DISPLAY` in machine-readable locations. This is an intake discoverability improvement only and does not close any strict blocker.
- 2026-05-21 owner evidence hash algorithm explicitness: `--hash-evidence` now emits `hashAlgorithm=sha256` beside `evidenceSha256`, size, mtime, boundary, and strict commands. Self-test and the completion audit checklist now require the algorithm field, preventing machine consumers from inferring the digest algorithm from a field name. This is evidence intake clarity only and does not close any strict blocker.
- 2026-05-21 owner evidence submission schema strictness: no existing non-template owner submission JSON files were found under the active task before the change, so `validateSubmission()` now requires `schemaVersion=devhub-0503-owner-evidence-submission-v1` instead of accepting missing schemaVersion. Self-test now rejects missing and wrong submission schemaVersion and all successful fixtures include the explicit schema. The completion audit command checklist now names required schemaVersion matching. This raises intake strictness without closing any strict blocker.
- 2026-05-21 owner template directory output hardening: `--print-template-dir` and `--print-evidence-template-dir` now return versioned directory-level machine contracts with `templateOnly=true`, scaffolding-only boundaries, and strict/recommended strict commands. Generated template directory README files also include the matching directory schemaVersion. The owner self-test locks both directory schema versions, boundaries, and README schema text, and the acceptance-pack/evidence-pack command checklist now names template directory schemaVersion boundaries. This prevents generated template directory output from being mistaken for owner evidence and does not close any strict blocker.
- 2026-05-21 owner evidence coverage metadata hardening: owner evidence coverage Markdown and JSON reports now carry `evidenceModifiedAt` and `evidenceSizeBytes` for every submitted action, using the same real file stat fields validated by the owner evidence verifier. Self-test asserts the metadata appears in both coverage artifacts, and the acceptance-pack workflow now states that coverage reports include mtime/size metadata while remaining intake checklists only. This improves reviewability of future owner submissions but does not close any strict blocker.
- 2026-05-21 owner evidence hash metadata hardening: `verify-0503-owner-evidence.mjs --hash-evidence` now emits `evidenceModifiedAt` and `evidenceSizeBytes` from the real file stat in addition to schemaVersion, boundary, SHA-256, and strict commands. Self-test locks text and binary evidence file sizes plus mtime output, and the acceptance-pack owner workflow tells owners to preserve file path, size, and mtime fields when copying digest output. A real temporary evidence-file probe confirmed schema, mtime presence, size, digest shape, and shell-portable strict command. This only improves evidence traceability; it does not close any strict blocker.
- 2026-05-21 owner evidence hash/validation contract hardening: `verify-0503-owner-evidence.mjs` now prints versioned, boundary-bearing machine contracts for `--hash-evidence`, `--evidence`, and `--evidence-dir`. Hash output uses `devhub-0503-owner-evidence-hash-v1`; single-submission validation uses `devhub-0503-owner-evidence-validation-v1`; directory validation uses `devhub-0503-owner-evidence-directory-validation-v1`. Each output carries strict/recommended strict commands and explicitly states it is not completion evidence. The owner self-test locks the hash schema/boundary plus validation output builders, the acceptance-pack command checklist now names evidence hash and validation schemaVersion boundaries, and `verify-0503-evidence-pack.mjs` enforces those checklist strings. A real temporary evidence-file probe confirmed schema, boundary, shell-portable strict command, and a valid 64-character SHA-256 digest. This is tooling safety only; it does not close any owner, hardware, admin, service, zero-egress, or license/legal gate.
- 2026-05-21 owner evidence CLI contract hardening: `verify-0503-owner-evidence.mjs` now prints versioned machine contracts for owner intake outputs: `--list-actions` includes `schemaVersion=devhub-0503-owner-action-list-v1`, strict/recommended strict commands, and an intake-only boundary; `--owner-summary` includes `schemaVersion=devhub-0503-owner-summary-v1`, strict/recommended strict commands, and the existing strict-authoritative boundary. The owner self-test now locks both schema versions, and current queue samples passed for `operator` list-actions and `product` owner-summary. This improves owner handoff safety only; it does not close any missing owner evidence.
- 2026-05-21 verifier hardening: `verify-0503-evidence-pack.mjs` now directly validates `0503-survey-acceptance-ledger.md` Markdown content against `0503-ledger-verification.json` and `0503-acceptance-pack.json`, including 34 prompts/0503 file rows, 1301 open checkboxes, 104 checked checkboxes, 1003 question markers, status/evidenceStatus counts, and the three owner/user/legal acceptance rows. This is an evidence-locking improvement only; it does not close any external owner or hardware gate.
- `teamCreate` was not available in this runtime; a `trellis-research` agent was attempted but did not produce an artifact before timeout, so this ledger was generated in the main session to protect machine resources.
- GitNexus/Serena/ABCoder MCP calls were not usable in this session (`Session terminated` or tool unavailable), so status uses local repository files, reports, and command output.
- This ledger is a planning and verification artifact only; it does not mark source specs complete by itself.
