# prompts/0503 Survey and Acceptance Ledger

Date: 2026-05-19

## Objective

Audit `prompts/0503` separately from the active `prompts/0503-2` R8 implementation ledger.

This file does not mark `prompts/0503` complete. It records why the open checkboxes in `prompts/0503` are not directly closable by implementation changes in the current task.

## Summary

| Scope | Files | Open checkboxes | Checked checkboxes | Question markers | Current role |
|---|---:|---:|---:|---:|---|
| `prompts/0503` | 34 | 1301 | 104 | 1003 | Upstream survey plus final user acceptance context |
| `prompts/0503-2` | 81 | tracked in `0503-2-completion-ledger.md` | tracked in `0503-2-completion-ledger.md` | n/a | Executable R8 PRD/spec implementation contract |

## Contract Interpretation

- `prompts/0503/00-overview.md` declares the round as a requirements survey round and states that this round does not write source code, PRDs, or specs.
- `prompts/0503/00-master-v2.md` declares the V2 round as a survey refinement round and states that the later R8.A/R8.B/R8.C specs live in `prompts/0503-2`.
- `prompts/0503/28-final-acceptance-checklist.md` is a final user acceptance checklist. It depends on V1, V2, and R8.A/B/C completion, and it requires user verification rather than code-only closure.

## File Ledger

| Path | Open | Checked | Question markers | Evidence status | Next action |
|---|---:|---:|---:|---|---|
| `prompts\0503\00-master-v2.md` | 0 | 0 | 0 | context | Keep as upstream V2 map. Do not treat as implementation-complete evidence. |
| `prompts\0503\00-overview.md` | 0 | 0 | 2 | context | Keep as upstream V1 map. It explicitly routes implementation into later PRD/spec phases. |
| `prompts\0503\01-meta-vision-survey.md` | 0 | 0 | 26 | survey | Preserve answered requirements as upstream context for `prompts/0503-2`. |
| `prompts\0503\02-global-experience-survey.md` | 0 | 0 | 29 | survey | Preserve answered requirements as upstream context for global UX specs. |
| `prompts\0503\03-theme-design-language-survey.md` | 0 | 0 | 27 | survey | Preserve answered requirements as upstream context for theme specs. |
| `prompts\0503\04-process-module-survey.md` | 24 | 27 | 22 | survey-partial | Do not close survey checkboxes by code. Map unresolved process choices only if a new PRD/spec task is opened. |
| `prompts\0503\05-port-module-survey.md` | 13 | 15 | 22 | survey-partial | Do not close survey checkboxes by code. Current executable port work remains tracked under `prompts/0503-2/R8.B`. |
| `prompts\0503\06-window-module-survey.md` | 25 | 4 | 19 | survey-partial | Do not close survey checkboxes by code. Current executable window work remains tracked under `prompts/0503-2/R8.B`. |
| `prompts\0503\07-ai-task-orchestration-survey.md` | 74 | 21 | 47 | survey-partial | Do not close survey checkboxes by code. Current executable AI task work remains tracked under `prompts/0503-2/R8.C`. |
| `prompts\0503\08-topology-flow-attached-survey.md` | 20 | 5 | 25 | survey-partial | Do not close survey checkboxes by code. Current executable topology work remains tracked under `prompts/0503-2/R8.C`. |
| `prompts\0503\09-cross-cutting-survey.md` | 5 | 25 | 26 | survey-partial | Preserve answered cross-cutting choices; unresolved items need explicit PRD/spec conversion. |
| `prompts\0503\10-integration-libraries-survey.md` | 0 | 0 | 38 | survey | Library choices are upstream context. Current implementation evidence remains in `prompts/0503-2` ledger rows. |
| `prompts\0503\11-roadmap-rollback-survey.md` | 58 | 7 | 20 | survey-partial | Do not close roadmap survey items by code. Use only as sequencing context unless user reopens planning. |
| `prompts\0503\12-cross-module-jump-survey.md` | 70 | 0 | 50 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\13-perception-vs-reality-survey.md` | 35 | 0 | 37 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\14-three-graph-systems-survey.md` | 102 | 0 | 53 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\15-ai-detection-zero-error-survey.md` | 28 | 0 | 41 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\16-csv-task-driver-deep-survey.md` | 68 | 0 | 52 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\17-watchdog-engineering-survey.md` | 55 | 0 | 43 | survey-open | V2 survey choices are not implementation completion evidence. Current watchdog spec closure remains under `prompts/0503-2/R8.C/spec-17-watchdog-subprocess.md`. |
| `prompts\0503\18-auto-inject-engineering-survey.md` | 66 | 0 | 52 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\19-popout-dock-engineering-survey.md` | 48 | 0 | 64 | survey-open | V2 survey choices are not implementation completion evidence. Current popout closure remains under `prompts/0503-2/R8.B`. |
| `prompts\0503\20-theme-quantitative-diff-survey.md` | 20 | 0 | 47 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\21-edge-case-failure-survey.md` | 9 | 0 | 61 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\22-user-journey-storyboard.md` | 30 | 0 | 33 | survey-open | Storyboard items need user or product acceptance evidence, not code-only closure. |
| `prompts\0503\23-extensibility-plugin-survey.md` | 54 | 0 | 54 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\24-legal-compliance-survey.md` | 38 | 0 | 31 | survey-open | Compliance survey choices are upstream context; implementation claims need separate legal/product acceptance evidence. |
| `prompts\0503\25-community-ecosystem-survey.md` | 67 | 0 | 33 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\26-market-best-practices-comparison.md` | 33 | 0 | 25 | survey-open | Market comparison items are benchmark context, not direct implementation closure. |
| `prompts\0503\27-easter-egg-shortcuts-survey.md` | 74 | 0 | 24 | survey-open | V2 survey choices are not implementation completion evidence. |
| `prompts\0503\28-final-acceptance-checklist.md` | 285 | 0 | 0 | user-acceptance-open | Requires real user-facing acceptance after R8.A/B/C completion. Do not mark by local tests alone. |
| `prompts\0503\99-research-snapshot.md` | 0 | 0 | 0 | reference | Preserve as historical source snapshot. |
| `prompts\0503\refs\market-research.md` | 0 | 0 | 0 | reference | Preserve as research context. |
| `prompts\0503\refs\source-snapshot-v2.md` | 0 | 0 | 0 | reference | Preserve as research context. |
| `prompts\0503\refs\spec-gap-analysis.md` | 0 | 0 | 0 | reference | Preserve as research context. |

## Prompt To Artifact Checklist

| Requirement | Source | Concrete evidence checked | Status |
|---|---|---|---|
| Distinguish `prompts/0503` from `prompts/0503-2` | `00-overview.md`, `00-master-v2.md`, current Trellis PRD | `0503-scope-audit.md` and this ledger | verified |
| Keep `prompts/0503` as upstream survey context | `00-overview.md`, `00-master-v2.md` | This ledger classifies survey files as survey/reference/user acceptance rather than implementation specs | verified |
| Continue executable implementation closure in `prompts/0503-2` | active task PRD | `0503-2-completion-ledger.md` remains the implementation ledger | verified |
| Do not close final user acceptance by proxy | `28-final-acceptance-checklist.md` | 285 acceptance items remain open because they require user-facing verification | verified |
| Do not claim hardware/admin/zero-egress/license-gated requirements | `prompts/0503-2/R8.B/spec-02`, `prompts/0503-2/R8.B/spec-11`, `prompts/0503-2/R8.C/spec-17`, `prompts/0503/24-legal-compliance-survey.md`, `prompts/0503/28-final-acceptance-checklist.md` | `check:r8-external-blockers` evidence recorded in `0503-2-completion-ledger.md`: one renderable Electron/BrowserWindow display, two registry virtual desktop IDs, `foregroundHookOptIn=true`, non-admin shell, `devhub-watchdog` not installed, zero-egress preflight blocked by `admin=false`, and no explicit legal-decision evidence for the current AGPL project license | verified |

## Acceptance Evidence Bridges

These rows connect individual `prompts/0503/28-final-acceptance-checklist.md` items to local implementation evidence without marking the user acceptance checkbox complete.

| Acceptance item | Local implementation evidence | Verification command | Evidence status |
|---|---|---|---|
| A.1.6 top navigation exposes process / port / window / AI task / topology | `devhub/src/renderer/components/monitor/index.tsx` defines top-level monitor tabs for `process`, `port`, `window`, `ai-task`, and `topology`; `devhub/src/renderer/components/monitor/MonitorPanel.test.tsx` asserts the five buttons and topology view activation. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/MonitorPanel.test.tsx --maxWorkers=1 -t "top-level monitor entries"` passed with 1 test. | locally verified, user acceptance still open |
| A.3.1 Cmd+K immediately opens the command palette | `devhub/src/renderer/App.tsx` registers `Ctrl+K` / `Meta+K` through `useGlobalShortcuts`; `devhub/src/renderer/hooks/useGlobalShortcuts.test.tsx` verifies matching shortcut dispatch and browser-default prevention. | `pnpm -C devhub exec vitest run src/renderer/components/command/R8CommandPalette.test.tsx src/renderer/components/command/command-search.test.ts src/renderer/hooks/useGlobalShortcuts.test.tsx src/renderer/components/statusbar/StatusBar.test.tsx src/main/services/R8RuntimeService.test.ts --maxWorkers=1 -t "command palette\|command search\|global shortcuts\|cmdk\|executable default commands\|theme switching\|history boost\|source groups\|R8CommandPalette\|useGlobalShortcuts\|StatusBar"` passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.2 command palette aggregates process / port / window / AI task / theme / settings / command sources | `R8RuntimeService.listCommands()` now includes executable process, port, window, AI, settings, topology, dashboard, diagnostic, and `theme.apply.*` commands; `R8CommandPalette.test.tsx` asserts visible source groups for process, port, window, AI, theme, settings, and command entries. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.3 fuzzy search recognizes Chinese, pinyin, and English | `R8CommandPalette.test.tsx` verifies typo-tolerant Fuse search, pinyin lookup for `liucheng`, and Chinese/English lookup for theme commands through the production search helper. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.4 recent history shows recent commands | `R8CommandPalette.test.tsx` verifies persisted `CommandHistoryEntry` rows render in `cmdk-group-history` with use count and invoke the selected command. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.5 intelligent sorting uses frequency plus recency decay | `devhub/src/renderer/components/command/command-search.ts` now applies persisted use-count boost plus relative recency decay; `command-search.test.ts` verifies equal text matches sort by history usage and recency. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.6 command groups display process / port / window / AI / setting categories | `R8CommandPalette.tsx` groups by command category and installed icon tokens; `R8CommandPalette.test.tsx` asserts process, port, window, AI, and settings groups with representative commands. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.7 leading `>` / `@` / `#` / `!` filters category scopes | `R8CommandPalette.tsx` parses scope prefixes and filters action, AI, object, and confirmation categories; `R8CommandPalette.test.tsx` verifies all four prefixes. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| A.3.8 Escape closes the command palette and returns focus | `R8CommandPalette.tsx` handles `Escape`; `R8CommandPalette.test.tsx` verifies the previously active element receives focus after close. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped. | locally verified, user acceptance still open |
| B.1.1 At least seven preset themes cover the required palettes | `THEME_PRESETS` now exposes real preset states for `constructivism`, `modern-light`, `warm-light`, `cyberpunk`, `swiss`, `dark`, and `light`; `SettingsDialog.tsx` renders these presets through the existing theme preset picker, and palette schemas already validate the same seven palette IDs. | `pnpm -C devhub exec vitest run src/renderer/theme/theme-language.test.ts src/renderer/theme/theme-distance.test.ts --maxWorkers=1` passed with 12 tests; the new theme-language assertion verifies all seven required preset palettes are present. | locally verified, user acceptance still open |
| B.1.2 theme switching is available from Settings and command palette | `SettingsDialog.tsx` already applies theme changes through `useTheme`; `R8RuntimeService.ts` now registers `theme.apply.*` commands, emits `theme-apply`, and `App.tsx` validates the palette with `paletteNameSchema` before calling `setTheme`. | Same command-palette targeted Vitest run passed with 32 tests and 128 skipped; `pnpm -C devhub exec eslint ... --max-warnings=0 && pnpm -C devhub typecheck && pnpm -C devhub check:no-emoji` passed. | locally verified, user acceptance still open |
| B.1.3 theme switching applies immediately without waiting for slow font preload | `useTheme()` writes `data-theme`, `data-palette`, and axis datasets before awaiting `preloadFontsForTheme()` or settings persistence; `useTheme.test.tsx` installs a never-resolving Font Loading API and verifies the selected theme is already on `document.documentElement` before the 1500ms timeout completes. `e2e/example.spec.ts` now samples 40 real `requestAnimationFrame` frames while switching from `modern-light` to `cyberpunk` in the running Electron Settings dialog and fails if the app shell loses area, display, visibility, opacity, or real body content during the transition. | `pnpm -C devhub exec vitest run src/renderer/hooks/useTheme.test.tsx src/renderer/theme/theme-language.test.ts src/renderer/theme/theme-distance.test.ts --maxWorkers=1` passed with 17 tests; `pnpm -C devhub exec tsc --noEmit --pretty false` passed. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with the visual-continuity frame sampling and restart persistence path. | locally verified with Electron visual-continuity evidence, user visual acceptance still open |
| B.1.4 explicit theme choices persist for restart restore | `useTheme().setTheme()` now has direct regression coverage proving it calls the real `window.devhub.settings.update({ appearance: ... })` path after applying the resolved palette, density, radius, and motion axes. `e2e/example.spec.ts` also persists the Paper Zen preset (`warm-light`, `comfortable`, `round`, `reduced`), closes the first Electron app, launches a second Electron app, and verifies the restored four-axis appearance state from the real settings store. | Same theme targeted Vitest run passed with 17 tests; targeted ESLint for `scripts/verify-theme-token-contract.mjs` and `src/renderer/hooks/useTheme.test.tsx` passed with `--max-warnings=0`. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with restart persistence evidence. | locally verified with Electron restart evidence, user restart acceptance still open |
| B.1.5 System theme following is available as an optional setting | `AppearanceSettings.followSystemTheme` is now persisted through shared settings, accepted by IPC sanitization, carried through popout theme sync schema, exposed in Settings as a "follow system theme" toggle, and resolved by `useTheme()` from real `matchMedia('(prefers-color-scheme: light/dark)')` state. Manual palette selection disables follow-system mode so user intent remains explicit. | `pnpm -C devhub exec vitest run src/renderer/theme/theme-language.test.ts src/renderer/theme/theme-distance.test.ts src/renderer/hooks/useTheme.test.tsx src/shared/schemas/r8-runtime.test.ts --maxWorkers=1 -t "theme\|Theme\|follow-system\|popout theme"` passed with 16 tests and 25 skipped; `pnpm -C devhub exec tsc --noEmit --pretty false`, targeted ESLint, `check:no-emoji`, and `check:zod-sot` passed. | locally verified, user acceptance still open |
| B.2.1 theme color changes are real full-palette changes | `colors.css` now has real `[data-theme="dark"]` and `[data-theme="light"]` color token blocks in addition to the existing five palettes; `verify-theme-token-contract.mjs` parses actual CSS and requires every theme pair to differ on the color signature built from surface, text, primary, secondary, and tertiary tokens. | `pnpm -C devhub check:theme-token-contract` passed: 7 palettes, 21 pairwise comparisons, minimum changed axes 7. | locally verified, user visual acceptance still open |
| B.2.2 theme radius changes are visible for every pair | `theme-tokens.css` defines distinct `--radius-card` values for all seven palettes: 0px, 2px, 8px, 10px, 12px, 14px, and 16px; the verifier fails if any pair lacks a radius delta. | `pnpm -C devhub check:theme-token-contract` passed; `devhub/docs/r8/theme-token-contract.md` records radius deltas for all 21 pairs. | locally verified, user visual acceptance still open |
| B.2.3 theme border changes are visible for every pair | `theme-tokens.css` uses distinct `--border-card` signatures across palettes, including hard 2px constructivism, neutral light/dark borders, cyberpunk neon borders, warm paper borders, and Swiss surface-token borders; the verifier compares full border signatures, not only width. | `pnpm -C devhub check:theme-token-contract` passed with all 21 pairs reporting `border` in changed axes. | locally verified, user visual acceptance still open |
| B.2.4 theme shadow changes are visible for every pair | `theme-tokens.css` now keeps distinct `--shadow-card` signatures across all palettes, covering hard offset, soft elevation, warm paper, neon glow, no-shadow Swiss, graphite shadow, and indigo light-console shadow. | `pnpm -C devhub check:theme-token-contract` passed with all 21 pairs reporting `shadow` in changed axes. | locally verified, user visual acceptance still open |
| B.2.5 theme font-family changes are visible for every pair | `theme-tokens.css` assigns pairwise-distinct heading font signatures for constructivism, modern-light, warm-light, cyberpunk, Swiss, dark, and light palettes; `light` intentionally uses `system-ui, 'Inter Variable'` order while `dark` uses `'Inter Variable', system-ui`. | `pnpm -C devhub check:theme-token-contract` passed with all 21 pairs reporting `font` in changed axes. | locally verified, user visual acceptance still open |
| B.2.6 theme spacing changes are visible for every pair | `theme-tokens.css` assigns pairwise-distinct `--spacing-base` values across the seven palettes: 4px, 5px, 6px, 7px, 8px, 9px, and 10px. | `pnpm -C devhub check:theme-token-contract` passed with all 21 pairs reporting `spacing` in changed axes. | locally verified, user visual acceptance still open |
| B.2.7 theme motion changes are visible for every pair | `theme-tokens.css` assigns pairwise-distinct `--motion-duration-normal` values across the seven palettes: 120ms, 150ms, 180ms, 200ms, 220ms, 250ms, and 350ms. | `pnpm -C devhub check:theme-token-contract` passed with all 21 pairs reporting `motion` in changed axes. | locally verified, user visual acceptance still open |
| B.2.8 every theme pair has documented six-plus-dimensional differences | `devhub/docs/r8/theme-token-contract.md` records canonical sources, acceptance rules, actual palette tokens, and the 21 pairwise delta rows for color, radius, border, shadow, font, spacing, and motion. The verifier can print the same Markdown table from actual CSS via `node ./scripts/verify-theme-token-contract.mjs --print-markdown`. | `pnpm -C devhub check:theme-token-contract`, theme targeted Vitest, targeted ESLint, `pnpm -C devhub exec tsc --noEmit --pretty false`, `pnpm -C devhub check:no-emoji`, and targeted `git diff --check` passed. | locally verified, user visual acceptance still open |
| B.3.1 Density controls provide compact, standard, and comfortable modes | `SettingsDialog.tsx` exposes the `informationDensity` segmented control for `compact`, `standard`, and `comfortable`; `theme-language.ts` normalizes those values into the four-axis `ThemeState`, and `applyThemeStateToDocument()` writes `data-density` to the document root for global CSS consumption. | Same theme-language targeted Vitest run passed; tests verify density normalization and document dataset application. | locally verified, user acceptance still open |
| B.3.2 density changes synchronize list, card, and grid sizing tokens | `theme-tokens.css` now defines explicit compact / standard / comfortable values for `--density-card-min-height`, `--density-list-row-height`, `--density-grid-gap`, `--project-list-row-height`, `--project-card-min-height`, and `--project-card-min-width`; `verify-density-motion-contract.mjs` parses actual CSS and requires strict compact < standard < comfortable ordering. | `pnpm -C devhub check:density-motion-contract` passed: 3 density levels and 3 motion levels. | locally verified, user visual acceptance still open |
| B.3.3 density changes apply immediately without layout-state delay | `useTheme().setDensity()` now has direct regression coverage proving it writes `data-density="comfortable"` through `applyThemeStateToDocument()` and persists the same appearance update through `window.devhub.settings.update(...)`. | `pnpm -C devhub exec vitest run src/renderer/hooks/useTheme.test.tsx src/renderer/theme/theme-language.test.ts src/renderer/theme/theme-distance.test.ts --maxWorkers=1` passed with 21 tests. | locally verified, user visual acceptance still open |
| B.3.4 density levels reserve non-overlapping minimum dimensions | `devhub/docs/r8/density-motion-contract.md` records the compact / standard / comfortable card, row, grid, project-list, card-min, and card-width values; the verifier fails if any min-height, row-height, gap, or width does not increase strictly. `e2e/example.spec.ts` now drives the real Electron Settings dialog, switches project-list density from compact to comfortable, and verifies the live virtualized list row-height contract changes from `64` to `144` while the persisted setting becomes `comfortable`. | `pnpm -C devhub check:density-motion-contract`, targeted ESLint, `pnpm -C devhub exec tsc --noEmit --pretty false`, `pnpm -C devhub check:no-emoji`, targeted `git diff --check`, and `pnpm -C devhub test:e2e --grep "P1.2-a" --reporter=line --workers=1` passed. | locally verified with Electron E2E evidence, user visual acceptance still open |
| B.4.1 RadiusFamily controls provide sharp, soft, and round modes | `SettingsDialog.tsx` exposes the `radiusFamily` segmented control for `sharp`, `soft`, and `round`; `theme-language.ts` normalizes persisted values and writes `data-radius-family` to the document root. | Same theme-language targeted Vitest run passed; tests verify radius-family resolution and dataset application. | locally verified, user acceptance still open |
| B.4.2 MotionLevel controls provide reduced, balanced, and expressive modes | `SettingsDialog.tsx` exposes the `motionLevel` segmented control for `reduced`, `balanced`, and `expressive`; disabling animations routes to `reduced`, and `theme-language.ts` writes `data-motion-level` to the document root. | Same theme-language targeted Vitest run passed; tests verify motion-level validation and dataset application. | locally verified, user acceptance still open |
| B.4.3 OS reduced-motion preference automatically applies reduced motion | `resolveThemeState()` now accepts a system motion preference, `useTheme()` reads real `matchMedia('(prefers-reduced-motion: reduce)')`, listens for system motion changes, and forces effective `data-motion-level="reduced"` when the OS requests reduced motion. CSS also retains the `@media (prefers-reduced-motion: reduce)` fallback. | Theme targeted Vitest passed with 21 tests, including an expressive saved setting resolving to effective reduced motion under OS reduced-motion. | locally verified, user OS/browser acceptance still open |
| B.4.4 motion-level changes synchronize all duration and transition tokens | `theme-tokens.css` now defines explicit reduced / balanced / expressive `--motion-duration-fast`, `--motion-duration-normal`, `--motion-duration-slow`, `--transition-default`, `--transition-fast`, `--animation-card-enter`, and `--duration-theme` values; `verify-density-motion-contract.mjs` requires reduced to be 0ms/none and balanced < expressive. | `pnpm -C devhub check:density-motion-contract` passed; `devhub/docs/r8/density-motion-contract.md` records the motion table. | locally verified, user visual acceptance still open |
| B.5.1 Settings provides a dedicated theme preview surface | `SettingsDialog.tsx` now includes `theme-preview-editor` in the Appearance panel with a live preview surface driven by the current theme editor draft rather than a static screenshot. The P8.2 Electron E2E opens the real Settings dialog and verifies the preview editor is visible in the running renderer. | `pnpm -C devhub exec vitest run src/renderer/theme/theme-pack.test.ts src/renderer/components/settings/SettingsDialog.theme-editor.test.tsx --maxWorkers=1` passed with 2 tests. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with live preview visibility assertions. | locally verified with Electron preview evidence, user visual acceptance still open |
| B.5.2 theme preview renders real component examples | The preview renders a real card, button, table, and bar-chart block with `theme-preview-card`, `theme-preview-button`, `theme-preview-table`, and `theme-preview-chart` test IDs and CSS-token-backed inline styles. The P8.2 Electron E2E verifies all four examples are visible in the running Settings dialog. | `SettingsDialog.theme-editor.test.tsx` asserts all four preview examples render in the real SettingsDialog tree. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with live component-example assertions. | locally verified with Electron preview evidence, user visual acceptance still open |
| B.5.3 users can inspect changes before applying a runtime theme | The Theme Editor exposes local draft token controls for accent, radius, spacing, and motion; changing `theme-editor-accent` updates `theme-live-preview` immediately without calling the settings update bridge. The P8.2 Electron E2E changes the real color input to `#112233`, verifies `theme-live-preview` updates, and verifies the running document palette remains `modern-light` before any theme apply action. | `SettingsDialog.theme-editor.test.tsx` changes the accent token to `#112233` and verifies the live preview data changes before export. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with preview-before-apply assertions. | locally verified with Electron preview evidence, user visual acceptance still open |
| B.5.4 Theme Editor supports single-token adjustment | `SettingsDialog.tsx` exposes editor controls for `accentColor`, `cardRadiusPx`, `spacingBasePx`, and `motionNormalMs`; those draft tokens are validated by `themePackTokenSchema` in `theme-pack.ts`. | `theme-pack.test.ts` validates a real single-token-edited theme pack payload through Zod. | locally verified, user acceptance still open |
| B.5.5 custom theme drafts export as `.devhub-theme.json` | `theme-pack.ts` defines `devhubThemePackSchema`, `buildThemePack()`, and `serializeThemePack()`; `SettingsDialog.tsx` exports the current draft through a local Blob download named `<palette>-YYYY-MM-DD.devhub-theme.json`; `docs/r8/theme-pack-format.md` records the actual schema. | Theme-pack targeted Vitest, targeted ESLint, `pnpm -C devhub exec tsc --noEmit --pretty false`, `pnpm -C devhub check:no-emoji`, and targeted `git diff --check` passed. | locally verified, user file-open acceptance still open |
| C.1.2 process cards expose a top-right topology badge | `ProcessView.tsx` renders `CardEdgeGraphBadge` for process cards with `data-graph-entry="process-card-attached-topology"` and real PID target metadata; tests verify the badge emits a process relationship navigation event. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/MonitorCardEdgeBadge.test.tsx src/renderer/components/monitor/PortView.port-popout.test.tsx --maxWorkers=1 -t "module tour\|tour actions\|NEW badge\|F1\|contextual help\|no real\|card edge graph badge"` passed with 11 tests and 26 skipped. | locally verified, user acceptance still open |
| C.1.3 process card context menu has verified kill/detail/popout/copy basics | `ProcessView.tsx` now exposes detail, real BrowserWindow process popout, directory, copy PID, copy command, process tree, tag edit, and kill actions from the process card context menu; the list-row context menu also exposes the same process popout and copy PID basics. The popout action calls the existing `window.devhub.r8.popout.create()` bridge with `surface='process'`, the real PID target, `mode='browserwindow'`, and route `/monitor`. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/ProcessView.test.tsx --maxWorkers=1 --reporter=verbose` passed with 8 tests; touched-file ESLint, TypeScript no-emit, `check:no-emoji`, and targeted `git -C devhub diff --check` passed. | locally verified, user acceptance still open |
| C.1.5 process cards support double-click to enter the detail drawer | `ProcessCard` exposes `data-detail-entry="process-card-double-click"` and calls the real `onShowDetail(pid)` handler on double-click without conflicting with the graph badge. | ProcessView targeted Vitest passed with 6 tests. | locally verified, user acceptance still open |
| C.2.4 process list row click opens the ProcessDetailDrawer path | `ProcessItem` exposes `data-detail-entry="process-row-click-drawer"`, selects the row, and calls `onShowDetail(pid)` on normal clicks while preserving multi-select behavior for Ctrl-click. | ProcessView targeted Vitest passed with 6 tests. | locally verified, user acceptance still open |
| C.3.2 ProcessDetailPanel header "看图" opens the attached topology view | `ProcessDetailPanel.tsx` exposes `process-attached-topology-button` with title `看图：查看进程关系视图`, switches to the relationship tab, and renders `AttachedGraphView` plus `AttachedFlowView` for the current PID; `ProcessDetailPanel.test.tsx` verifies the route. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/attached/AttachedGraphView.test.tsx src/renderer/components/monitor/ProcessDetailPanel.test.tsx src/renderer/components/monitor/PortFocusPanel.test.tsx src/renderer/components/monitor/WindowView.test.tsx src/renderer/components/topology/FullScreenTopologyView.test.tsx --maxWorkers=1 -t "relationship\|attached\|topology\|global\|graph\|关联\|看图\|selected\|depth"` passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| C.3.3 ProcessDetailPanel relationship tab embeds network and neural relationship graph surfaces | `ProcessDetailPanel.tsx` includes the `关联` tab, the `关系视图` attached entry, and `AttachedGraphView`; `AttachedGraphView.tsx` exposes `network-topology` and `neural-relationship` graph-kind buttons over the real `topology.attachedDeep10` preload bridge. | Same attached-topology targeted Vitest run passed with 27 tests and 5 skipped; `AttachedGraphView.test.tsx` now selects the real Cytoscape-backed DAG option rather than the obsolete `graph-node` marker. | locally verified, user acceptance still open |
| C.6.1 process detail has at least three relationship-view entrypoints | Process coverage now spans the process card edge badge (`process-card-graph-badge-1234`), ProcessDetailPanel header "看图" button, and ProcessDetailPanel `关联` / `关系视图` tab. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped; `pnpm -C devhub exec vitest run src/renderer/components/monitor/MonitorCardEdgeBadge.test.tsx --maxWorkers=1 -t "process card graph badge\|process module tour\|relationship"` passed with 3 tests and 5 skipped. | locally verified, user acceptance still open |
| C.6.2 command palette search for graph / topology / relationship shows the process relationship view | Runtime commands include `topology.global`, `topology.flow`, and scanner-backed relationship commands with Chinese/pinyin/English topology keywords; `R8CommandPalette.test.tsx` now verifies `图`, `topology`, and `关系` queries surface the global flow graph command. | `pnpm -C devhub exec vitest run src/renderer/components/command/R8CommandPalette.test.tsx src/renderer/components/topology/TopologyEntrypoints.test.tsx src/renderer/components/topology/FullScreenTopologyView.test.tsx src/renderer/components/monitor/MonitorPanel.test.tsx src/renderer/hooks/useGlobalShortcuts.test.tsx src/main/services/R8RuntimeService.test.ts --maxWorkers=1 -t "topology\|Topology\|全局拓扑\|流程图\|source groups\|global shortcuts\|opens fullscreen topology\|top-level monitor entries\|R8CommandPalette"` passed with 30 tests and 134 skipped. | locally verified, user acceptance still open |
| C.6.3 status bar keeps a topology button with active process count | `StatusBar.tsx` renders `topology-status-badge`, writes `data-active-process-count`, shows `topology-status-active-process-count`, and dispatches `devhub:open-topology-global`; `StatusBar.test.tsx` verifies the count and click path. | Topology targeted Vitest passed with 30 tests and 134 skipped; prior command/statusbar run passed `StatusBar.test.tsx` with 4 tests. | locally verified, user acceptance still open |
| C.7.3 process module has a three-step Tour for card/list switching, relationship entry, and operation menu | `ProcessModuleTour.tsx` defines the three-step process tour, persists dismissal in `devhub:process-module-tour:v1`, and disables actions when no real process exists instead of fabricating samples; `MonitorCardEdgeBadge.test.tsx` verifies real-process actions. | Module-tour targeted Vitest passed with 11 tests and 26 skipped. | locally verified, user acceptance still open |
| C.7.4 process module NEW badge is time-bounded to the R8 thirty-day window | `processModuleRelease.ts` centralizes the thirty-day release window; `ProcessView` renders `process-module-new-badge` only while active; `MonitorCardEdgeBadge.test.tsx` verifies active and expired dates. | Module-tour targeted Vitest passed with 11 tests and 26 skipped. | locally verified, user acceptance still open |
| C.7.5 process module F1 help opens contextual in-app help | `ProcessView.tsx` registers an F1 shortcut through `useGlobalShortcuts`, and `ProcessModuleHelp.tsx` renders a local help dialog with real process-count context and no external dependency. | Module-tour targeted Vitest passed with 11 tests and 26 skipped. | locally verified, user acceptance still open |
| D.1.1 port cards show port / proto / pid / state / security tier | `PortView.port-popout.test.tsx` verifies `port-card-3000-4242` exposes `data-r8a-fields="port,protocol,pid,state,securityTier"` and renders the concrete `:3000`, `TCP`, `PID: 4242`, `监听中`, and `SecurityTierBadge` fields. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/PortView.port-popout.test.tsx src/renderer/components/monitor/PortFocusPanel.test.tsx src/renderer/components/monitor/port/SecurityTierBadge.test.tsx --maxWorkers=1 -t "required port card fields\|security tier\|three port view modes\|restores the persisted port view mode\|opens a floating port card\|detaches the focus panel\|stale 数据\|stale data\|header attached graph"` passed with 9 tests and 30 skipped; full `SecurityTierBadge.test.tsx` passed with 4 tests. | locally verified, user acceptance still open |
| D.1.2 port security tier has explicit visual tone metadata | `SecurityTierBadge.tsx` exposes `data-security-tier`, `data-security-tone`, and `data-security-label` for each tier; `SecurityTierBadge.test.tsx` verifies all four configured tiers. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/port/SecurityTierBadge.test.tsx --maxWorkers=1` passed with 4 tests. | locally verified, user acceptance still open |
| D.1.3 port cards can pop out to a floating card | `PortView.port-popout.test.tsx` verifies the explicit click trigger opens `port-popout-card-3000-4242` with real trigger, sync-direction, and z-index metadata. | Port field/view/focus targeted Vitest passed with 9 tests and 30 skipped. | locally verified, user acceptance still open |
| D.1.4 port cards expose a top-right topology badge | `PortView.tsx` renders `CardEdgeGraphBadge` for port cards with `data-graph-entry="port-card-attached-topology"`, `data-graph-scope="port"`, and the real selected port target metadata. | Module-tour targeted Vitest passed with 11 tests and 26 skipped; port relationship targeted Vitest passed with 5 tests and 30 skipped. | locally verified, user acceptance still open |
| D.2.1-D.2.2 port tab has cards/list/relationship modes and persists the chosen mode | `PortView.tsx` exposes `data-port-view-modes="cards,list,relationship"` and stores `devhub:port-view-mode`; tests switch cards to list to relationship and verify persisted restore on mount. | Port field/view/focus targeted Vitest passed with 9 tests and 30 skipped. | locally verified, user acceptance still open |
| D.2.3 port relationship mode defaults to the monitored-port relationship graph when selected | `PortView.tsx` includes `cards`, `list`, and `relationship` modes; `PortView.port-popout.test.tsx` verifies the card topology badge switches to relationship mode and renders the relationship graph. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/PortRelationshipGraph.test.tsx src/renderer/components/monitor/PortRelationshipGraph.ui.test.tsx src/renderer/components/monitor/PortView.port-popout.test.tsx --maxWorkers=1 -t "relationship graph\|relationship depth\|owns\|connects\|card edge topology badge\|relationship mode"` passed with 5 tests and 30 skipped. | locally verified, user acceptance still open |
| D.3.1 PortFocusPanel embeds attached network and neural relationship graph surfaces | `PortFocusPanel.tsx` renders `port-attached-topology-section`, `AttachedGraphView`, and `AttachedFlowView` for the selected port; `PortFocusPanel.test.tsx` verifies attached graph root kind/id and focus behavior. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| D.3.2 PortFocusPanel detaches through the BrowserWindow popout bridge | `PortFocusPanel.tsx` exposes `data-detach-capability="browserwindow-popout"` and calls the real `window.devhub.r8.popout.create` bridge with the selected port focus route; tests verify detached state after the bridge resolves. | Port field/view/focus targeted Vitest passed with 9 tests and 30 skipped. | locally verified, user acceptance still open |
| D.3.3-D.3.4 PortFocusPanel renders stale cached data with a top warning instead of blanking | `PortFocusPanel.test.tsx` verifies stale incremental data renders `port-stale-warning` with `data-stale-source="cache"`, `data-stale-position="top"`, and visible cached-data text. | Port field/view/focus targeted Vitest passed with 9 tests and 30 skipped. | locally verified, user acceptance still open |
| D.5.1-D.5.3 port relationship view shows owns/connects edges and adjustable depth | `PortRelationshipGraph.tsx` builds `owns` and `connects` relationship edges from real monitored process/port/window data and exposes `port-relationship-depth-slider`; unit and UI tests verify `owns`, `connects`, and depth filtering. | Port relationship targeted Vitest passed with 5 tests and 30 skipped. | locally verified, user acceptance still open |
| D.6.2 port module has a three-step Tour for pop-out, security, and relationship entry | `PortModuleTour.tsx` defines the three-step port tour, shows real security summary counts, persists dismissal in `devhub:port-module-tour:v1`, and disables actions when no real port exists. | Module-tour targeted Vitest passed with 11 tests and 26 skipped. | locally verified, user acceptance still open |
| E.1.1-E.1.2 window cards and list rows show HWND, title, process identity, PID, always-on-top state, and instance key | `WindowView.test.tsx` verifies the card and list row render `DevHub`, `devhub.exe`, `PID: 1234`, `HWND: 9001`, `置顶`, and `data-window-instance-key="devhub.exe:1234:9001"` for disambiguation. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped; that run included all 16 `WindowView.test.tsx` tests. | locally verified, user acceptance still open |
| E.1.3 window titles redact sensitive token and API-key values | `WindowView.test.tsx` renders a window title containing `token=` and `api_key=` values, then verifies card and list row output replaces secrets with `[REDACTED]` text and does not expose the original values. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped; that run included all 16 `WindowView.test.tsx` tests. | locally verified, user acceptance still open |
| E.2.1 window focus / close / move / minimize / maximize paths have local verified coverage | `WindowView.test.tsx` verifies filtered-window focus, lasso-selected batch focus, retry of failed focus, custom confirmation before closing more than five selected HWNDs, move/resize, minimize, and maximize through the real window hook bridge with visible feedback. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/WindowView.test.tsx --maxWorkers=1 --reporter=verbose` passed with 18 tests; touched-file ESLint, TypeScript no-emit, `check:no-emoji`, and targeted diff whitespace checks passed. | locally verified, user acceptance still open |
| E.2.2 always-on-top toggle is reachable from the window tour action | `WindowView.test.tsx` verifies the window module tour topmost action calls `setWindowTopmost(selectedWindow.hwnd, true)` and shows the `窗口已置顶` visual feedback. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| E.2.3 window opacity adjustment calls the real opacity hook | `WindowView.test.tsx` verifies the opacity action parses the prompted value, calls `setWindowOpacity(selectedWindow.hwnd, 85)`, and renders a success message. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| E.2.5 keyboard event injection uses a custom target confirmation dialog | `WindowView.test.tsx` verifies safe-key input opens `window-batch-confirm-dialog`, displays the concrete HWND/title/key target, and calls `sendKeysToWindow(selectedWindow.hwnd, key)` only after confirmation. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| E.4.1 WindowView embeds the attached relationship graph surface | `WindowView.tsx` renders `window-relationship-panel`, `AttachedGraphView`, and `AttachedFlowView` for the selected window; `WindowView.test.tsx` verifies the top action button focuses the real panel and the card graph badge routes to it. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| E.5.2 window module has a three-step Tour for identity, operations, and always-on-top | `WindowView.test.tsx` verifies `window-module-tour` has three persisted steps, uses the real selected window identity, disables actions when no real window exists, opens the operation panel, and routes topmost through the actual hook. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| E.5.3 window operations provide visible feedback | `WindowView.test.tsx` verifies visible success or progress messages for topmost, safe-key send, move, opacity, failed focus retry, batch close confirmation, card focus quick action, minimize, and maximize. Regular window card/list focus quick actions and AI-window focus/minimize/maximize/restore/close quick actions now route through `handleWindowOperation()` so they reuse the same toast feedback path as the operation panel instead of silently calling hooks. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/WindowView.test.tsx --maxWorkers=1 --reporter=verbose` passed with 18 tests; touched-file ESLint, TypeScript no-emit, `check:no-emoji`, and targeted `git -C devhub diff --check` passed. | locally verified, user acceptance still open |
| E.5.4-E.5.5 keyboard injection confirmation and failure messaging are explicit | `WindowView.test.tsx` verifies the keyboard confirmation dialog contains `HWND 9001 / DevHub / Escape`, success messages name the target, and failure renders `键盘事件注入失败: 窗口 HWND 9001 / DevHub / Ctrl+D`. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| F.1.1 AI tool process detection has local coverage for Claude Code, Codex, Gemini CLI, Cursor, and Copilot | `AITaskTracker.test.ts` verifies process/command-pattern detection for `claude-code`, `codex`, and `gemini-cli`, plus unknown-process rejection. `CursorCopilotDetector.test.ts` verifies Cursor and Copilot title parsing, executable basename whitelisting, spoof rejection, 5 Hz scan frequency cap, same-HWND throttling, scanner-snapshot status, five GWT title-detection scenarios, hot rule reload, and confidence ceiling rejection. | `pnpm -C devhub exec vitest run src/main/services/AITaskTracker.test.ts src/main/services/cli-parser/CursorCopilotDetector.test.ts --maxWorkers=1 -t "detectAIToolType\|Cursor/Copilot title detection" --reporter=verbose` passed with 14 tests and 40 skipped. | locally verified, user acceptance still open |
| F.1.2 AI task state taxonomy exposes more than idle/running | `AITaskTracker.ts` contains the 12-state monitor state machine, while `derive-progress.test.ts` verifies distinct `receiving-input`, `awaiting-human`, and `stuck` rendering in addition to active progress states. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.1.3 state machine has cross-layer coordination and invalid-transition handling | `StateMachineCoordinator.test.ts` verifies system transition history, user-rule assertion violations, task state driven from fused progress snapshots, ring-buffer retention, and invalid transition rejection instead of direct mutation. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.1.4 state flips are debounced before confirmation | `AITaskTracker.test.ts` verifies high-confidence candidates are held until confirmation and idle candidates move back to waiting during confirmation, reducing state jitter. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.1.5 task state/progress can be driven by real stdout parser signals | `AITaskTracker.test.ts` verifies CLI parser subscription feeds real parser progress into `SignalCollector`, handles Gemini stdout parser state through the `cli_parse` channel, and preserves legacy signals. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.1.6 detection explanations are available through the diagnostic panel | `SignalDiagnosticPanel.test.tsx` verifies explanations are loaded from the typed preload bridge only after panel expansion, keeping the source path explicit and lazy. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.1.7 detection feedback persists locally with bounded learning | `MisreportLogger.test.ts` verifies SQLite-backed misreport persistence, bounded weight adjustments, cumulative source-learning caps, and `correct-detection` feedback as bounded positive local feedback. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.2.1 progress uses continuous estimates and distinct non-idle states | `derive-progress.test.ts` verifies coding fallback progresses within a continuous range, explicit progress is respected, and `receiving-input`, `awaiting-human`, and `stuck` render as distinct states; `ProgressBar.test.tsx` verifies determinate and indeterminate UI contracts. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.2.2 task retry progress can return to zero percent | `AITaskTracker.test.ts` and `derive-progress.test.ts` verify explicit retry progress events reset active task progress to zero rather than preserving stale percentages. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.2.3 long-running task progress keeps moving within a 30-second window | `derive-progress.test.ts` verifies estimated long-running coding progress advances within 30 seconds after the long-task threshold. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.2.4 progress confidence intervals are rendered for determinate estimates | `derive-progress.test.ts` builds bounded confidence ranges, and `ProgressBar.test.tsx` verifies confidence interval labels while preserving exact `aria-valuenow`. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.2.5 stuck progress state is distinct and test-covered | `derive-progress.test.ts` verifies `stuck` as a distinct non-idle state, and `AITaskTracker.test.ts` covers stuck-related state scoring paths. | AI state/progress targeted Vitest passed with 34 tests and 41 skipped. | locally verified, user acceptance still open |
| F.3.1 SKILL engine loads local SKILL definitions | `R8RuntimeService.test.ts` verifies strict builtin and user skill loading without executing scripts, diff streaming after local writes, file watching for add/change/unlink, and feature-flag behavior that preserves user skills. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.3.2 SKILL metadata includes identity, schema, output, license, and sandbox fields | `R8RuntimeService.test.ts` fixtures exercise validated skill manifests with `name`, `version`, `author`, `inputs`, `outputs`, `license`, and `sandbox`, while rejecting unsafe script paths instead of loading malformed metadata. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.3.3 CSV prompt supports `@skill:xxx` references | `CsvTaskDriver.test.ts` verifies `@skill:lint-fix` prompt parsing, validates the referenced local skill, and maps the skill reference into runtime rows without adding fake CSV columns. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.3.4 missing SKILL fails before launch with explicit prompt errors | `CsvTaskDriver.test.ts` rejects missing and malformed `@skill` references before runtime row creation, returning explicit `inputArgs` validation errors instead of starting a task. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.3.5 SKILL sandbox levels enforce read-only, read-write, and system behavior | `R8RuntimeService.test.ts` verifies read-write `on_fail` artifact execution, read-only write blocking with `E_PERMISSION`, and system sandbox child-process execution only when explicitly declared. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.3.6 SKILL scripts can call a real local MCP stdio server | `R8RuntimeService.test.ts` passes MCP stdio server metadata to a system SKILL script and verifies the script receives `echo:real` from the local server before retry. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.1 CSV driver supports the fixed 18-column task schema | `csv-task-row.test.ts` keeps the fixed 18-column order as the schema source of truth; `CsvParser.ts` reports strict row errors when a record does not contain 18 columns; runtime tests load 18-column CSV files. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.2 CSV launch validates duplicate IDs, paths, API key leakage, headers, and skills before execution | `CsvTaskDriver.test.ts` covers duplicate task IDs, required input file paths, likely API-key leakage, strict header errors, unknown skills, and missing dependencies before any runtime launch. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.3 CSV execution supports sequential and bounded parallel modes | `R8RuntimeService.test.ts` and `TaskQueueService.test.ts` verify DAG readiness, `concurrent` launch options, and independent parallel-group limits without pretending queued rows succeeded. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.4 CSV prompt interpolation supports row variables such as cwd and file | `CsvTaskDriver.test.ts` interpolates CSV prompt variables from real row values and verifies the expanded runtime prompt before launch. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.5 CSV prompt supports `@file:` external file references | `CsvTaskDriver.test.ts` expands `@file:` from real local files and rejects unreadable file references before creating runtime rows. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.6 repeated CSV task IDs can be skipped or rerun by explicit resume policy | `TaskQueueService.test.ts` verifies resume skip, force rerun, and row-hash change detection; `R8RuntimeService.test.ts` verifies CSV session resume control acknowledgements. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.7 failed CSV tasks can be retried without faking success for completed rows | `TaskQueueService.test.ts` verifies explicit retry transitions, exponential backoff, manual retry, and truthful `on_fail` routing; runtime tests verify `task:on-fail` retry audit paths. | CSV/SKILL targeted Vitest passed with 5 files, 55 tests, and 116 skipped. | locally verified, user acceptance still open |
| F.4.8 CSV/task results produce real artifacts and exportable launch material | `task:export-results` now provides a unified CSV/JSON task-result export workflow. `R8RuntimeService.test.ts` creates and completes a real task, writes JSON and CSV artifacts to a real temp directory, reads them back, and verifies status, `artifactsPath`, CSV escaping, bytes, and SHA-256 metadata; `R8OpsPanel.test.tsx` verifies the user-facing export action calls the preload bridge and renders returned artifact paths/files; `prompts/0421/contracts/23-ipc-contracts-master.md` now lists the public `task:export-results` invoke channel in the X2 preload whitelist. | Focused task-result export/preload Vitest passed on 2026-05-20 with 3 passed files, 1 skipped file, 6 passed tests, and 163 skipped; `src/preload/preloadContract.test.ts` also passed unfiltered with 1 file and 4 tests. TypeScript no-emit, touched-file ESLint, Zod SoT, no-emoji, no-cloud-deps, no-OCR-deps, and targeted whitespace checks passed. | locally verified, user acceptance still open |
| F.5.1 Watchdog monitors AI tool subprocess health through multiple heartbeat sources | `WatchdogEngine.test.ts`, `WatchdogSupervisor.test.ts`, and `R8RuntimeService.test.ts` verify registered heartbeat sources, runtime source adapters, child PID liveness, supervisor lifecycle events, and renderer event streams. | Watchdog targeted Vitest passed with 4 files, 47 tests, and 125 skipped; the focused `WatchdogEngine.test.ts` rerun passed 12 tests. | locally verified, user acceptance still open |
| F.5.2 crashed subprocesses request restart without fake respawn success | `R8RuntimeService.test.ts` detects a killed real child process and records a restart action; `WatchdogSupervisor.test.ts` detects killed spawned children after the handshake grace window without claiming a fake replacement. | Watchdog targeted Vitest passed with 4 files, 47 tests, and 125 skipped. | locally verified, user acceptance still open |
| F.5.3 memory over-limit can request watchdog restart through a configured threshold | `WatchdogEngine.ts` now supports `memoryRestartMb`, reads latest heartbeat `memoryMb`, emits `memory-over-limit` state/action history, and requests restart even during grace; `WatchdogEngine.test.ts` verifies the max-memory threshold path with concrete `384 MB > 256 MB` evidence. | `pnpm -C devhub exec vitest run src/main/services/watchdog/WatchdogEngine.test.ts --maxWorkers=1` passed with 12 tests. | locally verified, user acceptance still open |
| F.5.4 watchdog restart count has a storm cap | `WatchdogEngine.test.ts` blocks the sixth restart in one hour, emits `storm-detected`, and routes the instance to human/fallback pending instead of looping. | Focused `WatchdogEngine.test.ts` rerun passed 12 tests. | locally verified, user acceptance still open |
| F.5.5 watchdog restart emits notification and audit evidence | `R8RuntimeService.test.ts` verifies watchdog restart actions update task queue state, create inject action records, and emit unified watchdog notifications; watchdog event history records action rows. | Watchdog targeted Vitest passed with 4 files, 47 tests, and 125 skipped. | locally verified, user acceptance still open |
| F.6.1 task start can trigger prompt injection into an AI tool target | `R8RuntimeService.test.ts` verifies `allow_inject` CSV task start triggers the real inject path, resolves target aliases, and records cancellation/whitelist evidence without native fake success. | Inject targeted Vitest passed with 4 files, 61 tests, and 111 skipped. | locally verified, user acceptance still open |
| F.6.2 injection dry-run preview resolves targets before execution | `InjectService.test.ts` and `InjectTargetResolver.test.ts` verify dry-run target resolution, whitelist checks, alias/countdown resolution, chunk planning, and first-time confirmation without typing into a window. | Inject targeted Vitest passed with 4 files, 61 tests, and 111 skipped. | locally verified, user acceptance still open |
| F.6.3 injection failures surface explicit diagnostic categories | `InjectService.test.ts` covers native-disabled, shim, pty, focus polling, screenshot, and UIA failure boundaries; runtime tests keep native execution failure truthful instead of reporting success. | Inject targeted Vitest passed with 4 files, 61 tests, and 111 skipped. | locally verified, user acceptance still open |
| F.6.4 injection attempts are auditable and replayable through local artifacts | `InjectService.test.ts` writes SQLite audit rows, before/after screenshots, and post-inject verification evidence; `InjectSafetyModal.test.tsx` verifies countdown cancellation and user confirmation surfaces. | Inject targeted Vitest passed with 4 files, 61 tests, and 111 skipped. | locally verified, user acceptance still open |
| F.7.1 monitor path handles at least twelve active AI task cards locally, with backend snapshot throttling | `AITaskView.test.tsx` renders twelve real-shaped active tasks across the UI, while `R8RuntimeService.test.ts` verifies monitor snapshot stream throttling after real CLI parser events. This is local automated evidence, not a 24-hour human latency study. | `AITaskView.test.tsx` passed with 2 tests; watchdog/monitor targeted Vitest passed with 47 tests and 125 skipped. | locally verified, user acceptance still open |
| F.7.2 AI task monitor has card, list, and detail views | `AITaskView.tsx` now exposes explicit `cards`, `list`, and `detail` active-task modes; `AITaskView.test.tsx` verifies twelve cards, twelve list rows, selected-row handoff, and a detail panel for the selected task. | `pnpm -C devhub exec vitest run src/renderer/components/monitor/AITaskView.test.tsx --maxWorkers=1` passed with 2 tests. | locally verified, user acceptance still open |
| F.7.3 AI task monitor exposes a timeline/Gantt-style phase view | `AIProgressTimeline.tsx` exposes `ai-progress-gantt-phasebar` with phase counts and timeline state metadata; `AITaskView.test.tsx` opens the card timeline and verifies the `thinking,coding` phase sequence from the real preload-shaped timeline API. | `AITaskView.test.tsx` passed with 2 tests. | locally verified, user acceptance still open |
| F.7.4 task visual editor exists through the CSV DAG editor | `DagEditorPanel.test.tsx`, `DagCanvas.test.tsx`, and `NodeDetailPanel.test.tsx` verify CSV locking, four synchronized views including Gantt, drag dependency updates, template insertion, all 18 columns, schema validation, external-change modal, and canvas keyboard navigation. | DAG editor targeted Vitest passed with 3 files, 6 tests, and 1 skipped. | locally verified, user acceptance still open |
| G.1.1-G.1.6 global topology has network / neural / flow graph kinds and can switch them | `FullScreenTopologyView.tsx` loads `network-topology`, switches to `neural-relationship` and `flow`, persists pending flow navigation intents, and exposes renderer export/save paths; `FullScreenTopologyView.test.tsx` verifies graph-kind switching, node labels, degradation expansion, direct flow open, and selected-node restore. | Topology targeted Vitest passed with 30 tests and 134 skipped. | locally verified, user acceptance still open |
| G.2.1 global graph exists as a MonitorPanel topology tab | `MonitorPanel.tsx` includes the topology tab and renders `FullScreenTopologyView`; `MonitorPanel.test.tsx` verifies the top-level topology entry and view activation. | Topology targeted Vitest passed with 30 tests and 134 skipped; earlier `MonitorPanel.test.tsx -t "top-level monitor entries"` passed with 1 test. | locally verified, user acceptance still open |
| G.2.2 global graph has at least three entrypoints across sidebar / command palette / status bar / shortcut | `Sidebar.tsx` exposes `data-activity-bar-icon="topology-global"`, `R8RuntimeService.ts` exposes `topology.global` / `topology.flow`, `StatusBar.tsx` exposes `topology-status-badge`, and `App.tsx` registers `Ctrl+T` / `Meta+T` through `useGlobalShortcuts`. | Topology targeted Vitest passed with 30 tests and 134 skipped. | locally verified, user acceptance still open |
| G.2.3-G.2.5 attached graphs exist in process, port, and window detail surfaces | Process, port, and window detail surfaces each render `AttachedGraphView` and `AttachedFlowView` with concrete root kind/id attributes, preserving the global graph as a separate MonitorPanel route. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped. | locally verified, user acceptance still open |
| G.2.6 global and attached graph selected-node state synchronizes through session storage and navigation events | `AttachedGraphView.tsx` writes `devhub:topology:global:selected-node` and dispatches `devhub:open-topology-global`; `FullScreenTopologyView.tsx` consumes the pending selected node, requests the selected node from the topology bridge, shows the node detail card, clears the pending key, and can route back to process detail. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped after the stale selector regression was fixed; prior topology targeted Vitest passed with 30 tests and 134 skipped. | locally verified, user acceptance still open |
| G.2.7 global and attached graphs share `GraphService` for data construction | `R8RuntimeService.buildGlobalTopology()` and `R8RuntimeService.topologyAttachedDeep10()` both call the same `GraphService.buildGlobal()` path with different slice scopes/depth/selected-node inputs; renderer surfaces differ but data construction stays centralized. | Source audit plus attached-topology targeted Vitest passed with 27 tests and 5 skipped; prior topology targeted Vitest passed with 30 tests and 134 skipped. | locally verified, user acceptance still open |
| G.4.3 selected topology node shows a node detail card | `FullScreenTopologyView.tsx` restores a pending selected node, renders `topology-node-detail`, and exposes a concrete "open in process detail" action for process nodes. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped; prior topology targeted Vitest passed with 30 tests and 134 skipped. | locally verified, user acceptance still open |
| G.4.5 graph depth controls are available on attached and port relationship graphs | `AttachedGraphView.tsx` exposes `attached-depth-slider` with 1-10 depth and lazy expansion; `PortRelationshipGraph.tsx` exposes `port-relationship-depth-slider` for 1-3 relationship layers. | Attached-topology targeted Vitest passed with 27 tests and 5 skipped; port relationship targeted Vitest passed with 5 tests and 30 skipped. | locally verified, user acceptance still open |
| G.6.3 topology discoverability measures are active | The local implementation exposes topology through the Monitor tab, sidebar activity entry, statusbar badge, command palette, and `Ctrl+T` shortcut; the same tests cover all local entrypoint paths without emoji assets. | Topology targeted Vitest passed with 30 tests and 134 skipped; `pnpm -C devhub check:no-emoji` passed. | locally verified, user acceptance still open |
| H.1.1 idle zero-egress has automated diagnostic-network evidence and a real 60-second capture runner, but the live capture is blocked in this shell | `R8RuntimeService.test.ts` verifies diagnostic export opens zero outbound Node http/https/net clients; `scripts/verify-zero-egress-capture.mjs` now provides a real Windows `pktmon` 60-second startup capture runner that launches `pnpm dev`, parses packet counters, writes JSON evidence, and exits blocked instead of passing when Administrator privileges are missing. | `pnpm -C devhub check:zero-egress-capture:self-test` passed; `pnpm -C devhub check:zero-egress-capture:preflight` returned `ready=false` and exit code 2 because `ZRAINBOW\ZRainbow` is not Administrator; `check:no-cloud-deps`, `check:no-ocr-deps`, and `check:no-emoji` passed. | partial local evidence, administrator live capture required |
| H.1.4 README provides a zero-egress verification tutorial | `devhub/README.md` now documents `60-second zero-egress verification`, the no-cloud/no-OCR/no-outbound Vitest guards, and the Administrator-only `pnpm check:zero-egress-capture` packet-level workflow. | `pnpm -C devhub check:no-emoji` passed with `No emoji found in 779 files`; scoped whitespace/diff checks passed after this update. | locally verified, user acceptance still open |
| H.1.5 CI runs local zero-egress and dependency-egress guards | `devhub/.github/workflows/ci.yml` now runs `check:no-cloud-deps`, `check:no-ocr-deps`, and a focused diagnostic no-outbound Vitest gate on Windows and Linux jobs. | Local equivalents passed: no-cloud, no-OCR, and the focused no-outbound Vitest command. | locally verified, user acceptance still open |
| H.2.1 Settings lists all local data storage paths | `R8RuntimeService.listDataOwnershipPaths()` enumerates real Electron `userData` roots, settings/runtime stores, queue SQLite, CSV tasks, skills, recordings, backups, diagnostics, audit, feedback, inject, and recovery paths; `SettingsDialog.tsx` exposes the `Data / OWNERSHIP` category and root cards with existence, kind, sensitivity, exportability, counts, bytes, update time, and truncation status. | `pnpm -C devhub exec vitest run src/shared/schemas/r8-runtime.test.ts src/main/services/R8RuntimeService.test.ts src/renderer/components/settings/SettingsDialog.data-ownership.test.tsx --maxWorkers=1 -t "data ownership\|Data Ownership\|SettingsDialog data ownership"` passed with 3 tests and 159 skipped; `pnpm -C devhub exec tsc --noEmit --pretty false` passed. | locally verified, user acceptance still open |
| H.2.2 One-click export creates a ZIP of local data | `window.devhub.r8.dataOwnership.exportAll()` calls `R8RuntimeService.exportDataOwnershipArchive()`, which reuses `createBackup()` with the real default classified categories (`settings`, `csv-tasks`, `skills`, `audit-log`); service tests assert the generated ZIP has a real `PK` header and persisted category metadata. | Same data ownership targeted Vitest run passed; `devhub/docs/r8/data-ownership.md` records the export contract and boundaries. | locally verified, user acceptance still open |
| H.2.3 In-app data viewer browses local data | `SettingsDialog.tsx` renders `DataOwnershipPanel` with a bounded entry viewer; `R8RuntimeService.listDataOwnershipEntries()` validates root IDs, resolves relative paths under the selected root, rejects `../outside` with `E_DATA_OWNERSHIP_PATH_OUT_OF_SCOPE`, and returns real file/directory entries. | Same data ownership targeted Vitest run passed; UI test verifies root browsing, nested `SKILL.md` browsing, export result display, and open-path bridge invocation. | locally verified, user acceptance still open |
| H.2.4 Uninstall asks whether to delete data | `devhub/build/installer.nsh` defines an NSIS `customUnInstall` prompt that asks whether to delete DevHub local data, defaults to No, skips update flows with `${isUpdated}`, and deletes only `%APPDATA%\DevHub` plus `%LOCALAPPDATA%\DevHub` when the user explicitly chooses Yes; `devhub/package.json` wires it through `build.nsis.include` and keeps `deleteAppDataOnUninstall=false`. | `pnpm -C devhub check:nsis-uninstall-data` passed, verifying include wiring, assisted installer mode, update guard, Yes/No default-safe prompt, scoped delete paths, and absence of broad profile deletion or `DEVHUB_USER_DATA_DIR` following. Electron-builder official NSIS docs confirm `include`, `customUnInstall`, `deleteAppDataOnUninstall`, and `${isUpdated}`. | locally verified by static installer contract, real uninstall packaging/user acceptance still open |
| H.2.5 Secret-shaped settings fields are encrypted at rest without adding API-key UI | `AppStore.updateSettings()` and `getSettings()` now route settings objects through a sensitive-field protector before `electron-store` persistence; imported or legacy string leaves named `apiKey`, `token`, `secret`, `password`, `credential`, `authorization`, or SMTP password variants become Electron `safeStorage` base64 envelopes instead of plaintext. This honors `prompts/0503-2/00-r8-master-prd.md` NO-API-KEY-UI by protecting imported/legacy fields rather than adding credential forms. | `pnpm -C devhub exec vitest run src/main/store/AppStore.test.ts --maxWorkers=1 -t "safeStorage\|sensitive settings\|Settings Management"` passed with 6 tests and 13 skipped; `pnpm -C devhub exec tsc --noEmit --pretty false` passed. Electron official docs confirm `safeStorage` is main-process local string encryption and Windows availability after `ready`. | locally verified, user acceptance still open |
| H.3.2 sensitive diagnostic text is redacted in local exports | `R8RuntimeService.test.ts` verifies diagnostic packs redact `DEVHUB_DIAG_TOKEN` values and do not include unredacted secret-like strings; `PRIVACY.md` documents local redaction boundaries. | Prior diagnostic targeted Vitest and the focused no-outbound diagnostic test passed; `pnpm -C devhub check:no-emoji` passed. | locally verified, user acceptance still open |
| H.3.3 logRedactor unit coverage is at least 90% | `src/shared/utils/logRedactor.ts` now provides a shared renderer-safe log redaction module for assignment-style secrets, bearer tokens, OpenAI-style keys, generic `tok-` tokens, GitHub tokens, AWS access keys, JWT-like values, and URL credentials. `projectStore.addLog()` redacts every incoming `LogEntry.message` before storing it for `LogPanel`, so sensitive process output is not persisted in the renderer log store. | `pnpm -C devhub exec vitest run src/shared/utils/logRedactor.test.ts --coverage --coverage.include=src/shared/utils/logRedactor.ts --maxWorkers=1` passed with 5 tests and V8 coverage `100% statements / 100% branches / 100% functions / 100% lines`; `pnpm -C devhub exec vitest run src/shared/utils/logRedactor.test.ts src/renderer/stores/projectStore.test.ts --maxWorkers=1` passed with 6 tests. | locally verified, user acceptance still open |
| H.6.2 component keyboard and ARIA audit reports zero blocking findings | `scripts/a11y-component-audit.mjs` scans renderer TSX surfaces for named dialogs, switches, roving tabindex groups, focusable regions, and keyboard activation. | `pnpm -C devhub a11y:component-audit` passed with 187 TSX files scanned, 10 audited ARIA surfaces, and 0 blocking findings. | locally verified, user acceptance still open |
| J.1.1 emoji scan reports zero emoji hits across the current app tree | `devhub/scripts/check-no-emoji.mjs` is wired as `pnpm check:no-emoji` and currently scans 779 files without emoji hits. | `pnpm -C devhub check:no-emoji` passed with `No emoji found in 779 files.` | locally verified, user acceptance still open |
| J.1.3 production dependency license gate passes locally | `devhub/scripts/check-license.mjs` validates production package licenses and documented manifest exceptions without adding external network calls. | `pnpm -C devhub check:license` passed with `472 production package entries validated; 1 manifest exception(s) documented.` | locally verified, user acceptance still open |
| J.1.4 SBOM generation is automated | `devhub/scripts/generate-sbom.mjs` generates a CycloneDX 1.5 SBOM from real `pnpm licenses list --json --prod` output; `package.json` exposes `sbom:generate` and `check:sbom`, and CI runs `check:sbom`. | `pnpm -C devhub check:sbom` passed and generated `out/sbom.cyclonedx.json` with 509 components. | locally verified, user acceptance still open |
| J.1.5 no-emoji detection script exists and is executable | `devhub/package.json` exposes `check:no-emoji`, and the same gate is also part of `pnpm lint`; current execution returns zero matches. | `pnpm -C devhub check:no-emoji` passed. | locally verified, user acceptance still open |
| J.1.6 CI contains a zero-egress diagnostic guard | `devhub/.github/workflows/ci.yml` now includes the focused no-outbound diagnostic Vitest gate in both CI jobs. | Focused no-outbound diagnostic Vitest passed locally with 1 test and 133 skipped. | locally verified, user acceptance still open |
| J.1.9 TypeScript type check reports zero errors for the current app | The app-level `typecheck` script runs `tsc --noEmit`; the current run completed with exit code 0 after the attached topology test selector fix. | `pnpm -C devhub typecheck` passed. | locally verified, user acceptance still open |
| J.1.10 ESLint gate is explicit in CI and touched files pass locally | `devhub/.github/workflows/ci.yml` keeps `pnpm lint`; the latest touched-file ESLint run covered watchdog, AI task monitor, timeline, alias store, and generated governance script changes. | `pnpm -C devhub exec eslint ... --max-warnings=0` passed for touched code files; scoped `git diff --check` passed. | locally verified, user acceptance still open |
| K.1 project LICENSE exists, but current license is AGPL-3.0 rather than the recommended Apache-2.0 | `devhub/LICENSE` exists and `README.md` accurately states AGPL-3.0. The license was not changed because relicensing is a product/legal owner decision. | Local file audit verified `devhub/LICENSE`; no license text was modified. | partial legal/product decision required, user acceptance still open |
| K.2 NOTICE and third-party license evidence exist | `devhub/NOTICE` now points to `pnpm check:license` and the generated CycloneDX SBOM as the complete third-party component/license record. | `pnpm -C devhub check:license` passed; `pnpm -C devhub check:sbom` passed with 509 components. | locally verified, user acceptance still open |
| K.3 SECURITY.md defines vulnerability disclosure handling | `devhub/SECURITY.md` now documents supported versions, private reporting content, response targets, and security-sensitive boundaries. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| K.4 PRIVACY.md is bilingual and local-first | `devhub/PRIVACY.md` now contains English and Chinese sections covering local data, sensitive data, network use, and user control. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| K.5 EULA and TERMS files exist | `devhub/EULA.md` and `devhub/TERMS.md` now document AGPL preservation, local system access, acceptable use, and local-first operation. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| K.6 CODE_OF_CONDUCT.md exists | `devhub/CODE_OF_CONDUCT.md` now defines expected conduct, unacceptable behavior, enforcement, and reporting boundaries. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| K.7 CONTRIBUTING.md exists | `devhub/CONTRIBUTING.md` now documents setup, required checks, PR expectations, and issue quality. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| K.8 fair-use statement exists in README and in-app About settings | `devhub/README.md` contains a trademark and fair-use notice, and `SettingsDialog` now renders an Advanced settings "关于与商标声明" section with vendor-name/logo fair-use boundaries, AGPL-3.0 license pointer, NOTICE/SBOM evidence pointer, and no endorsement/agency claim. | `pnpm -C devhub exec vitest run src/renderer/components/settings/SettingsDialog.statusbar.test.tsx --maxWorkers=1 --reporter=verbose` passed with 1 file / 6 tests; touched-file ESLint, `tsc --noEmit`, and `check:no-emoji` passed. | locally verified, user acceptance still open |
| K.10 production dependency license blacklist gate passes, but project-level AGPL blacklist remains a legal conflict | `check-license.mjs` blocks forbidden production packages and unapproved production dependency licenses; it currently passes. The acceptance item's AGPL blacklist conflicts with the existing project `LICENSE`, so project relicensing remains blocked on owner/legal decision. | `pnpm -C devhub check:license` passed with 472 production package entries and 1 documented exception. | partial legal/product decision required, user acceptance still open |
| L.1 GitHub Issue templates include at least five categories | `devhub/.github/ISSUE_TEMPLATE/` now includes bug, feature, performance, security-boundary, and UI/UX templates plus config. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| L.2 GitHub PR template contains more than eight required review fields | `devhub/.github/pull_request_template.md` now includes summary, scope, spec link, implementation notes, validation, risk, rollback, privacy/security impact, and user-facing changes. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| L.3 CI has at least ten required gates | `devhub/.github/workflows/ci.yml` now includes install, lint, typecheck, no-emoji, Zod SoT, license, SBOM, no-cloud, no-OCR, zero-egress, i18n, unit tests, build, and a11y smoke on Windows, with matching core gates on Linux. | CI file audit plus local no-cloud/no-OCR/license/SBOM/no-outbound commands passed. | locally verified, user acceptance still open |
| L.5 public roadmap exists | `devhub/ROADMAP.md` now records quarterly direction and monthly plan. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| L.6 changelog follows Keep a Changelog structure | `devhub/CHANGELOG.md` now contains an Unreleased section with Added, Changed, and Security groups. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| L.7 theme pack format is defined | `devhub/docs/r8/theme-pack-format.md` now defines a local JSON shape, rules, validation expectations, and import/export boundaries. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| L.8 SKILL metadata format is defined | `devhub/docs/r8/skill-metadata-format.md` now defines required YAML fields, sandbox levels, and runtime guarantees aligned with the implemented SKILL tests. | File audit plus scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| L.10 README contains fair-use statement and main badges | `devhub/README.md` now contains license, CI, stars, Electron, React, and TypeScript badges plus the trademark/fair-use notice. | README scoped diff/no-emoji checks passed. | locally verified, user acceptance still open |
| B.6.1 提供至少 3 个节日主题（春节 / 圣诞 / 万圣节） | `HOLIDAY_THEME_DEFINITIONS` now ships `spring-festival`, `christmas`, and `halloween` production decoration packs with local date windows, palette CSS variables, and real decoration configs. `SettingsDialog` renders the registry through `holiday-theme-list`. | `pnpm -C devhub check:theme-seasonal-visual-contract` passed; `theme-language.test.ts` verifies the three IDs and Spring Festival date behavior. | locally verified, user visual/date acceptance still open |
| B.6.2 节日主题在到达日期前自动询问启用，用户可关闭 | `promptHolidayThemeIfNeeded()` prompts once per holiday/year before the date and persists `accepted` / `dismissed`; `holidayDecorationsEnabled` and `holidayAutoPromptEnabled` are persisted appearance settings exposed in Settings. | `pnpm -C devhub exec vitest run src/renderer/theme/theme-language.test.ts src/renderer/components/settings/SettingsDialog.theme-editor.test.tsx --maxWorkers=1` passed with 13 tests. | locally verified, real seasonal prompt observation still open |
| B.6.3 专注工作模式开关屏蔽全部节日装饰 | `holidayFocusMode` is a persisted appearance setting; `resolveHolidayTheme()` suppresses active holiday decoration while preserving the base theme, and `applyHolidayThemeToDocument()` clears `data-holiday-theme` when suppressed. | `theme-language.test.ts` verifies focus work mode suppression; `SettingsDialog.theme-editor.test.tsx` verifies the Settings surface. | locally verified, user visual acceptance still open |
| B.7.1 切换主题时图表配色同步变化 | `theme-tokens.css` defines per-palette `--chart-*` semantic tokens; `ProcessSparkline` and `StatusBarProcessHistoryWidget` use `var(--chart-series-1)`, grid, and warning tokens instead of hard-coded accent defaults. `e2e/example.spec.ts` now reads runtime graph/topology CSS variables from the real Electron renderer while switching theme from `modern-light` to `cyberpunk`, proving the browser-applied graph token set changes with the theme. | `check:theme-seasonal-visual-contract`, focused Vitest, targeted ESLint, and `tsc --noEmit` passed. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with runtime topology token assertions. | locally verified with Electron runtime token evidence, user visual acceptance still open |
| B.7.2 拓扑图节点颜色与主题色板对齐 | `theme-tokens.css` defines per-palette topology node/edge tokens; `DagCanvas` reads computed CSS tokens for Cytoscape styles, while `GraphCanvas` and `TopologyMiniWidget` expose `data-theme-sync="topology-palette"` and use topology token attributes. The P8.2 Electron E2E now asserts the running renderer applies cyberpunk topology tokens `--topology-node-process=#00ffff`, `--topology-edge-network=#39ff14`, and `--topology-node-label=#ffffff`, each changed from the seeded `modern-light` values. | `pnpm -C devhub exec vitest run src/renderer/components/dag-editor/DagCanvas.test.tsx src/renderer/components/topology/FullScreenTopologyView.test.tsx src/renderer/components/statusbar/StatusBar.test.tsx --maxWorkers=1` passed with 9 tests. `pnpm -C devhub test:e2e --grep "P8.2" --reporter=line --workers=1` passed with runtime topology token assertions. | locally verified with Electron runtime token evidence, user visual acceptance still open |
| B.7.3 高对比度主题下文字 / 图表均可读 | `cyberpunk` and `swiss` palette token blocks now define explicit `--chart-text-color`, `--chart-grid-color`, `--topology-node-label`, node colors, and edge colors rather than inheriting low-contrast defaults. `verify-theme-seasonal-visual-contract.mjs` now computes WCAG contrast against each high-contrast palette's real `--surface-950` background and requires chart/topology text tokens to be at least 4.5:1 while graph series, node, and edge tokens must be at least 3:1. | `pnpm -C devhub check:theme-seasonal-visual-contract` passed with high-contrast WCAG ratio checks; no-emoji and scoped diff checks passed. | locally verified with WCAG contrast gate, user visual acceptance still open |

## Non-Completion Boundary

The active overall R8 goal is still not complete.

Open executable blockers remain in `prompts/0503-2`, not in `prompts/0503`:

- `prompts/0503-2/R8.B/spec-02-port-floating-window.md`: real second-display packaged assertion / live multi-display hardware verification.
- `prompts/0503-2/R8.B/spec-11-window-virtual-desktop.md`: true Windows virtual-desktop switch event stream and live physical monitor disconnect/reconnect hardware verification.
- `prompts/0503-2/R8.C/spec-17-watchdog-subprocess.md`: Windows Service install/uninstall through explicit UAC.
- `prompts/0503/28-final-acceptance-checklist.md` H.2.4 now has local static installer-contract evidence, but still requires a packaged Windows uninstaller/manual user acceptance pass before final acceptance can be marked complete.

Open survey and acceptance items remain in `prompts/0503`, but they are not code-completion checkboxes. They require either user survey decisions, PRD/spec conversion, or final user acceptance.
